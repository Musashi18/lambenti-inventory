# HERMES_STATE

## Current session — Lead-Time Horizon expansion and Momentum Engine classifier hardening
- Expanded root-dashboard `Lead-Time Horizon` from the 10 longest item planning windows to the 16 longest, filling the full-width signal card with an exact, longest-first list while retaining source labels (observed/manual/catalog) and proportional bars. Live production `/` rendered all 16 rows, from `PSU-12V-GS-UL` at `44.8d` through `M2_2_3MM_THREADED` at `11.9d`, with no horizontal overflow or overlap.
- Hardened passive Founder OS task/activity interpretation before Momentum metrics: only strong artifact types or meaningful artifact-file extensions can qualify as direct evidence; generic `file` metadata and Founder OS JSONL telemetry no longer become work solely because a category/signal exists. The detector now raises the work confidence floor to 55%, infers an `Unknown` category only from strong task evidence, clamps reported active duration to the observed wall-clock interval, and deduplicates same-interval daily/weekly exports with explicit idle/distraction winning any conflict. It remains conservative: uncertain, idle, and distraction blocks are excluded from work metrics and never create operational progress.
- Added regression coverage for generic-file rejection, source-code-file acceptance, conflicting-export safety, duration clamping, unknown-to-firmware inference, and the 16-row horizon. Live parser evidence after the change: 1,007 recent events classified as 466 `WORK`, 388 `UNCERTAIN`, 95 `IDLE`, and 58 `DISTRACTION`; only `32.29h` of direct-evidence work is eligible across that read window. No inventory, purchasing, receiving, accounting, supplier, or tracking data changed.
- Verification passed: targeted 29-test slice, `npm run typecheck`, `npm run lint`, full serial Vitest (88 files / 458 tests), 6144 MB production build/restart with `/` HTTP 200, UI-contract smoke across all routes, root browser 16-bar/652px panel/no-horizontal-overflow probe, and 0 root browser console/JS errors.

## Current session — Four-week work rhythm visual refinement
- Refined the root dashboard `Four-week work rhythm` into a quieter dark data panel: removed the high-saturation teal gradient/glow/drop shadow, reduced the chart height, used slate borders and a restrained teal current-week bar, and added a faint midline so comparisons remain easy without decorative chrome.
- Each weekly slot now keeps its exact date visible and distinguishes `Current`, `Recorded`, and `No data`; list-item accessibility text also carries the recorded/no-recorded state. The panel remains Sunday–Saturday and direct-work-evidence-only; no activity, database, inventory, purchasing, receiving, accounting, or supplier data semantics changed.
- Verification passed: targeted page/momentum tests (3 tests), lint, typecheck, full serial Vitest (88 files / 454 tests), 6144 MB production build plus local `/` HTTP 200, UI-contract smoke, browser DOM geometry (892px panel; four unclipped 96px tracks; no horizontal overflow), and 0 browser console/JS errors.

## Current session — Momentum Engine Sunday weekly work history
- Added a four-bar `Four-week work rhythm` panel inside the root dashboard Momentum Engine. It is calendar-anchored to `America/Toronto` Sunday–Saturday weeks, always renders four chronological slots (including zero-hour weeks), labels the current partial week, shows the exact hour total, and exposes accessible per-week summaries/tooltips.
- Corrected the weekly accumulator arithmetic so the four slots are separated by seven days rather than one day. Sunday-boundary blocks are split between weeks. Regression coverage proves the Sunday reset, a Saturday-to-Sunday split, zero-week rendering, and exclusion of idle/distraction time.
- Founder OS aggregate persistence is live in `FounderActivityWeek`: the dashboard materializes privacy-preserving weekly totals (minutes, verified-work blocks, source-block count, observed time) while raw activity detail remains in the local Founder OS JSONL. Local production browser loading created/updated 3 aggregate weeks; this feature does not touch stock, purchasing, receiving, invoices, accounting, or supplier data.
- Verification: `npx prisma migrate status` confirms the schema is current; Prisma client generation succeeded after releasing the port-5173 lock; lint, typecheck, targeted Momentum/page tests (3 tests), production build, and a prior full serial Vitest suite (88 files / 454 tests) passed. Browser production smoke at `/` showed the rendered four bars, 3 recorded weeks, no clipping/overlap, and 0 console/JS errors.

## Current session — dynamic nested BOM cost propagation hardening
- Confirmed the shared active-item cost resolver is the live source for `/inventory/items`, `/inventory/valuation`, and dashboard valuation. It resolves accounting landed cost first, then manual item cost, then recursive active-BOM rollups for finished goods with no manual override; it is re-run on every active cost read, so a constituent cost change flows through nested subassemblies to `LAMBENTI_PACKAGE` without persisting a stale finished-good price.
- Hardened the resolver to memoize unavailable rollups per resolution pass and always release recursion guards; malformed legacy cycles therefore return unpriced rather than looping or poisoning unrelated rollups. BOM line create/edit operations now reject indirect active dependency cycles before saving.
- Supplier-entry price changes now use `revalidateWorkspace()`, refreshing the dashboard as well as Items and Valuation. Regression coverage proves a missing package component keeps the package unpriced, supplying that cost enables the nested rollup, and later component repricing updates the downstream package total; it also proves cycle rejection/containment.
- Current live evidence: `LAMBENTI_MAIN_UNIT` resolves to `USD 9.4647` and `LED_CONN` to `USD 1.9988`; `LAMBENTI_PACKAGE` remains intentionally unpriced because direct package components `NOTE`, `GUIDE`, and `RETAIL_BOX` have no unit-cost evidence. No cost, stock, accounting, purchasing, or BOM data was mutated.
- Verification: targeted pricing/BOM/supplier-action suite (3 files / 15 tests), lint, typecheck, full serial Vitest (87 files / 452 tests), 6144 MB production build, local production HTTP 200 for Items/Suppliers/Valuation, Items browser DOM cost inspection, and 0 browser console/JS errors.

## Current session — dashboard Momentum Engine / Atlas retirement
- Moved Momentum Engine into the root dashboard Signals grid directly beneath `Lead-Time Horizon`; both panels use `lg:col-span-2`, and live DOM measurement confirmed the same 926px width with Momentum immediately below the lead-time card. The dashboard now calls the standalone `getFounderOsMomentum()` service instead of Atlas Mission Control.
- Reworked passive Founder OS classification before scoring: blocks are `WORK`, `IDLE`, `DISTRACTION`, or `UNCERTAIN`. A block only counts as work with direct artifact/file/git evidence or a specific work signal; explicit idle/depth/idle-duration conditions win, distraction never contributes work time, and weak/unknown evidence is excluded. Dashboard totals, category bars, confidence, and verified/excluded counts are current-day scoped.
- Retired the Atlas UI without deleting its implementation: `/atlas`, `/atlas/overlay`, and `/atlas/simulator` now immediately redirect to the dashboard; their former sources are retained in comments. Atlas is removed from sidebar and smoke route manifests. `/api/atlas/mission-control` is disabled with HTTP 410. No inventory, accounting, purchasing, receiving, tracking, or supplier-message mutation occurred.
- Final verification: `npm run lint`, `npm run typecheck`, full `npm run test:serial` (87 files / 448 tests), production build/restart via `npm run lambenti:serve:verified`, `npm run smoke:ui-contracts`, and `git diff --check` all passed. Browser `/` renders Momentum Engine with no sidebar Atlas link; browser `/atlas` renders the dashboard; Atlas API returned `{ "error": "Atlas has been retired." }` with HTTP 410.

## Current session — Atlas exact Phase I goal-position bridge
- Atlas previously exposed an evidence-weighted graph/readiness score, but it did not make the physical 25-unit Phase I position first-class or consistently distinguish ledger-built packages from directly buildable or merely material-covered units. Added a typed, sanitized `goalPosition` DTO sourced from the existing dashboard `launchReadiness` summary: target package, ledger-built count, direct build capacity, material coverage, separate build/input gaps, status, named physical constraint, next human-gated workflow, and curated graph milestones from first batch through first 50 sales.
- `/atlas` now renders `Goal Position · Live Operational Truth` directly below the Command Dock. It states the exact physical target without converting parts or planning baselines into finished product; shows separate `Ledger-Built Packages`, `Direct Builds Now`, `Material Covered`, `Build Gap`, and `Input Gap` metrics; makes Phase I/II milestones explicit as measured, stale, or unmeasured; and links only to the existing human-gated next workflow. `/atlas/overlay` now carries the compact ledger-built/direct-buildable signals. Atlas API remains presentation-sanitized/read-only.
- Live production evidence on the current local database: `0/25` `LAMBENTI_PACKAGE` units ledger-built, `0` directly buildable now, `0/25` material-covered, `25` build gap, `25` input gap, status `BLOCKED`, named direct constraint `LAMBENTI_MAIN_UNIT`. This is the current operational position, not a forecast or mutation.
- Verification passed: focused Atlas/smoke suite (`5` files / `23` tests), `npm run typecheck`, `npm run lint`, full `npm run test:serial` (`87` files / `446` tests), guarded production build/runtime via `MSYS_NO_PATHCONV=1 npm run lambenti:serve:verified -- --routes=/atlas,/api/atlas/mission-control,/atlas/overlay,/atlas/simulator`, `npm run smoke:ui-contracts`, and `git diff --check`. All four Atlas routes returned HTTP 200. Browser DOM/API checks found goal metrics + four milestones, correct API physical tuple, `0` clipped descendants, no horizontal overflow, no console errors, and no browser/API `sourceRef`, `var/accounting-documents`, or `C:/Users` leak. No stock, purchasing, receiving, accounting, invoice/payment, tracking, Alibaba, delivery, or supplier-message side effects occurred.

## Current session — Ship24 delivered backend archive / 10-active cap
- Implemented automatic Ship24 backend archive cleanup when a local tracking row becomes `DELIVERED`: after a successful Ship24 delivered refresh, the app now finds matching active Ship24 trackers and PATCHes them with `isSubscribed:false` and `isTracked:false` so delivered shipments stop consuming Ship24's 10 active-shipment availability. Local delivered tracking history, raw status, event timeline, and audit evidence are retained.
- Active/due Ship24 refreshes also archive already-delivered local rows on the Ship24 backend before polling active shipments, and Ship24 active polling is capped to 10 rows even if a caller passes a higher limit. Tracking workbench/scheduler messages now surface how many delivered backend trackers were archived.
- Added regression coverage for delivered Ship24 refresh archive behavior, pre-refresh cleanup of already-delivered rows, and the 10-active Ship24 cap. Verified targeted tracking/API/scheduler tests (`3` files / `41` tests), `npm run typecheck`, `npm run lint`, full `npm run test:serial` (`87` files / `441` tests), and `NODE_OPTIONS='--max-old-space-size=6144' npm run build`. No stock, PO receiving, accounting, invoice/payment, Alibaba capture, delivery-confirmation-as-receipt, or supplier-message side effects were performed.

## Current session — dashboard package build-capacity correction
- Root cause: the dashboard package capacity path was treating recursively buildable finished-good subassemblies as if they were already available package inputs. Live data had `LAMBENTI_MAIN_UNIT` on-hand/available `0` but stage-local buildability `26`, so `LAMBENTI_PACKAGE` incorrectly appeared as `26` buildable. The package build action is actually blocked until main units are built/ledgered.
- Fixed `summarizeBuildCapacity()` so `LAMBENTI_PACKAGE` capacity is computed from direct package BOM input availability only. Advisory `buildableSubassemblyCapacity` remains visible internally/stage-locally, and `buildRows` can still show `LAMBENTI_MAIN_UNIT 26` / `LED_CONN 30` as subassembly build stages without inflating package buildability. Dashboard and Atlas copy now say package buildability requires already-built package inputs/subassemblies.
- Added regression coverage in dashboard unit and integration tests proving unbuilt finished-good subassemblies keep package buildability at `0`, even when their own BOMs can build 26+ units. Updated `DECISIONS.md` and the `lambenti-inventory-ops-shortcuts` dashboard nested-BOM reference so future agents do not reintroduce recursive package-capacity math.
- Verification passed: targeted dashboard/page/Atlas tests (`4` files / `16` tests), `npm run lint`, `npm run typecheck`, full `npm run test:serial` (`87` files / `438` tests), `NODE_OPTIONS='--max-old-space-size=6144' npm run build`, managed production runtime for `/` HTTP 200, browser `/` showed `Buildable now 0`, `0 package builds now; subassemblies must already be ledger-built`, `LAMBENTI_PACKAGE 0 buildable`, `LAMBENTI_MAIN_UNIT 26 buildable`, and 0 console/JS errors, plus `npm run smoke:ui-contracts` passed.

## Current session — Ship24 refresh parity, tracking cards, and Mission Control real-data polish
- Fixed the Ship24 refresh path so app/scheduler/API refreshes actively re-poll non-delivered rows instead of silently doing nothing when `nextRefreshAt` is in the future. `npm run agent:tracking-refresh` now defaults to `{ dueOnly:false }` for active-shipment parity with manual Ship24 dashboard refresh, while preserving an explicit `--due-only` option. `/api/tracking/refresh` smoke returned HTTP 200 with `scanned:1`, `refreshed:1`, `failed:0`, and heartbeat timestamps.
- Hardened the Ship24 adapter and status persistence: Ship24 request payloads now send supported `courierCode` hints (for UPS/FedEx/Canada Post/China Post) instead of `courierName`; `out_for_delivery` now normalizes to `OUT_FOR_DELIVERY`; and existing `TrackingEvent` rows are updated when a later provider refresh improves the normalized event status/location/raw payload. Live row `1ZV90R362090867759` now stores `currentStatus: OUT_FOR_DELIVERY`, `statusDescription: Out For Delivery Today`, `refreshStatus: SUCCESS`, `refreshError:null`, and the latest event status `OUT_FOR_DELIVERY` at Concord, ON.
- Repaired card presentation: `/tracking` open shipment cards are full-width/min-width-safe, long tracking/order labels wrap instead of hiding content, the shipment-progress rail wraps cleanly on small widths, and `OUT_FOR_DELIVERY` gets a distinct readable badge. `/atlas` Mission Control cards now state `Live database read`, expose command-dock cards as workflow links, show source count in Trust, remove hover popovers that could cover neighboring graph cards, and widen/space the Atlas dependency map so node cards do not overlap.
- Verification passed: targeted suite (`src/modules/tracking/service.integration.test.ts`, `src/app/tracking/page.test.ts`, `scripts/tracking-refresh.test.mjs`, `src/modules/atlas/service.test.ts`, `src/modules/atlas/evidence-adapters.test.ts`: 5 files / 41 tests), `npm run lint`, `npm run typecheck`, full `npm run test:serial` (87 files / 432 tests), and `NODE_OPTIONS='--max-old-space-size=6144' npm run build`. Fresh production runtime on `127.0.0.1:5173` returned `/api/tracking/refresh` HTTP 200 and `/api/atlas/mission-control` HTTP 200 with sourceCount 6, confidence 75, generatedAt current. Browser `/tracking` DOM found `Out For Delivery` visible, open shipment details readable, top-level card overlap count 0, and 0 console/JS errors; browser `/atlas` found live database read, command-dock links, 32 interactive buttons, top-level overlap count 0, no bad links, and 0 console/JS errors. No stock, receiving, purchasing, accounting, Alibaba capture/import, delivery confirmation, payment, or supplier-message side effects were performed.

## Current session — Atlas AI Layer functional layout repair
- Made the `/atlas` `Atlas AI Layer` functional as a read-only intelligence surface instead of inert buttons. Each action now updates selected-node output with an explicit insight panel: `Explain Why This Matters`, `Predict Delays`, `Suggest Next Action`, `Find Bottlenecks`, `Generate Execution Plan`, and `Estimate Launch Impact`. Output is derived only from sanitized Atlas presentation data, node score/risk/confidence/dependency state, existing blockers/evidence, forecasts, and linked human-gated workflows; it does not mutate stock, purchasing, receiving, accounting, tracking, Alibaba, payments, delivery, or supplier messages.
- Repaired the visual layout around the progress map. The legend is now normal-flow below the map instead of an absolute bottom overlay, Phase I card spacing/map height now leaves clearance for the actual card bodies, and the former right-rail `Atlas AI Layer`, `Dependency Pulse`, and `Evidence Stream` cards now fill the wide empty area under the map to the left of the inspector. `/atlas` also has bottom padding so the fixed theme toggle does not cover the lower cards.
- Verification passed: targeted Atlas/smoke tests (`scripts/smoke.test.mjs`, `src/modules/atlas/presentation.test.ts`, `src/modules/atlas/scoring.test.ts`: 3 files / 14 tests), `npm run typecheck`, `npm run lint`, guarded build through `MSYS_NO_PATHCONV=1 npm run lambenti:serve:verified -- --routes=/atlas,/api/atlas/mission-control,/atlas/overlay,/atlas/simulator`, managed runtime probes for those 4 routes all HTTP 200, deterministic Playwright `/atlas` probe with all 6 AI actions clicked/aria-pressed/readout markers present, no legend/card overlap, AI/dependency/evidence cards left of the inspector filling the empty map area, no theme-toggle overlap, no horizontal overflow, 0 browser console/page errors, and no browser/API leak of `sourceRef`, `var/accounting-documents`, or `C:/Users`; `npm run smoke:ui-contracts`; full `npm run test:serial` (`87` files / `430` tests).

## Current session — valuation concentration visual and Atlas suggested UI changes
- Implemented the `/inventory/valuation` visual slice. The existing `Value Concentration` section now shows share-of-total valuation rather than only relative-to-largest bars: `Top 5 Share`, `Largest Position`, `Long Tail`, a stacked concentration strip, ranked SKU bars with percentage + USD value, and a `Concentration Reading` note. It remains read-only and does not mutate stock, pricing, purchasing, accounting, receiving, Alibaba, delivery, payment, or supplier-message state.
- Implemented the suggested `/atlas` UI changes as a limited read-only slice, then evolved the map toward a Founder OS decision engine. `/atlas` now has an `Atlas Command Dock`; `Atlas Projection Navigation` for Roadmap/Dependency/Kanban/Timeline/Executive/Risk/Manufacturing/Launch/Founder Daily projections; `Command Palette` via Ctrl+K; `Search / Zoom / Path Controls`; `Founder Focus Mode`; `Strategic Playback`; first-class `Done So Far` vs `Needs Development`; selected/hover/focus dimming; layered strategic-score node cards with completion rings, heatmap status, predictive chips, and animated dependency-flow particles; an `Executive Intelligence Panel` with radial gauges and likely-completion windows; `Why This Node Is Here`; `What Would Move It`; and an `Atlas AI Layer` of read-only intelligence actions. Atlas still consumes sanitized `buildAtlasMissionControlView` data and does not expose raw `sourceRef` or private accounting paths.
- Verification passed: targeted Atlas/smoke tests (`scripts/smoke.test.mjs`, `src/modules/atlas/presentation.test.ts`, `src/modules/atlas/scoring.test.ts`: 3 files / 14 tests), targeted valuation source-contract test (`src/app/inventory/valuation/page.test.ts`: 3 tests), `npm run typecheck`, `npm run lint`, `NODE_OPTIONS='--max-old-space-size=6144' npm run build`, managed runtime probes for `/atlas`, `/api/atlas/mission-control`, `/atlas/overlay`, `/atlas/simulator`, and `/inventory/valuation` HTTP 200, `npm run smoke:ui-contracts`, full `npm run test:serial` (`87` files / `430` tests), browser `/atlas` interaction check showing default 18 nodes, `Done So Far` 3 visible, `Needs Development` 15 visible, search `Firmware` 1 visible/1 match, selected-path toggle, horizontal overflow 0, console/page errors 0, and no browser/API private-path leak. Browser `/inventory/valuation` evidence from the earlier slice showed `Top 5 Share 62%`, `Largest Position LOOSE_EC`, `Long Tail USD 695.17`, `Concentration Reading`, horizontal overflow 0, and console/JS errors 0.

## Current session — Atlas Momentum Engine worked-time refresh repair
- Root cause: the Founder OS minute snapshot cron was healthy (`activity_snapshots.jsonl` mtime current), but `activity_blocks.jsonl` was only refreshed during Founder OS review/export runs and was stale after the morning, so `/atlas` Momentum Engine hours appeared frozen. A second robustness gap in `src/modules/atlas/activity-events.ts` meant Atlas preferred the latest weekly period and could ignore a fresher current-day daily export.
- Fixed Atlas activity loading so a current/fresher daily export is merged with the latest weekly export and duplicate blocks are deduped across period labels by block identity. Added regression coverage in `src/modules/atlas/activity-events.test.ts` for stale-weekly/fresh-daily behavior.
- Fixed the Lambenti profile exporter path: `founder_os_activity_export.py` now upserts current period/label block rows instead of endlessly appending duplicates; added `founder_os_activity_export_tick.py` to refresh current daily and weekly exports quietly; created no-agent cron `25d050f6ccef` (`Lambenti Founder OS activity block export`) every 5 minutes and ran it successfully. Live API evidence after refresh: `/api/atlas/mission-control` reports `dailyTotalHours: 3.5`, `dailySectorWork` Planning `3h`, Engineering `0.5h`, Industrial Design `0.1h`, and browser `/atlas` renders `3.5h WORKED TODAY` with 0 console/JS errors.
- Verification passed: RED targeted test failed before code fix; targeted Atlas tests (`activity-events`, `scoring`, `service`: 3 files / 9 tests), `npm run typecheck`, `npm run lint`, `NODE_OPTIONS='--max-old-space-size=6144' npm run build`, full `npm run test:serial` (87 files / 428 tests), managed runtime probes for `/atlas`, `/api/atlas/mission-control`, `/atlas/overlay`, `/atlas/simulator` all HTTP 200, and `npm run smoke:ui-contracts` with no failures. No stock/accounting/purchasing/receiving/Alibaba/payment/delivery/supplier-message side effects occurred.

## Current session — Atlas interactive progress map upgrade
- Upgraded the new client-side `AtlasProgressMap` at `src/app/atlas/atlas-progress-map.tsx` and mounted it near the top of `/atlas`. It now renders a more functional visual map with six Phase Lens filters (`Everything`, `Launch Path`, `Needs Development`, `Done So Far`, `Trust Gaps`, `Next Move`), a Mission Focus Strip for highest leverage/current bottleneck/largest risk, a Development Area Summary, clickable node cards with area/horizon/completion/risk/trust/blocker metadata, SVG dependency lines with arrowheads, selected/dependency/unlock highlighting, a readable legend, Node Inspector, Dependency Pulse, and sanitized Evidence Stream. The map uses `buildAtlasMissionControlView`, so raw `sourceRef` values and private accounting-document paths are not serialized to the browser.
- Updated `/atlas` UI-contract coverage so `npm run smoke:ui-contracts` now asserts the stronger map markers (`Done So Far`, `Needs Development`, `Mission Focus Strip`, `Development Area Summary`, `Node Inspector`, `Dependency Pulse`, `Evidence Stream`) in addition to the existing command-deck markers.
- Verification passed: targeted Atlas/smoke tests (`src/modules/atlas/presentation.test.ts`, `src/modules/atlas/scoring.test.ts`, `scripts/smoke.test.mjs`: 3 files / 13 tests), `npm run typecheck`, `npm run lint`, `NODE_OPTIONS='--max-old-space-size=6144' npm run build`, managed runtime probes for `/atlas`, `/api/atlas/mission-control`, `/atlas/overlay`, and `/atlas/simulator` all HTTP 200, `npm run smoke:ui-contracts`, full `npm run test:serial` (`87` files / `427` tests), direct API sanitization probe (`sourceRef:false`, `var/accounting-documents:false`, `Cache-Control: private, no-store`), and browser `/atlas` interaction/visual checks showing all 18 nodes by default, working `Done So Far`/`Needs Development`/focus-strip controls, 0 console/JS errors, and no horizontal overflow.

## Current session — Atlas roadmap implementation pass
- Implemented the practical Atlas roadmap items from the visual map while preserving the read-only Founder OS boundary. Added `src/modules/atlas/presentation.ts` with evidence privacy tiering/sanitizers, a sanitized mission-control view, daily overlay brief, and decision-timeline entries. `/api/atlas/mission-control` now exposes the sanitized read-only Atlas DTO behind `atlas:view` with `Cache-Control: private, no-store`; API/browser evidence confirms the payload includes `dailyBrief` and `decisionTimeline` but does not include `sourceRef` or raw accounting-document paths.
- Made stale-evidence detection real in `src/modules/atlas/scoring.ts`: stale signals are counted in `evidenceCoverage.staleEvidenceCount`, reduce coverage confidence, and increase residual risk for affected nodes. `/atlas` now renders `Stale Signals`, an Atlas privacy marker, and a `Decision Timeline`; `/atlas/overlay` now renders compact `Next Action`, `Top Risk`, `Velocity Caveat`, and `Confidence Marker` cards from the shared presentation helper.
- Verification passed after final edits: targeted Vitest (`src/modules/atlas/presentation.test.ts src/modules/atlas/scoring.test.ts src/app/api/route-security.test.ts scripts/smoke.test.mjs`: 4 files / 15 tests), `npm run typecheck`, `npm run lint`, full `npm run test:serial` (`87` files / `427` tests), `NODE_OPTIONS='--max-old-space-size=6144' npm run build`, managed runtime rebuild/probes for `/atlas`, `/atlas/overlay`, `/atlas/simulator`, and `/api/atlas/mission-control` all HTTP 200, full `npm run smoke:ui-contracts` with no failures, API probe showing `Cache-Control: private, no-store`, sanitized payload (`hasSourceRef:false`, `hasRawAccountingPath:false`), and browser `/atlas` DOM/console check showing `Decision Timeline`, `Atlas privacy tier`, no `sourceRef`, and no horizontal overflow. No stock/accounting/purchasing/receiving/Alibaba/payment/delivery/supplier-message side effects occurred.

## Current session — Atlas security and recursive next-step research
- Researched current Atlas implementation and external secure-coding guidance. Atlas remains a read-only Founder OS slice over existing dashboard/tracking/accounting/automation evidence, but its route authorization was too broad because `/atlas`, `/atlas/overlay`, and `/atlas/simulator` used generic `item:view`, which the `AGENT` role also has. Added explicit `atlas:view` permission for human operator roles and kept it out of `AGENT`, aligning with OWASP least-privilege/deny-by-default guidance.
- Hardened `/atlas/simulator` input handling: focused-hours query values are now server-side bounded to 1–12 hours/day in 0.5h increments, with non-finite input falling back to 6h. The scenario dropdown was already allowlisted by `AtlasScenarioKind`; the numeric bound closes the client-side-min/max bypass path.
- Verification passed: targeted tests (`src/modules/auth/permissions.test.ts src/modules/atlas/scenarios.test.ts src/modules/atlas/scoring.test.ts src/modules/atlas/activity-events.test.ts src/modules/atlas/service.test.ts`: 5 files / 20 tests), `npm run typecheck`, `npm run lint`, `NODE_OPTIONS='--max-old-space-size=6144' npm run build`, full `npm run test:serial` (`86` files / `423` tests), managed runtime rebuild/probes for `/atlas`, `/atlas/overlay`, and `/atlas/simulator`, full `npm run smoke:ui-contracts`, browser `/atlas` DOM/console check with 0 console/JS errors and no horizontal overflow, and HTTP evidence that `hours=999` renders `12 Focused Hours / Day` rather than `999`. No stock/accounting/purchasing/receiving/Alibaba/payment/delivery/supplier-message side effects occurred.

## Current session — Atlas Founder OS activity coverage bridge
- Enabled Atlas to read the profile-local Founder OS passive activity export at `C:/Users/musas/AppData/Local/hermes/profiles/lambenti/founder_os/activity_blocks.jsonl` through new `src/modules/atlas/activity-events.ts`. `getAtlasMissionControl()` now loads these activity events by default, maps high-confidence/high-leverage blocks into velocity evidence, and keeps time-only activity from increasing graph completion unless a future event explicitly carries a progress contribution.
- Replaced the stale disabled-coverage state with a safer projection gate: Atlas now distinguishes `coverage enabled but current validated sample too small` from `coverage not enabled`. With the current live local data, `/atlas` no longer renders the disabled-coverage phrase; it renders the Founder OS coverage-enabled caveat because the current validated high-leverage sample is only about `0.3h`, below the `~2h` minimum for a credible date. This avoids the misleading 2057 date produced by naively projecting from a tiny partial-week sample.
- Verified: targeted Atlas tests (`src/modules/atlas/activity-events.test.ts src/modules/atlas/scoring.test.ts src/modules/atlas/service.test.ts`, 3 files / 6 tests), `npm run typecheck`, `npm run lint`, `NODE_OPTIONS='--max-old-space-size=6144' npm run build`, local production runtime at `http://127.0.0.1:5173`, HTTP probes for `/atlas`, `/atlas/overlay`, and `/atlas/simulator`, and browser `/atlas` console check with 0 messages/errors. The Founder OS snapshot cron `b006f706d239` is enabled and running every 1 minute.

## Current session — Atlas functionality check and visual command deck
- Checked current Atlas functionality from the live repo/runtime: `/atlas`, `/atlas/overlay`, and `/atlas/simulator` are route-integrated and read-only over existing inventory/tracking/accounting/automation evidence. Targeted Atlas service/evidence/scoring/scenario tests passed before changes (`4` files / `8` tests), and the existing page rendered with `0` browser console/JS errors.
- Main improvement found: Atlas was functionally useful but too text/card-heavy for a “Founder OS” command surface. Added an `Atlas Visual Command Deck` near the top of `/atlas` with a mission/probability orbit, Phase I critical-path dependency spine, and visual probability interval bands for launch, first-batch success, manufacturing delay risk, cash shortage risk, and burnout risk. This is UI-only and preserves Atlas’ no-mutation boundary: no stock, accounting, purchasing, receiving, Alibaba, payment, delivery, or supplier-message side effects.
- Verification passed after the visual pass: `npm run typecheck`, `npm run lint`, targeted Vitest (`src/modules/atlas/service.test.ts src/modules/atlas/evidence-adapters.test.ts src/modules/atlas/scoring.test.ts src/modules/atlas/scenarios.test.ts scripts/smoke.test.mjs`: `5` files / `13` tests), `NODE_OPTIONS='--max-old-space-size=6144' npm run build`, fresh `npm run start:local` runtime at `http://127.0.0.1:5173`, full `npm run smoke:ui-contracts`, full rendered `node scripts/browser-section-smoke.mjs --base-url=http://127.0.0.1:5173` (`24` pages), and browser DOM/visual/console evidence for `/atlas` showing the new command deck markers, `0` console/JS errors, and `horizontalOverflow:0`.

## Current session — Project Atlas Founder OS MVP
- Implemented Project Atlas as a functional read-only Founder Operating System slice inside the Lambenti inventory app. New `/atlas` Mission Control page uses an evidence-backed dependency graph, launch/company probability intervals, bottleneck/risk ranking, an Opportunity Engine, Reality Engine, Strategic Radar, Progress Galaxy, company-health intervals, and counterfactual statements. New `/atlas/overlay` provides the minimal Daily Mission Ring overlay view, and `/atlas/simulator` supports scenario comparisons for focused-hours, outsourcing PCB assembly, hiring manufacturing help, delaying packaging, and launching before non-critical perfection.
- Added Atlas kernel modules under `src/modules/atlas/`: seed graph, typed read model, evidence adapters over existing dashboard/tracking/accounting/automation sources, scoring/probability/risk logic, scenario simulator, and tests. The implementation is intentionally no-migration/read-only: Atlas consumes existing operational truth from `getDashboardSummary()`, `getTrackingDashboard()`, `getAccountingWorkbench()`, and `getAutomationOverview()` and does not create purchase requests, receive stock, post journals, pay invoices, run Alibaba capture, or send supplier messages.
- Integrated Atlas into navigation and route coverage: sidebar link, `scripts/smoke.mjs`, `scripts/browser-section-smoke.mjs`, `scripts/functional-qa-agent.mjs`, and `scripts/ui-contract-smoke.mjs`. Also moved the dark-mode toggle to the right on desktop (`lg:left-auto lg:right-4`) because adding Atlas made the fixed bottom-left toggle overlap the sidebar navigation at desktop widths.
- Verification passed: targeted Atlas/theme/smoke tests (`14` tests), `npm run typecheck`, `npm run lint`, full `npm run test:serial` (`85` files / `417` tests), `NODE_OPTIONS='--max-old-space-size=6144' npm run build`, managed production runtime probes for `/atlas`, `/atlas/overlay`, `/atlas/simulator`, and `/`, full `npm run smoke:ui-contracts`, full `npm run smoke` (`24` pages / `5` UI contracts / `4` JSON APIs / `3` CSV exports), `npm run smoke:browser` (`24` pages), browser console checks on `/atlas`, `/atlas/overlay`, and `/atlas/simulator` with `0` console/JS errors, and DOM evidence that the dark-mode toggle overlaps no sidebar nav links. No stock/accounting/purchasing/receiving/Alibaba/delivery/supplier-message side effects occurred.

## Current session — sidebar logo insertion and live editor
- Implemented a client-side sidebar logo manager in `src/components/sidebar-logo-control.tsx`, mounted from `src/components/sidebar.tsx`. Existing logo clicks open the edit modal; hover/focus shows a clean top-right `Edit` overlay; replacing the image happens through the dropbox already inside the edit modal. When no logo source is present the sidebar renders the same small dropbox directly.
- Repaired the logo distortion path: sidebar/editor images now render at their natural aspect ratio (`width:auto`, centered with `translateX(-50%)`, `object-fit:contain`) instead of forcing both width and height to the frame. Per operator follow-up, removed the visible edge/corner resize handles because they were just another image-size control and made the editor noisier. The editor now keeps a simpler functional control set: horizontal/vertical crop sliders, image-size slider, logo box height, edge blend width, and edge opacity. Dropped/chosen images are stored as local data URLs with settings in `localStorage` key `lambenti-sidebar-logo-settings`; save applies and closes, cancel leaves the current saved logo unchanged.
- Verification passed on the final tree: `npm run lint`, `npm run typecheck`, targeted Vitest (`src/components/sidebar.test.ts scripts/smoke.test.mjs`: 2 files / 9 tests), full `npm run test:serial` (`81` files / `409` tests), `NODE_OPTIONS='--max-old-space-size=6144' npm run build`, managed production runtime restart on `127.0.0.1:5173` build `iwhBsTxBAtY8NFg7uhsPi`, full `npm run smoke:ui-contracts` with no failures, and Playwright/browser smoke proving the edit modal has `resizeButtonCount:0`, no edge-drag copy, preserved logo aspect ratio (`ratioDelta:0.0001`), and no console/page errors. No stock, accounting, purchasing, receiving, Alibaba, delivery, or supplier-message side effects occurred.

## Current session — tracking/incoming UI compactness and row readability
- Implemented the requested `/tracking` compactness pass: `What Needs Attention` now renders only when provider setup, due refreshes, failed rows, or unlinked evidence actually need attention; archived tracking numbers are a collapsed `<details>` section by default; descriptions under `Tracking Workbench`, `Active Tracking Numbers`, and `Live Shipment Radar` were removed; bottom cards now order as `Add Tracking Evidence`, `Reorder Forecasting Input`, `Delivered Tracking History`, then `Archived Tracking Numbers`; bottom-card descriptions moved inside expanded disclosure content. `/incoming` no longer renders the `Packing Slip Duplicate Check` section.
- Added alternating row backgrounds for organized item/row lists: `/inventory/items` grouped catalog rows, shared `DashboardTable` rows including valuation grouped tables, `/incoming` receipt lines, and tracking delivered/archive/lead-time rows. Used `bg-white`/`bg-slate-50` so existing dark-theme overrides keep the alternating contrast dark-mode safe. Updated source-contract tests and the UI-contract smoke marker for `/incoming`; no stock, PO receiving, accounting, purchasing, Alibaba, delivery, or supplier-message side effects occurred.
- Verification passed after the final patch: targeted Vitest (`src/app/tracking/page.test.ts src/app/incoming/page.test.ts src/app/inventory/items/page.test.ts src/app/inventory/items/items-catalog.test.ts src/app/inventory/valuation/page.test.ts`: 5 files / 16 tests), `npm run typecheck`, `npm run lint`, full `npm run test:serial` (`81` files / `407` tests), `NODE_OPTIONS='--max-old-space-size=6144' npm run build`, runtime restart on `127.0.0.1:5173` build `baqHCCp8sTSGVRSZ9jkd6` with HTTP `200` probes for `/tracking`, `/incoming`, `/inventory/items`, `/inventory/valuation`, and full `npm run smoke:ui-contracts` with no missing markers, console errors, page errors, or failures. Browser DOM evidence on `/tracking` showed no `What Needs Attention` for the current no-issue state, hidden removed descriptions, and collapsed bottom cards in the requested order; `/incoming` showed no packing-slip duplicate section; `/inventory/items` showed alternating dark-mode row backgrounds.

## Current session — tracking archive UI, accounting bundle dedupe, and BOM-aware low-stock filtering
- Implemented requested `/tracking` UI changes: active-card Archive/Delete controls are no longer visible on the card and now live inside the collapsed `Shipment Details` section under `Tracking Data`; an `Archived Tracking Numbers` section is rendered at the bottom of the page from `dashboard.archivedRows`, with archived rows retained as provenance and not active refresh/delivery/receiving proof.
- Root-caused the `/accounting` invoice-bundle dropdown duplicates to historical `VOID` supplier invoices still being included in the source-document review dropdown. Live read-only DB check showed `38` supplier invoices, `29` voided duplicates, and one PO with `30` invoice rows (`29` void / `1` active). The UI now offers only non-VOID invoice bundles, labels options with status/PO suffix, service-side attachment rejects direct VOID invoice selection, PO fallback ignores VOID invoices, and analyzer matching no longer suggests VOID invoice bundles.
- Dashboard low-stock logic now uses active BOM data: low-stock components used only by finished-good BOM parents whose finished build is stocked at/above its reorder point are removed from current `lowStockItems` and shown separately as `Not Currently Needed Low Stock Components` with the finished-build coverage reason. Components for under-covered finished builds remain in current low stock.
- Verification: targeted Vitest passed (`src/app/tracking/page.test.ts src/app/accounting/page.test.ts src/app/page.test.ts src/modules/accounting/documents.integration.test.ts src/modules/dashboard/service.integration.test.ts src/modules/dashboard/service.test.ts`: 6 files / 27 tests); `npm run typecheck` passed; `npm run lint` passed; full `npm run test:serial` passed (`81` files / `404` tests); `NODE_OPTIONS='--max-old-space-size=6144' npm run build` passed. Production runtime restarted on `127.0.0.1:5173` build `cFi8on2lYwt8CQfI9Eki3`; HTTP probes `/`, `/tracking`, `/accounting` returned `200`. Browser evidence: `/tracking` shows `Archived Tracking Numbers` at page bottom and Archive/Delete buttons only after expanding shipment details; `/accounting` and `/` rendered with `0` browser console/JS errors; `/` includes `Not Currently Needed Low Stock Components`. `npm run smoke:ui-contracts` passed for `/`, `/tracking`, `/accounting` route markers and console checks but exited `1` on unrelated existing marker drift in `/inventory/items` (`Needs Cost`) and `/incoming` (`Packing Slip Duplicate Check`).

## Current session — accounting OCR and tracking archive repair
- Fixed accounting upload OCR/text extraction reliability for Windows local production: PDF utilities now resolve Git-for-Windows Poppler executables such as `D:/Git/mingw64/bin/pdftotext.exe` when the Next server process does not inherit the Git Bash PATH, and duplicate uploads of a previously unreadable/unextracted `AccountingDocument` now re-run extraction instead of returning the stale empty row forever. Reprocessed live `8574184905.pdf`: status `ANALYZED`, extracted text length `1197`, classification `CUSTOMS_DOCUMENT`, stock movement delta `0`, supplier invoice delta `0`.
- Fixed tracking supersession/archive behavior: when an active linked tracking number refreshes to `DELIVERED`, older active same-order/same-PO tracking rows are automatically marked `ARCHIVED`, removed from active polling, and audited with `AUTO_ARCHIVE_ASSOCIATED_TRACKING_NUMBER`; delivered rows retain related tracking-number context. `/tracking` active cards now expose explicit `Archive` and `Delete` actions for active tracking rows. Archived live old linked number `888071620741` for Alibaba order `304716450001023166` / PO `cmq60abrf000llh40c01xxprm` because replacement `1ZA6D5820419152616` is delivered; stock movement delta `0`, supplier invoice delta `0`.
- Verification: targeted Vitest `src/modules/documents/extract.test.ts src/modules/accounting/documents.integration.test.ts src/modules/tracking/service.integration.test.ts src/app/tracking/page.test.ts` passed (`48` tests); `npm run typecheck` passed; `npm run lint` passed; full `npm run test:serial` passed (`81` files / `402` tests); `NODE_OPTIONS='--max-old-space-size=6144' npm run build` passed. Production runtime restarted on `127.0.0.1:5173` build `VxPYu3-vgoLC_sDR1nqwI`; HTTP probes `/accounting` and `/tracking` returned `200`; browser console checks on `/accounting` and `/tracking` had `0` console/JS errors; `/tracking` showed `1` open shipment, `4` delivered, `6` saved numbers, Archive/Delete buttons visible, and old `888071620741` absent from active rows.

## Previous session — firmware V22 organic-magic actuation pass
- Second compression for bootloader limit: user’s Arduino build uses bootloader space (`Maximum is 32384 bytes`), not no-bootloader/ISP (`32768`). Removed all remaining `powf()` uses from V22 by changing field linearization from `powf(x, 0.42)` to `sqrtf(x)` (sqrt already linked for magnet magnitude), making pre-glow use `smootherstep01(preX)` directly, and replacing the no-op `powf(u, 1.0)` gamma pass with `clamp01(u)`. This preserves organic spring, top-end reach assist, robust calibration, full-triangle fusion, hard-off, and Timer1/Timer2 PWM invariants. Expected UX impact: field response is slightly less aggressive at the far/low edge than the old 0.42 power curve, but low-end bridge, LOW_END_LIFT, spatial blend, and top-end assist remain active.
- Bootloader compile verification after second compression: Arduino/MiniCore compile passed with FQBN `MiniCore:avr:328:variant=modelPB,clock=16MHz_external,LTO=Os_flto,bootloader=uart0,BOD=2v7,eeprom=keep,baudrate=default`: `31784 bytes (98%)` of `32384` program storage, globals `928 bytes (45%)` of `2048` RAM. Final attachment/Desktop SHA-256 `0555e5265cbd002bb174884c05cc34ba322f7455265298aaad081103db3be8bf`.
- Follow-up compression: user reported Arduino AVR linker overflow by 210 bytes for `Lambenti_V22_organic_magic.ino` (`.text` would not fit in `text`). Compacted the V22 organic spring by removing the optional top-end spring frequency boost and velocity clamp constants/branches while preserving the organic position+velocity spring, adaptive slow/fast response, low-PWM calming, overshoot prevention, settle snap, hard-off/removal bypass, V21 top-end reach assist, robust calibration gates, and all Timer1/PB1 invariants. Also precomputed spring omega constants to avoid runtime `2*pi*freq` multiply.
- Compile verification after compression: using Arduino IDE bundled CLI at `C:/Users/musas/AppData/Local/Programs/Arduino IDE/resources/app/lib/backend/resources/arduino-cli.exe`, temporary sketch folder `.hermes/tmp/Lambenti_V22_organic_magic`, and FQBN `MiniCore:avr:328:variant=modelPB,clock=16MHz_external,LTO=Os_flto,bootloader=no_bootloader,BOD=2v7,eeprom=keep,baudrate=default`, compile passed: sketch uses `32684 bytes (99%)` of `32768` program storage; globals use `928 bytes (45%)` of `2048` dynamic memory. Final SHA-256 for attachment/Desktop copy: `52a6579b057f9c9a0a273a792849f020a632d05e0ed7933e2182a55ab0f33162`.
- Request: research additional ways to make Lambenti feel more organic/magical, define product improvements, determine mathematical implementation, and implement.
- Research translation: minimum-jerk human-motion literature frames “smooth/organic” as low jerk with continuous velocity/acceleration; interactive filtering research (1€ filter) supports speed-adaptive smoothing to reduce jitter without lag; spring-animation/control references support critically damped springs as a stable real-time way to approach moving targets without overshoot. Product definition: Lambenti should feel like the light is attached to an invisible physical field, not like raw sensor values driving PWM—slow hovering should be calm, deliberate sweeps should respond, stops/starts should not twitch, top-end should complete, and removal must still clear fast.
- Implementation artifact: created `.hermes/desktop-attachments/Lambenti_V22_organic_magic.ino` and copied it to `C:/Users/musas/Desktop/Lambenti_V22_organic_magic.ino`. V22 starts from V21 and preserves the V7/V20/V21 hardware/safety baseline: ATmega328PB/MiniCore, Wire1, TCA9546A 0x70, 3x MMC5603 0x30, D9/PB1 Timer1 Fast PWM TOP 511 (~31.25 kHz), PB1 hard-off, boot zeroing, full-triangle production fusion, fractional output smoothing, low-PWM latch, raw-removal release, robust boot-zero gates, and bounded top-end reach assist.
- Firmware changes: added an online organic target spring after mapping/monotonic guards/target conditioning and before brightness smoothing. The spring keeps `organicTargetPosition`, `organicTargetVelocity`, and timestamp state; uses an implicit-Euler critically/slightly-over-damped spring with adaptive slow/fast omega constants, low-PWM calmer scaling, overshoot prevention, settle snap, and full bypass/reset during ABSENT/removal/release. Debug output includes `organic=<position>/<velocity>` under `DEBUG_SERIAL`.
- Verification: after the compression patch, Arduino/MiniCore compile passed with `32684 bytes (99%)` flash and `928 bytes (45%)` RAM. Static verification also passed: delimiter stack empty, delimiter errors 0, preprocessor `#if`/`#endif` balanced 45/45, required V22 organic/top-end/calibration symbols present, stale `V21`/`V20` counts 0, attachment and Desktop copy SHA-256 `52a6579b057f9c9a0a273a792849f020a632d05e0ed7933e2182a55ab0f33162`.
- Hardware tests still needed: compare V21 vs V22 on slow hover, slow inward sweep, sudden stop, center reach, and fast removal. If V22 feels too syrupy, raise `ORGANIC_SPRING_OMEGA_SLOW` or `ORGANIC_SPRING_LOW_FREQ_SCALE`; if it feels twitchy, lower `ORGANIC_SPRING_OMEGA_SLOW`. Do not tune by changing Timer1 or hard-off.

## Current session — firmware V21 magic-smooth actuation pass
- Request: research advanced ways to improve Lambenti firmware feel, make magnetic actuation smoother/even, improve calibration robustness, and implement a creative patch because current hardware reliably reaches only high-80/low-90% brightness.
- Implementation artifact: created `.hermes/desktop-attachments/Lambenti_V21_magic_smooth.ino` and copied it to `C:/Users/musas/Desktop/Lambenti_V21_magic_smooth.ino` for Arduino flashing. V21 preserves the V7/V20 hardware baseline: ATmega328PB/MiniCore, Wire1, TCA9546A 0x70, 3x MMC5603 0x30, D9/PB1 Timer1 Fast PWM TOP 511 (~31.25 kHz), PB1 hard-off, boot zeroing, triple-sensor fusion, fractional output smoothing, low-PWM latch, and raw-removal release.
- Firmware changes: added a bounded top-end reach assist that only engages deep in the active field and center-confidence region to close the last visible 8-12% without lowering the learned full-scale reference or making a broad 100% plateau; added robust calibration gates requiring enough baseline samples per sensor, full-triangle zero-pass samples, isolated zero-spike rejection, dropped-sample/rejected-spike fault limits, and richer debug calibration statistics; made runtime fusion fail-safe require `MIN_REQUIRED_SENSORS` reads instead of accepting partial one/two-sensor fusion in production.
- Verification: Arduino compile could not be run because `arduino-cli`, `avr-g++`, `g++`, and `clang++` are not installed in the current Windows/Git-Bash environment. Static verification passed on 2026-06-28: delimiter stack empty, delimiter errors 0, preprocessor `#if`/`#endif` balanced 45/45, required V21 symbols present, stale `V20` count 0, attachment and Desktop copy SHA-256 `9ce5c49fb80661f0101750f4d8a3b286ed3bb823f3f2f699a438d0b25f65f7cf`.
- Hardware tests still needed: no-magnet hard-off/no-glow, boot with magnet/noisy field rejects calibration, slow outer approach starts visibly without flicker, slow inward sweep avoids M-shaped dips, center can reach full/near-full without a large plateau, fast removal clears quickly, and sensor disconnect/failure hard-faults off.

## Current session — valuation robustness hardening
- Request: assess future robustness of the valuation system and improve visible weaknesses.
- Robustness assessment: after the prior regression fix, core pricing precedence was correct but still fragile because `/inventory/valuation` displayed price/value without price-source provenance. That made future regressions hard to spot: an operator could not tell whether a row came from accounting landed cost, manual item cost, or BOM rollup. Tests also did not explicitly cover accounting landed cost overriding a finished-good manual override/BOM rollup, nor did valuation rows preserve source provenance end-to-end.
- Improvements: added typed `PricedItemCostSource` metadata to priced valuation inputs/rows; `getActivePricedItemValuationInputs()` now carries `source`, `sourceLabel`, and `sourceRefs` from `resolveItemUnitCostIndex`; `/inventory/valuation` now renders a `Price Source` column with readable source labels/refs while keeping BOM rows compact. Added regression coverage for source provenance and landed-cost precedence over manual finished-good overrides/BOM rollups, plus a valuation page source contract for the new Price Source column.
- Verification on 2026-06-27 after final edits: targeted tests passed (`src/app/inventory/valuation/page.test.ts`, `src/modules/inventory/valuation.test.ts`, `src/modules/inventory/pricing.test.ts`, `src/modules/accounting/landed-cost.test.ts`: 4 files / 15 tests); `npm run lint && npm run typecheck` passed; `npm run test:serial` passed (81 files / 399 tests); `NODE_OPTIONS='--max-old-space-size=6144' npm run build` passed; `git diff --check` passed. Browser smoke after restart: `/inventory/valuation` rendered `Price Source`, total `USD $1764.79`, `0805_CLIP` row `2500.00 / USD 0.06 / Accounting landed cost · 2500 units · WINNIE-XU-304716450001023166 / USD 145.50`, `LAMBENTI_PACKAGE` row `BOM rollup · v1`, no long-float text; `/` dashboard rendered `Inventory Valuation USD $1764.79` and `Components on hand 12137.63`; browser console/JS errors 0.
- Residual weaknesses: source classification is rule-based and still depends on analyzer/OCR quality; future robust step would be a read-only anomaly report showing items whose live `StockLot.unitCost`, manual item cost, and accounting landed cost diverge materially. Current implementation does not mutate ledgers or prices and keeps stock receiving/accounting side-effect boundaries intact. Local runtime restarted on `127.0.0.1:5173` via Hermes process `proc_71779fc57aa8`.

## Current session — valuation regression repair after receipt-lot weighting
- Request: valuation system broke and most item prices were wrong after the receiving/lot-cost repair.
- Root cause: `calculatePricedItemValuations()` had been changed to prefer `StockLot.unitCost` whenever lots existed. In the live DB, every stocked/lot-backed item with current pricing evidence (clips, screws, adhesive, cable, LED strips, connectors) had accounting-landed-cost pricing; stale historical `StockLot.unitCost` values incorrectly overrode that resolved pricing. This dropped many visible prices/values. A second bug remained in landed-cost evidence classification: the Alibaba TA contract text contained `Incoterms and duties`, so the generic duty regex treated the original supplier order/payment document as extra attached landed-cost evidence and double-counted the 0805 clip order payment.
- Fix: priced item valuation is again anchored to the resolved unit-cost engine (accounting landed cost → manual item cost → BOM rollup) and uses ledger quantity × resolved unit cost, with only 2-decimal quantity rounding retained. `getActivePricedItemValuationInputs()` no longer loads/passes stock lots for priced valuation. The original supplier-order/payment exclusion now runs before generic duty classification unless there is a stronger customs/clearance/brokerage/import-fee signal.
- Live read-only evidence after fix: valuation sample rows now match resolved input costs: `0805_CLIP` quantity `2500`, unitCost `0.0582`, value `145.50`; `5952_DIE_CUT_CONN` `0.235` / `119.38`; `5952_DIE_CUT_MAIN` `0.4701` / `237.40`; `CABLE-UL2464-2C-1P5M` `1.7639` / `172.86`; `LED-COB-12V-3000K` `0.855` / `84.33`. The clip landed-cost index now has sourceRefs only `WINNIE-XU-304716450001023166`; `TA_CONTRACT_1780971366313.pdf` is no longer counted as attached landed-cost evidence.
- Verification on 2026-06-27: targeted tests passed (4 files / 15 tests), `npm run lint`, `npm run typecheck`, full `npm run test:serial` (80 files / 396 tests), `NODE_OPTIONS='--max-old-space-size=6144' npm run build`, and `git diff --check` all passed. Browser `/inventory/valuation` rendered total `USD $1764.79`, 0805 clip row `2500.00 / USD 0.06 / USD 145.50`, no long-float text, and dashboard `/` rendered `Inventory Valuation USD $1764.79` plus `Components on hand 12137.63` with 0 console/JS errors.
- Safety: no live stock receiving, item price edit, invoice approval/payment/reconciliation, journal posting, purchase/order action, Alibaba capture/import, delivery confirmation, or supplier-message side effects were performed. Local runtime is running on `127.0.0.1:5173` via Hermes process `proc_13eb2829370d`. The accounting/purchasing skill reference was patched to remove the stale instruction that priced valuation should prefer lot-level costs.

## Current session — receiving fade-out, quantity display resolution, and receipt-lot valuation repair
- Request: make `/incoming` visually process confirmed receipts without a manual refresh, stop dashboard stock quantities from exposing floating-point tails (common quantity resolution 2 decimals), prevent duplicate `StockLot(itemId, lotCode)` create errors on receipt, make manual item price corrections update only the most recent receipt lot valuation, and prevent supplier-order payment totals from being double-counted into item costs.
- Implemented receiving UX: `ReceiveIncomingLineForm` now emits a success event, the server-rendered incoming PO line is wrapped in `IncomingLineReceiptShell`, and successful receipt rows animate with `duration-500` collapse/fade before the existing refresh fallback runs. `/incoming` receiving progress/ordered/received/remaining quantities now use shared two-decimal formatting.
- Implemented quantity/cost correctness: added `src/modules/inventory/quantity-format.ts`; dashboard KPI/table stock quantities and valuation quantities render at 2 decimals. `createStockMovement()` now reuses an existing same-item/same-lot-code receipt lot when unit cost/currency match, and rejects same-code receipts with a clear different-cost error instead of leaking a Prisma unique-constraint failure.
- Implemented valuation repair: priced-item valuation now considers per-lot on-hand costs, so old stock keeps its historical receipt-lot cost while the latest receipt lot can be corrected independently. `updateItem()` now audits and syncs a changed manual estimated unit cost only into the most recent PO receipt lot for that item; older lots remain untouched. Alibaba/order payment receipt classification now excludes original supplier order/payment documents from additional landed-cost evidence unless they carry explicit customs/duty/additional-freight signals, preventing the order total from being counted a second time.
- Verification on 2026-06-27: targeted regression tests passed (6 files / 26 tests), `npm run lint`, `npm run typecheck`, full `npm run test:serial` (80 files / 396 tests), `NODE_OPTIONS='--max-old-space-size=6144' npm run build`, and `git diff --check` all passed. Runtime smoke restarted `127.0.0.1:5173` after killing stale PID `38664`; browser `/` rendered `Components on hand` as `12137.63` (not `12137.630000000001`), browser `/incoming` rendered 2-decimal quantities and found the receipt row wrapper class `transition-all duration-500`, and browser `/inventory/valuation` rendered `0805_CLIP` as `2500.00 / USD 0.02 / USD 50.00` with no long-float text and 0 console/JS errors.
- Safety: no live receipt confirmation, stock movement, manual item price edit, purchase/order approval, invoice approval/payment/reconciliation, journal posting, Alibaba capture/import, delivery confirmation, or supplier-message side effects were performed. Integration tests used `TEST-*` fixtures and cleaned up; runtime mutation was limited to restarting the local Next server. Note: the dirty tree also contains pre-existing/unrelated UI edits in `src/app/accounting/page.tsx` and `src/app/tracking/page.tsx`; they were not part of this receiving/valuation fix.

## Current session — lead-time priority, tracking refresh/archive, dashboard queue UX
- Request: make manual lead-time inputs primary, explain observed weighted average, improve Lead-Time Horizon color semantics/order, keep supplier lead time in sync, prioritize PO recommendations by supply + lead time, rename tracking refresh and refresh all active rows, archive superseded same-order tracking numbers, move `What Needs Attention` below active tracking numbers, improve review-action routing, and add hover summaries to dashboard operations queues.
- Implemented `Item.manualLeadTimeDays` via migration `20260621120000_manual_item_lead_time`. Manual lead-time saves now set `manualLeadTimeDays` and `leadTimeDays`, make manual the primary planning value even when observed samples exist, skip item observed-average overwrites while manual is set, and mirror the manual value to the preferred supplier lead-time field with audit logs. Observed weighted average remains visible as quantity-weighted purchase/order→receipt/delivery evidence and is ignored for planning when manual primary exists.
- Dashboard `/` Lead-Time Horizon now orders by longest primary lead time, uses green for observed/evidence-based rows and blue/indigo for manual-primary rows (orange/red reserved for warning states), explains observed weighted average inline, and dashboard Operations Flow queue cards show smooth hover popups with one-line attention summaries. Review Actions now links to `/#human-approval-queue` instead of dumping the operator into accounting by default; individual review rows still link to the relevant workflow.
- Purchasing recommendations now include covered supply, reorder gap, lead time/source, priority score/label, and are sorted by priority then longest lead time/supply gap. One-day lead-time items are explicitly classified `LOW` priority even when low stock, while longer-lead/supply-gap parts rank higher.
- `/tracking` button text is now `Refresh`; the manual action refreshes active non-delivered/non-archived tracking numbers and resets `nextRefreshAt` even when config is missing. `What Needs Attention` now renders below `Open Shipments`. The earlier same-order auto-archive behavior was removed after it hid live tracking evidence: same-order numbers now remain active evidence, while the dashboard screens duplicate shipment cards visually and preserves linked tracking-number references.
- Disappearance repair on 2026-06-21: live row `1ZA6D5820419152616` disappeared from Open Shipments because same-order auto-archive marked it and `888071620741` `ARCHIVED` after transient/test tracking number `1Z675EW60490310056` was linked to Alibaba order `304716450001023166` and then cleaned up, leaving no active replacement. Restored `1ZA6D5820419152616` to `IN_TRANSIT`/`SUCCESS` and `888071620741` to `PENDING`/`SUCCESS`, with audit action `RESTORE_TRACKING_NUMBER_FROM_ERRONEOUS_ARCHIVE`; `/tracking` now shows `1ZA6D5820419152616` with linked shipment numbers `888071620741, 1ZA6D5820419152616`.
- Verification on 2026-06-21: `npm run prisma:deploy` applied the migration; initial `prisma:generate` hit the known Windows query-engine lock, then passed after `npm run runtime:stop`. Targeted tests passed (6 files / 38 tests), `npm run typecheck`, `npm run lint`, full `npm run test:serial` (79 files / 386 tests), `NODE_OPTIONS='--max-old-space-size=6144' npm run build`, and `git diff --check` exit 0 (CRLF warnings only). After disappearance repair, targeted tracking integration tests passed (27 tests), full gate passed again (`npm run lint && npm run typecheck && npm run test:serial && npm run build`: 79 files / 389 tests), `git diff --check` exit 0 (CRLF warnings only), runtime restarted on PID `71584`, browser `/tracking?smoke=restore-disappeared-tracking` showed 3 open shipments / 6 saved numbers / 0 failed refreshes / 0 console or JS errors.
- Safety: no stock receiving/movement, purchase approval/order placement, invoice approval/payment/reconciliation, journal posting, Alibaba capture/import, delivery confirmation, or supplier-message side effects were performed. Live DB mutation was schema migration plus tracking-metadata repair only; side-effect checks returned `stockMovementSideEffects:0` and `invoiceSideEffects:0`; tests used `TEST-*` fixtures and cleaned up.

## Current session — embedded Hindsight + dark/lead-time/dashboard/currency pass
- Request: set up local embedded Hindsight managed by Hermes, port memory into it, verify key features, fix item-page dark-theme gaps/light refresh flash, make dashboard stock/shortage type labels follow item-page component-use sections, add manual lead-time editing on `/tracking`, add a dashboard lead-time graphic, and auto-update USD conversion rates from a live source.
- Hindsight: installed/started the lambenti embedded daemon on `http://127.0.0.1:9536` backed by `C:/Users/musas/.pg0/instances/hindsight-embed-lambenti`; patched the local Hindsight embed daemon launcher so Windows starts the venv Python executable correctly. Created the `hermes-lambenti` bank, ported 54 memory/vault documents (Hermes MEMORY/USER entries plus LambentiMemoryVault markdown chunks), and verified retain/list/recall/reflect paths with live client calls. Config uses local LM Studio `qwen2.5-coder-7b-instruct` via provider `lmstudio`; no remote private memory upload was performed.
- Theme/items/dashboard: default document HTML now renders `data-theme="dark"`, `public/theme-init.js` defaults/catches to dark, root CSS defaults to dark before hydration, and the item catalog section header row `bg-slate-100/80` now resolves to dark `rgb(30, 41, 59)`. `StockSummary`/`ShortageSummary` now carry `useGroupOverride`, so dashboard `In-Stock Quantities`, `Low Stock Dashboard`, and `Upcoming Shortages` use the same component-use grouping label as `/inventory/items` after item-page section changes and workspace revalidation.
- Lead-time/currency: `/tracking` lead-time audit now includes every active item, labels manual vs observed rows, and exposes an audited `Save lead time` form for manual planning estimates when no purchase-to-received sample exists; observed purchase/order→receipt/delivery samples remain automatic and can sync item lead times. Dashboard now includes `Lead-Time Horizon` bars from the same tracking lead-time log. Currency conversion now fetches live USD-base rates from `https://open.er-api.com/v6/latest/USD` with 12h cache and static fallback; priced item/BOM/valuation paths pass live rates into unit-cost resolution.
- Verification on 2026-06-21: `npm run typecheck`, targeted tests (6 files / 37 tests), `npm run lint`, full `npm run test:serial` (79 files / 382 tests), and `NODE_OPTIONS='--max-old-space-size=6144' npm run build` all passed. Live rate source returned HTTP 200 with CAD 1.414888 per USD (CAD→USD 0.7068). Fresh `npm run start:local` served `127.0.0.1:5173`; HTTP HTML starts with `<html lang="en" data-theme="dark">`; browser `/inventory/items` showed dark body `rgb(2, 6, 23)` and dark section row `rgb(30, 41, 59)`; browser `/` rendered `Lead-Time Horizon`; browser `/tracking` rendered `26 Items · 8 Samples` in the lead-time audit. Browser console for `/` was clean; browser console for `/tracking` still reported one empty-source exception with no console messages, while DOM/tests/build passed.
- Safety: no stock receiving/movement, purchase approval/order placement, invoice approval/payment/reconciliation, journal posting, Alibaba capture/import, delivery confirmation, supplier-message sending, or live manual lead-time data mutation was performed. Live side effects were limited to starting local Hindsight/Postgres-backed memory infrastructure and local Next runtime, plus read-only live FX/tracking/dashboard/item rendering checks.

## Current session — item catalog sectioning and tracking info-received classification
- Request: order the active item catalog by the component/use categorization previously implemented for item dropdowns, auto-place new components into their inferred section, allow manual movement to a different section from the items page, and treat carrier-information-received tracking rows as `INFO_RECEIVED` instead of pending.
- Implemented item catalog sectioning: `/inventory/items` now groups the active catalog through shared `groupItemOptionsByUse()` sections (`Finished Builds`, `Enclosures`, `Hardware`, `Fasteners and Accessories`, `PCBs & Controller Boards`, `Electronics & Electrical`, etc.) while preserving the existing coarse `ItemCategory` field and row actions.
- Implemented manual section overrides: migration `20260620213000_item_use_group_override` adds nullable `Item.useGroupOverride`; each item row exposes a `Catalog Section` selector that saves through `updateItemUseGroupFormAction()` / `UPDATE_ITEM_USE_GROUP_OVERRIDE` audit logs. The shared grouping helper honors overrides, so dropdowns and page grouping that use the helper reflect manual moves while blank override returns the item to automatic keyword/category classification.
- Implemented tracking status fix: `normalizeTrackingStatus()` and evidence classification now map `info_received`, `received_information`, carrier-received-information phrases, shipper/label-created phrases, and shipment-information-sent phrases to `INFO_RECEIVED`. Current `/tracking` runtime now renders the UPS label-created row `1ZV90R362090867759` as `Info Received` rather than `Pending`.
- Verification on 2026-06-20: `npm run prisma:deploy` applied the new migration; `npm run prisma:generate` passed after killing stale port-5173 PID 14412 that locked the Prisma query-engine DLL. Targeted tests passed (4 files / 35 tests), `npm run typecheck` passed, `npm run lint` passed, full `npm run test:serial` passed (79 files / 380 tests), and `npm run build` passed. Runtime smoke on `127.0.0.1:5173` returned HTTP 200 for `/inventory/items` and `/tracking`; browser evidence showed 26 catalog-section controls and item section headers, and tracking article `1ZV90R362090867759` displayed `Info Received`.
- Safety: no stock receiving/movement, purchase request/order mutation, invoice approval/payment/reconciliation, journal posting, Alibaba capture/import, delivery confirmation, or supplier-message side effects were performed. Live DB mutation was limited to applying the schema migration; tests used `TEST-*` fixtures and cleaned up. The current browser-console tool reported an empty-source exception during smoke despite no console messages; HTTP/rendered DOM evidence and build/tests passed.

## Previous session — unified item unit-cost engine / valuation parity
- Request: create a price-per-unit engine using quantity, item price, shipping, email-order imports, added duty/fee receipts, and refined 4-decimal unit-cost precision; tie item-page unit cost, valuation page priced items, dashboard inventory valuation/value mix, BOM dark mode, and finished-good BOM rollup cost.
- Implemented `src/modules/inventory/unit-cost-engine.ts`: order-level shipping/tax/duty/brokerage/other charges allocate by extended merchandise value (fallback quantity only when no value basis exists), use largest-remainder cent allocation so rounded line allocations sum back exactly, and compute 4-decimal landed unit costs.
- Implemented `src/modules/inventory/pricing.ts`: active item unit-cost resolution now prefers accounting landed-cost evidence (including attached duty/fee/shipping receipts), then manual item unit cost, then derived active BOM rollup for finished goods when no manual override exists. Finished-good add-item unit cost remains optional; active item edit can still set a manual finished-good override.
- Tied valuation surfaces: `/inventory/valuation` and dashboard now use the same priced-item valuation inputs; `/inventory/items` displays the same resolved unit cost/source; the live dashboard Inventory Valuation and `/inventory/valuation` both rendered USD $1237.48, with dashboard value mix summing Component USD $1214.30 + Finished Good USD $23.18.
- UI fix: BOM component rows no longer use light-only odd/even row backgrounds; they use the theme-aware table-row hover token for dark-mode compatibility.
- Verification on 2026-06-20: targeted tests passed (5 files / 31 tests), `npm run typecheck`, `npm run lint`, full `npm run test:serial` passed (79 files / 375 tests), `NODE_OPTIONS='--max-old-space-size=6144' npm run build` passed, production route probes for `/inventory/items`, `/boms`, and `/inventory/valuation` returned HTTP 200, and browser smokes for `/inventory/items`, `/boms`, `/inventory/valuation`, and `/` rendered with 0 console/JS errors after clearing a stale port-5173 server.
- Follow-up existing-item backfill on 2026-06-20: audited all 26 active component/consumable/finished-good items through `resolveItemUnitCostIndex()`; all 26 resolved, with 14 accounting landed-cost component records, 9 item-unit-cost records, and 3 BOM-rollup finished goods. Backfilled the 14 accounting landed-cost component item-master records to the resolved 4-decimal USD unit costs with `CONFIRMED` confidence and per-item `AuditLog` action `BACKFILL_ITEM_UNIT_COST_FROM_RESOLVED_PRICING`. Finished goods with blank manual unit cost remain dynamically BOM-rollup priced (`LAMBENTI_MAIN_UNIT` USD 9.4647, `LAMBENTI_PACKAGE` USD 15.6770, `LED_CONN` USD 0.9119) so future component cost changes still flow through automatically; manually-priced finished goods already match their item-unit costs.
- Safety: no stock receiving/movement, purchase request/order mutation, invoice creation/approval/payment/reconciliation, journal posting, Alibaba capture/import, delivery confirmation, or supplier-message side effect was performed. The follow-up backfill changed only item-master cost metadata for accounting-landed-cost items plus audit-log entries; stock movement count stayed 59 and supplier invoice count stayed 38.

## Current session — additional shipping receipt valuation chain
- Request: ensure that each newly imported shipping/duties receipt, once attached as accounting evidence, pushes the added cost through item price display and inventory valuation every time.
- Implemented attached landed-cost evidence broadening: `getLandedCostRows()` now counts attached/applied `PAYMENT_RECEIPT` documents that carry explicit additional shipping/freight/carrier/logistics charge signals, in addition to customs/duty/brokerage/clearance receipts. The amount is still normalized to USD, allocated by invoice-line value, and exposed through landed-cost refs; generic supplier payment receipts without landed-cost signals remain excluded.
- Locked the end-to-end chain with tests: regression coverage now proves an attached customs receipt plus a second additional shipping receipt accumulates into `allocatedAttachedLandedCostEvidence`, increases landed totals, feeds `getItemLandedCostIndex()`, and then drives `calculatePricedItemValuations()` for inventory valuation without creating stock movements. Accounting source-contract coverage also verifies attach actions revalidate the workspace paths that include `/accounting/landed-cost`, `/inventory/items`, and `/inventory/valuation`.
- Verification on 2026-06-20: targeted accounting/page tests passed (3 files / 17 tests), `npm run typecheck`, `npm run lint`, full serial Vitest passed (77 files / 362 tests), `NODE_OPTIONS='--max-old-space-size=6144' npm run build` passed, `npm run lambenti:serve:verified` rebuilt/restarted a fresh runtime (PID 4308, build `SpTGF3b4t1CSJOf2SWzHE`) with route probes green, `npm run smoke:browser -- --base-url=http://127.0.0.1:5173` passed all 21 pages, targeted Playwright DOM evidence confirmed the live `PaymentReceipt.pdf` amount/landed-cost/item-price/valuation chain with zero console/page errors, and `git diff --check` passed.
- Safety: no stock receiving/movement, purchase approval/order placement, invoice approval/payment/reconciliation, journal posting, Alibaba capture/import, delivery confirmation, or supplier-message side effects were performed. Changes are computed landed-cost/valuation logic plus regression/source-contract tests.

## Previous session — customs receipt landed-cost pricing reflection
- Request: after attaching a customs duties/fees payment receipt as accounting evidence to its associated order, make the paid amount increase the order/item landed cost and make item pricing/valuation update from that evidence.
- Implemented computed landed-cost evidence allocation: `getLandedCostRows()` includes attached/applied accounting documents linked to the supplier invoice or purchase order when they are customs landed-cost evidence, parses attached legacy receipts such as `PaymentReceipt.pdf` from extracted text (`charged 80.74 CAD`), normalizes to USD, allocates by invoice-line value, and exposes evidence refs in landed-cost CSV/report rows. Generic supplier payment receipts are not counted unless customs/duty/brokerage/clearance signals are present.
- Implemented item-price propagation: `/inventory/items` displays accounting-derived landed unit cost when available while keeping the editable manual unit-cost field unchanged; `/inventory/valuation` values active stock using accounting landed cost first and manual estimated cost as fallback. The live attached `PaymentReceipt.pdf` adds USD 60.55 across `5952_DIE_CUT_CONN` and `5952_DIE_CUT_MAIN`, moving visible landed unit costs to about USD 0.2350 and USD 0.4701 respectively.
- Implemented parser/test hardening: new uploads with customs/duty/FedEx-clearance language classify as `CUSTOMS_DOCUMENT` before generic payment receipt, and amount parsing supports number-then-currency charged totals like `80.74 CAD`.
- Verification on 2026-06-20: targeted accounting/item tests passed (4 files / 25 tests), full serial Vitest passed (77 files / 362 tests), `npm run lint`, `npm run typecheck`, `NODE_OPTIONS='--max-old-space-size=6144' npm run build`, `git diff --check`, and browser smokes for `/accounting/landed-cost`, `/inventory/items`, and `/inventory/valuation` passed with the live receipt reflected and 0 console/page errors.
- Safety: no stock receiving/movement, purchase approval/order placement, invoice approval/payment/reconciliation, journal posting, Alibaba capture/import, delivery confirmation, or supplier-message side effects were performed. Changes are computed reporting/pricing display plus parser/test code.

## Previous session — OCR retry lock, title-case sweep, UI graphics audit, and implemented UI improvements
- Request: double-check `/accounting` invoice-review retry OCR behavior; then identify title/button/section descriptors on each page, enforce title case for major words, and research/report workflow-first UI improvements for pages with graphics.
- OCR finding/fix: reproduced that retry/manual re-analysis controls could still be exposed for already linked/applied source documents, allowing reviewed evidence to be re-analyzed after apply/attach. Added `documentCanBeReanalyzed()` UI gating and service-layer `assertAccountingDocumentCanBeReanalyzed()` so `APPLIED`/`ATTACHED`/linked documents reject retry OCR and manual text edits. Regression coverage proves retry/manual edits are locked after apply and do not create stock movement side effects.
- Title-case sweep: audited 21 rendered routes (`/`, inventory, purchasing, BOM, incoming, integrations, automation, tracking, and accounting subsections). Patched title/button/label/descriptive-card casing across dashboard, accounting, tracking, inventory items/movements/valuation, BOMs, incoming, purchasing, suppliers, and integrations. Final rendered audit found 0 title-case issues across 671 title/button/label candidates and 0 browser console/page errors.
- UI graphics audit: captured screenshots and visual-signal inventory under `var/hermes-ui-audit/` (`title-audit-final.json`, `visual-page-audit.json`, `screenshots/*.png`, `ui-improvement-suggestions.md`). Pages with meaningful graphics/visual systems: `/`, `/tracking`, `/accounting`, `/inventory/items`, `/boms`, `/incoming`; lower-priority visual/table pages are `/inventory/valuation`, `/inventory/movements`, and `/automation`. Design recommendations prioritize next action, visual hierarchy, progressive disclosure, accessibility, and no decorative charts.
- Implemented workflow-first UI improvements requested from the audit: root `/` now has a `Launch Next Action` strip and buildable-now gauge caption; `/tracking` converts Risk Spectrum into ordered actions (`Fix Failed`, `Refresh Due`, `Link Evidence`, `Review Open`) and shows `Last Good Refresh`; `/accounting` has a dominant `Bookkeeping Workflow Rail`, prioritized source-document triage, and `Evidence Only Until Apply` guardrail; `/inventory/items` has per-row `Stock Health`; `/boms` has a per-BOM `Build Constraint` bar plus `LAMBENTI_PACKAGE` launch-critical badge; `/incoming` has per-PO `Receiving Progress`, packing-slip duplicate check, and emphasized human-count field; `/inventory/valuation` has a value-concentration bar; `/inventory/movements` has a ledger reading guide/sticky SKU/signed chips; `/automation` has severity grouping.
- Verification on 2026-06-18 after implementation: targeted UI source-contract tests passed (9 files / 20 tests), `npm run typecheck`, `npm run lint`, full `npm run test:serial` (75 files / 354 tests), and `NODE_OPTIONS='--max-old-space-size=6144' npm run build` passed. Fresh `npm run start:local` on `127.0.0.1:5173` served the app; `npm run smoke:browser -- --base-url=http://127.0.0.1:5173` passed all 21 pages, and `node var/hermes-ui-audit/check-ui-improvement-markers.mjs http://127.0.0.1:5173` confirmed all new UI markers across 9 changed routes with 0 console/page errors.
- Implemented operator-performance architecture upgrades: `scripts/lambenti-runtime.mjs` now owns local runtime status/ensure/verified-start with stale-port cleanup, 6144 MB build guard, route probes, source fingerprint, build ID, PID, and `.hermes/runtime/lambenti-local-server.json` metadata; `scripts/ui-contract-smoke.mjs` is the reusable Playwright UI contract smoke for route markers, sidebar alignment, dark-mode movement hover, sticky cell hover, and console/page errors; `src/app/globals.css` now uses semantic table hover CSS variables instead of one-off movement-row colors; `package.json` exposes `runtime:status`, `runtime:ensure`, `runtime:stop`, `lambenti:serve:verified`, `smoke:ui-contracts`, and `verify:tiny-ui`; `docs/local-runbook.md` documents the runtime workflow, tiny UI ladder, and UI scope classifier.
- Implemented token-efficiency operator upgrades: `scripts/context-snapshot.mjs` and `npm run context:snapshot` now provide compact dirty-tree context with changed-file counts, top `git diff --numstat` files, status preview truncation, checkpoint line counts, runtime metadata, and suppressed Git CRLF/LF warning summaries. Use `--paths=<files-or-dirs>` before reading diffs on large dirty trees.
- Verification on 2026-06-20 after operator-performance upgrades: focused tests passed (3 files / 11 tests), `npm run typecheck`, `npm run lint`, `npm run lambenti:serve:verified` passed with build ID `ImDEmvt75cTdXAGin8eM-` and PID `38856`, `npm run smoke:ui-contracts -- --base-url=http://127.0.0.1:5173` passed, `npm run verify:tiny-ui` passed end-to-end, `npm run smoke:browser -- --base-url=http://127.0.0.1:5173` passed 21 pages, and full `npm run test:serial` passed 77 files / 361 tests.
- Safety: no stock receiving/movement, PO approval/order placement, invoice approval/payment/reconciliation, journal posting, Alibaba capture/import, delivery confirmation, or supplier-message side effects were performed. Runtime side effects were limited to test fixtures that cleaned themselves up and restarting the local Next server.

## Previous session — run-now resume verification after Codex limit
- Request: cancel the scheduled 11:45pm resume and run the saved last-night crash-recovery/current Lambenti tasks immediately.
- Action taken: removed cron job `76a240ff00eb` so it will not duplicate the work at 23:45. Re-read crash recovery queue plus repo startup context and verified the two newest recovered prompts against the current tree.
- Current source evidence: `/tracking` source has collapsed `Shipment details`, Provider heartbeat, `LIVE` heartbeat countdown, full package tracking disclosures, absent delivered-history retained/not-polled copy, and Ship24 duplicate-deactivation code/tests. `/accounting` source has `Accounting Workbench`, `Command Center`, daily bookkeeping links, collapsed bottom information panels, absent stale `Simple accounting command center`, embedded scanned-PDF OCR fallback, and the Alibaba email page no longer renders `Direct Alibaba portal agent`.
- Verification on 2026-06-18 09:39 EDT: targeted run-now test suite passed (`src/app/tracking/page.test.ts`, `src/modules/tracking/service.integration.test.ts`, `src/app/api/tracking/refresh/route.test.ts`, `src/app/accounting/page.test.ts`, `src/app/accounting/invoices/page.test.ts`, `src/modules/documents/extract.test.ts`, `src/modules/suppliers/service.test.ts`, `src/modules/email-imports/alibaba-email.test.ts`: 8 files / 53 tests). `npm run typecheck`, `npm run lint`, full `npm run test:serial` (75 files / 353 tests), `NODE_OPTIONS='--max-old-space-size=6144' npm run build`, `npm run smoke -- --base-url=http://127.0.0.1:5173` (21 pages / 5 UI contracts / 4 JSON APIs / 3 CSV exports), and `npm run smoke:browser -- --base-url=http://127.0.0.1:5173` (21 pages) all passed.
- Runtime/browser evidence: restarted a fresh `npm run start:local` after killing stale port-5173 PID 23544. Targeted DOM checks found tracking Provider heartbeat/LIVE/ring/collapsed disclosures, accounting Workbench/Command Center/collapsed bottom panels/bookkeeping links, 5 local tracking rows with 0 local duplicates, and live Ship24 API status 200 with 5 active trackers / 5 unique active tracking numbers / 0 duplicate active tracking numbers. A first combined DOM check saw one transient minified React #418 pageerror, but isolated reruns of `/tracking` and `/accounting` produced 0 console/page errors and the full browser-section smoke passed all routes.
- Safety: no stock receiving/movement, purchase approval/order placement, invoice approval/payment/reconciliation, journal posting, Alibaba capture/import, delivery confirmation, or supplier-message side effects were performed in this run-now resume verification. The fresh Next listener is currently expected on `127.0.0.1:5173` via the `npm run start:local` background wrapper.

## Previous session — OCR, tracking heartbeat, order-email cleanup, supplier identity hardening
- Request: fix accounting PDF extraction failures showing `spawn pdftoppm ENOENT`; move `/tracking` `Refresh due statuses` into the Provider heartbeat card, make countdown persistent/actual-heartbeat-backed, add yellow pending/no-update tracking section; remove `Direct Alibaba portal agent` from Order Email Agent; investigate supplier junk/duplicates and prevent invalid/duplicate suppliers.
- Implemented OCR fallback: `src/modules/documents/extract.ts` now tries directly embedded PDF JPEG/JPX image streams before requiring Poppler `pdftoppm`; missing Poppler now reports an actionable install/override message only after embedded-image fallback fails. Added regression coverage in `src/modules/documents/extract.test.ts` proving embedded scanned PDFs OCR without a `pdftoppm` warning.
- Implemented tracking heartbeat/UI: `Refresh due statuses` moved to the Provider heartbeat card; `TrackingProviderHeartbeat` now schedules against persisted `nextRefreshAt`, calls `/api/tracking/refresh`, refreshes the page after heartbeat checks, and displays `lastCheckedAt`; API refresh returns heartbeat timestamps. Added yellow pending/no-provider-updates section beside the Info received radar and yellow pending status color.
- Implemented order-email cleanup: removed the `Direct Alibaba portal agent` section from `/integrations/alibaba-email`; mailbox/manual order-email workflow remains.
- Supplier root cause: supplier rows were being created from Alibaba portal/message/email text because supplier parsing accepted generic `Supplier` labels without a delimiter and saved raw conversation snippets such as tracking-message text, `each once.`, `Tel.`, and date/contact/company UI strings. Existing active DB had no canonical duplicate groups after normalization, but several junk rows were still visible or listed in the cleanup queue.
- Implemented supplier hardening: supplier display/dropdowns now use canonical identity keys, embedded company extraction from Alibaba thread-list labels, stricter valid-supplier checks, canonical duplicate reuse for custom supplier creation, and email import supplier creation now refuses invalid/generic supplier labels instead of creating Supplier rows. Cleanup queue no longer prints raw junk supplier labels on the page.
- Verification on 2026-06-17 20:48 EDT: targeted tests passed (`src/modules/documents/extract.test.ts`, `src/app/tracking/page.test.ts`, `src/modules/suppliers/service.test.ts`, `src/modules/email-imports/alibaba-email.test.ts`, `src/app/suppliers/page.test.ts`: 5 files / 32 tests); initially full serial Vitest exposed supplier-test-name and tracking-API-mock regressions, which were fixed; final `npm run test:serial` passed (75 files / 353 tests). `npm run typecheck` passed; `npm run lint` passed; plain/4096 MB builds intermittently crashed the Next/TypeScript worker after compile, then `NODE_OPTIONS='--max-old-space-size=6144' npm run build` passed. Production smoke on `127.0.0.1:5173` verified `/tracking`, `/integrations/alibaba-email`, and `/suppliers`; marker checks passed for Provider heartbeat, heartbeat refresh button, yellow pending panel, removed Direct Alibaba portal section, and hidden supplier junk labels. API heartbeat smoke `POST /api/tracking/refresh` returned `scanned:0`, `skipped:3`, and persisted heartbeat timestamps. No stock, receiving, purchase approval/order placement, invoice approval/payment, journal posting, Alibaba capture/import, delivery confirmation, or supplier-message side effects were performed.

## Current session — crash recovery / lost prompt resume
- Request: recover progress, resume jobs, and cue appropriate lost queued prompts.
- Recovery evidence: `session_search` found two newest unanswered one-message TUI sessions from 2026-06-17 (`20260617_184733_dd4568` tracking prompt, `20260617_184005_20b57e` accounting prompt). `.hermes/crash-recovery-queued-prompts-20260617-1905.md` contains 13 recovered one-message prompts and now records the current recovery status.
- Active job state at recovery start: Hermes process manager had no background processes; `read_terminal` reported no in-app terminal open; `127.0.0.1:5173` was initially closed; `TASK_QUEUE.md` showed `In progress: None`.
- Resumed/verified tracking lost prompt: source/browser evidence shows open-shipment information after `Shipment progress` is collapsed behind `Shipment details`, delivered-history full package data is behind a disclosure, the exact removed delivered-row sentence is absent, Provider heartbeat renders a `LIVE` countdown/progress ring, local DB has 5 tracking rows / 3 active rows / 0 duplicate local tracking numbers, and live Ship24 `/public/v1/trackers?limit=100` reports 5 active trackers / 5 unique active tracking numbers / 0 duplicate active tracking numbers.
- Resumed/verified accounting lost prompt: source/browser evidence shows `Accounting Workbench`, no stale `Simple accounting command center`, `Command Center`, daily bookkeeping routine links to sections/pages, the three bottom accounting panels are collapsed, OCR retry/manual paste controls exist, and clickable-section/browser smoke passes.
- Verification on 2026-06-17 local: targeted tracking/accounting/OCR tests passed (4 files / 24 tests); `npm run typecheck` passed; `npm run lint` passed; `npm run test:serial` passed (75 files / 350 tests, with expected Prisma retry/unique-constraint stdout only); `npm run build` passed; `npm run smoke -- --base-url=http://127.0.0.1:5173` passed (21 pages, 5 UI contracts, 4 JSON APIs, 3 CSV exports); `npm run smoke:browser -- --base-url=http://127.0.0.1:5173` passed (21 pages); targeted Playwright DOM check found 0 console/page errors for `/tracking` and `/accounting`.
- Current runtime correction: the Hermes-tracked `npm run start:local` wrapper processes exited, but the spawned Next `node` child is still listening on `127.0.0.1:5173` as PID 5680 (`D:\Program Files (x86)\node.exe`). HTTP `/tracking?smoke=process-recovery` and `/accounting?smoke=process-recovery` returned 200 after this correction. Hermes process manager currently has no tracked background processes; use the port/PID check before assuming the app is down.

## Previous session — tracking visual command-layer graphics pass
- Request: improve the `/tracking` page UI and research/implement the sexiest graphics schemes.
- Research/orientation: inspected current repo/checkpoints, `/tracking` source/tests, tracking UI dark-mode guardrails, and public design references. Useful patterns translated into Lambenti-safe UI: Linear-style dense premium command surfaces, Vercel-style dark radiant panels/grid signal, Stripe-style bold metric emphasis, and logistics-homepage emphasis on AI/actionable shipping insights. A `delegate_task` advisory pass was attempted but returned hallucinated/nonexistent file paths, so implementation decisions used direct repo/browser evidence.
- Implemented `/tracking` visual command layer: after the summary cards, the page now renders a dark premium `Live shipment radar` with stage distribution bars, open/due/failed radar numbers, and a `Risk spectrum` segmented attention load. A paired panel adds `Provider heartbeat` with a conic live/setup ring and `Carrier mix` / `Evidence constellation` bars. Copy explicitly preserves the safety boundary: no stock receipt or delivery confirmation happens here.
- Files changed for this slice: `src/app/tracking/page.tsx` and `src/app/tracking/page.test.ts`. No schema, data model, provider, stock, purchasing, accounting, Alibaba capture/import, delivery confirmation, or supplier-message path was added.
- Verification on 2026-06-16 21:58 EDT: targeted page source-contract test passed (`src/app/tracking/page.test.ts`, 1 test); `npm run typecheck` passed; full gates passed with `npm run lint`, `npm run test:serial` (75 files / 348 tests), `npm run build`, and `git diff --check` (exit 0 with existing CRLF warnings only).
- Runtime/browser verification: killed stale port-5173 PID 31192, started fresh `npm run start:local` as process `proc_daf2010621b4`, and HTTP `/tracking?smoke=visual-command-layer` returned 200 with the new visual markers. Browser `/tracking?smoke=visual-command-layer-browser` rendered the visual command layer, live shipment radar, risk spectrum, provider heartbeat, carrier mix, evidence constellation, 3 open tracking rows, dark body background `rgb(2, 6, 23)`, 456 stylesheet rules, no horizontal overflow, and 0 console/JS errors. Screenshot captured at `C:\Users\musas\AppData\Local\hermes\profiles\lambenti\cache\screenshots\browser_screenshot_724906a7c5f24026b9135658de51f864.png` (vision analysis unavailable because the local Qwen model does not support image inputs).
- Current runtime: fresh Next listener is available on `127.0.0.1:5173` (observed PID 22856 after a second start attempt correctly failed with `EADDRINUSE`).

## Previous session — tracking expandable package data and dashboard coverage bridge

- Research/orientation: inspected checkpoint files, current tracking/dashboard source and tests, the tracking UI/dark-mode reference, and dashboard useful-visual-signals guardrail. Ran one local Qwen `delegate_task` advisory audit; it returned generic recommendations, so implementation decisions used direct repo/runtime evidence.
- Implemented `/tracking` expandable full package tracking data: active shipment cards and delivered-history rows now expose a `Full package tracking data` disclosure with complete linked-order metadata, refresh schedule, full carrier event timeline, raw provider status payload, and raw event payloads. Data comes from existing `TrackingNumber.rawStatusJson` and all `TrackingEvent` rows already loaded by `getTrackingDashboard()`; no migration or new provider dependency was added.
- Implemented dashboard useful graphic enhancement: the root `/` launch target graph now includes a `Phase I coverage bridge` segmented bar showing assembled units, currently buildable units, and remaining Phase I gap before the existing ready/gap/target metrics. This reuses existing launch-readiness data and remains read-only.
- Verification on 2026-06-16 21:45 EDT: targeted tracking/dashboard tests passed (4 files / 23 tests); `npm run typecheck`, `npm run lint`, full `npm run test:serial` (74 files / 345 tests), `npm run build`, and `git diff --check` passed. Fresh `npm run start:local` served HTTP 200 for `/tracking?smoke=package-data-dashboard` with `Full package tracking data`, `Raw provider status payload`, and `Complete event timeline`; `/` returned HTTP 200 with `Phase I coverage bridge`, launch coverage aria label, and `Useful visual signals`. Browser smoke confirmed `/tracking` rendered 3 expandable full-data disclosures, expanded first disclosure showed metadata + timeline + raw payload headings, dark-mode CSS loaded (421 rules), and 0 console/JS errors. Browser `/` confirmed the coverage bridge/segments, `Useful visual signals` before KPI cards, equal graph-panel heights in rows, dark background, and 0 console/JS errors.
- Safety: UI/read-only summarization only. No stock receiving/movement, delivery confirmation, purchase action/order placement, invoice approval/payment/journal, Alibaba capture/import, or supplier message side effects were performed. Runtime mutation was limited to test fixtures that cleaned themselves up.

## Previous session — accounting approval usability and safety-gate pass
- Request: research and implement ways to make the accounting section more usable for the operator's use case, using subagents as necessary.
- Research/orchestration: inspected prior accounting checkpoints/session history, current accounting routes/modules/tests, and a read-only accounting/purchasing live snapshot. Ran one Hermes `delegate_task` Qwen advisory audit and one centralized Lambenti Agent Control Plane read-only audit (`C:/LambentiAI/lambenti-agent/runs/run_20260616_210048`); both returned generic/hallucinated file suggestions, so implementation decisions used direct repo/live-browser evidence.
- Live accounting state before implementation: 37 supplier invoices; 1 `RECEIVED` open AP invoice (`WINNIE-XU-304716450001023166-2`, USD145.50) missing a due date; 0 approved invoices; 0 active duplicate invoice clusters; default AP posting setup ready.
- Implemented `/accounting` command-center refinement: attention queue now distinguishes invoices blocked before approval from invoices actually ready for approval; the daily bookkeeping routine now shows `ready · blocked` instead of a vague received-invoice count. This makes the live missing-due-date Winnie XU invoice surface as a blocker instead of implying it can be approved immediately.
- Implemented `/accounting/invoices` usability/safety refinement: the approval workbench now splits received invoices into `Ready to approve`, `Fix before approval`, and `Payment queue`; blocked rows show exact blockers, inline `Set due date`, and no approve button. The full supplier-invoice ledger also replaces approval controls with `Resolve approval blockers first` while blockers remain.
- Implemented service-layer guardrail: `updateInvoiceStatus(... APPROVED ...)` now rejects supplier-invoice approval when no source evidence exists, before posting AP journals. This preserves the previous no-stock/no-payment boundary while making the UI promise enforceable for evidence readiness.
- Verification: targeted accounting tests passed (4 files / 10 tests); `npm run lint`, `npm run typecheck`, full `npm run test:serial` (74 files / 345 tests), and `npm run build` all passed. HTTP `/accounting/invoices?smoke=accounting-usability` returned 200 with `Ready to approve`, `Fix before approval`, `Blocked before approval`, `Set due date`, and `Resolve approval blockers first`. Browser smokes for `/accounting/invoices` and `/accounting` rendered the blocker workflow with 0 console/JS errors; `/accounting/invoices` showed 0 approve-from-queue buttons, 1 Set due date button, and `Approval blocked` for the live blocker row.
- Safety: no live invoice approval, payment reconciliation, journal posting, bank allocation, stock receiving/movement, purchase action, Alibaba capture/import, delivery confirmation, or supplier message was performed. Runtime mutation was limited to test fixtures that cleaned themselves up.

## Previous session — tracking dashboard research, refresh-failure repair, and progress-rail UX slice
- Request: research best tracking-dashboard examples/patterns, use advisory agents for research/testing/checking, improve `/tracking`, and fix the visible refresh failure.
- Agent orchestration: initial `delegate_task` failed because LM Studio exposed `qwen2.5-coder-7b-instruct` at only 8,192 context versus Hermes' 64,000-token child-agent minimum. Attempted to reload Qwen at 65,536 context with `--parallel 1 --gpu 0.75`, but LM Studio failed at ~65%; restored a temporary direct local Qwen load at 32,768 context / parallel 1 to run the advisory agents, then after verification successfully reloaded Qwen at 65,536 context / parallel 1 / `--gpu 0.5`. A final `delegate_task` smoke completed on `qwen2.5-coder-7b-instruct`, confirming Hermes delegation is available again. Ran 5 local advisory research agents, 5 test-planning agents, and 5 check/review agents via the LM Studio OpenAI-compatible API; outputs are saved under `var/hermes-tracking-agents/`.
- Research synthesis: accessible current references emphasized central shipment dashboards, real-time carrier statuses, exception/error surfacing, estimated delivery/status milestones, webhooks/refresh hooks, carrier auto-detection, and notifications/status timelines. AfterShip and some ShipStation/Shopify pages were blocked by bot protection/403 in this environment, so implementation used accessible EasyPost, Ship24, 17TRACK, ShipStation homepage evidence plus direct repo/browser evidence. Best low-risk slice for Lambenti was not a new logistics table: keep the attention-first workbench and add a clear status/progress rail plus robust provider failure handling.
- Root cause reproduced with live Ship24 evidence: tracking `7321315589070429` was stuck at `refreshStatus=FAILED` with generic `Ship24 tracking service returned HTTP 400`. A wrapped fetch of the app's actual request showed Ship24 returned `tracker_conflict`: `A Tracker with similar clientTrackerId and conflicting parameters already exists and is still active.` Direct Ship24 calls with fresh client tracker IDs returned HTTP 201, confirming stale bare `TrackingNumber.id` reuse as the failure mode.
- Implemented fix in `src/modules/tracking/service.ts`: Ship24 requests now use a conflict-resistant client tracker ID of `<TrackingNumber.id>-<request-signature>` where the signature is a short SHA-256 over tracking number, shipment reference, destination/origin country, and carrier hint. This preserves the local row ID prefix while avoiding active Ship24 tracker conflicts after mutable request parameters change. Ship24 HTTP failure messages now include the provider's error message instead of only `HTTP 400`.
- Implemented UI improvement in `src/app/tracking/page.tsx`: each open shipment card now includes a `Shipment progress` rail (`Info received → In transit → Out for delivery → Delivered`) with a blocked/provider-review state for failed/exception rows. This mirrors strong tracking-dashboard status timelines while keeping `/tracking` scan-friendly and operator-focused.
- Verification on 2026-06-16: targeted tracking tests passed (`npm run test -- src/modules/tracking/service.integration.test.ts src/app/tracking/page.test.ts --run --no-file-parallelism`: 2 files / 19 tests). Full gates passed: `npm run lint`, `npm run typecheck`, `npm run test:serial` (74 files / 344 tests), and `npm run build`.
- Live refresh verification: direct `refreshTrackingNumber()` for `7321315589070429` changed the live row from `FAILED` / generic HTTP 400 to `SUCCESS`, `currentStatus=PENDING`, `refreshError:null`, next refresh scheduled, with `stockMovementSideEffects:0` and `invoiceSideEffects:0`. `npm run agent:tracking-refresh -- --json` then reported configured true, scanned 0 due, refreshed 0, failed 0, skipped 2.
- Runtime/browser smoke: killed stale port-5173 PID 15744, rebuilt, started fresh `npm run start:local` as `proc_e16790917144` / PID 18016 on `127.0.0.1:5173`. HTTP `/tracking?smoke=tracking-refresh-fix` returned 200 with Ship24 configured and progress markup. Browser `/tracking?smoke=tracking-refresh-fix-browser` showed 2 open shipments, due refresh 0, failed refresh 0, 1 delivered, 3 saved numbers, 2 shipment-progress rails, CSS loaded (417 rules), dark background `rgb(2, 6, 23)`, and 0 console/JS errors. Visual smoke found the rails/cards readable in dark mode.
- Safety: shipment metadata/status refresh only. No stock receiving, PO receiving quantity changes, invoice approval/payment, bank reconciliation, Alibaba capture/import, delivery confirmation, or supplier message sending was performed. Test fixtures cleaned themselves; live side-effect counts for tracking/Ship24 actors and tracking-number invoice evidence remained zero.

## Previous session — launch control Phase I goal/copy microfix
- Request: update the root-dashboard Launch Control card title to `Phase I Launch Readiness`, remove the description under the title, and change the Phase I goal from 50 packages to 25 units.
- Implemented in `src/app/page.tsx`: title is exact-title case, the explanatory paragraph under it is removed, visible launch metric label is `25-unit Phase I target`, remaining gap copy says units, gauge reads `0/25 units`, and the Useful Visual Signals launch card kicker says `25-unit target + value mix`.
- Implemented in `src/modules/dashboard/service.ts`: Phase I launch-readiness source target is now 25 units, with next-action reason copy updated from the old target wording to 25-unit language. Updated `src/modules/dashboard/service.test.ts` and `src/app/page.test.ts` expectations.
- Verification on 2026-06-15: targeted dashboard suite passed (`npm run test -- src/modules/dashboard/service.test.ts src/app/page.test.ts -- --run --no-file-parallelism`, 2 files / 4 tests), `npm run typecheck` passed, `npm run lint` passed, and `npm run build` passed. Fresh `npm run start:local` served `/` at HTTP 200. Browser DOM/visual smoke confirmed exact title, no removed description, no visible old-target text, `25-UNIT PHASE I TARGET`, `0/25 units`, collapsed Open next actions, and 0 browser console/JS errors.
- Safety: dashboard read-only layout/summary change only. No stock, purchasing, receiving, invoice/payment, GL, Alibaba capture/import, delivery confirmation, or supplier-message side effects were performed.

## Previous session — dashboard launch/value-mix card orientation microfix
- Request: inside the same root-dashboard graph card, make the launch target meter and value mix full-width horizontal sections rather than left/right, with launch target meter first and value mix below.
- Implemented UI-only change in `src/app/page.tsx`: `LaunchTargetAndValueMixGraph` now uses a vertical `flex-col` stack; `LaunchTargetGraph` renders first; `CompactValuationMixGraph` renders below as a full-width subpanel. The standalone value-mix card remains removed and the equal-height adjacent Operations Flow card remains intact. Updated `src/app/page.test.ts` source-contract coverage and patched the dashboard visual-signals skill reference with the corrected orientation convention.
- Verification on 2026-06-15: targeted dashboard suite passed (`npm run test -- src/modules/dashboard/service.test.ts src/app/page.test.ts -- --run --no-file-parallelism`, 2 files / 4 tests), `npm run typecheck` passed, `npm run lint` passed, and `npm run build` passed. Fresh `npm run start:local` served `/` at HTTP 200. Browser visual/DOM smoke confirmed launch/value-mix top-to-bottom order (`launchBeforeValueMix:true`, `valueMixBelowNotRight:true`), Operations Flow still same row/same 430px card height, `Open next actions` still collapsed, and browser console/JS errors were 0.
- Safety: dashboard read-only layout change only. No stock, purchasing, receiving, invoice/payment, GL, Alibaba capture/import, delivery confirmation, or supplier-message side effects were performed.

## Previous session — dashboard layout/card consolidation microfix
- Request: combine the dashboard `Value mix` card with the `Launch target meter`, keep the adjacent card the same size, collapse the launch-card `Open next actions` section by default, and swap the `Useful visual signals` section above the 10 KPI stat-card grid.
- Implemented UI-only dashboard changes in `src/app/page.tsx`: `Open next actions` is now a collapsed `<details>` disclosure; `Useful visual signals` now renders above the 10-card stat grid; `Value mix` is compactly embedded inside the `Launch target meter` graph card; the graph grid uses equal-height rows so the adjacent `Operations flow` card matches the updated launch card.
- Verification on 2026-06-15: targeted dashboard suite passed (`npm run test -- src/modules/dashboard/service.test.ts src/app/page.test.ts -- --run --no-file-parallelism`, 2 files / 4 tests), `npm run lint` passed, `npm run typecheck` passed, and `npm run build` passed. Fresh `npm run start:local` served `/` at HTTP 200. Browser smoke verified `Open next actions` collapsed, visual signals before KPI cards, no standalone `Value mix` graph heading, `VALUE MIX` inside the launch card, launch/operations cards same row and same 430px outer height, and 0 console/JS errors.
- Safety: dashboard read-only layout change only. No stock, purchasing, receiving, invoice/payment, GL, Alibaba capture/import, delivery confirmation, or supplier-message side effects were performed.

## Previous session — dashboard useful visual graph slice
- Request: add dashboard graphs that make `/` look polished/"sexy" without adding useless decoration or risky side effects.
- Research/design evidence: inspected current checkpoint files, `git status`, root dashboard source, dashboard service, dashboard tests, package gates, and live DB dashboard data. Live DB summary showed 0 assembled packages, 0 current package build capacity, bottleneck `LAMBENTI_MAIN_UNIT`, component capacities ranging from 0 to 100 buildable packages, 18 low-stock rows, 4 purchase recommendations, 6 incoming orders, 5 review actions, and inventory value concentrated in components (USD 844.60 / 98%) vs finished goods (USD 15.08 / 2%). A direct local LM Studio `qwen2.5-coder-7b-instruct` advisory call recommended launch gap, stock pressure, operations flow, and value mix charts while avoiding decorative/over-complex charts.
- Implemented useful read-only dashboard graphs: `summary.dashboardGraphs` now derives launch target meter data, package BOM bottleneck bars, low-stock pressure bars, operations-flow bars linking to existing workflows, and ledger-derived valuation mix. `/` renders these as dark-mode-readable graph cards under `Useful visual signals` with copy explicitly limiting the visuals to launch gap, bottlenecks, stock pressure, workflow load, and valuation mix rather than dashboard confetti. No chart dependency/schema/mutation path was added.
- Verification on 2026-06-15: targeted dashboard tests passed after the final dark-mode contrast patch (`npm run test -- src/modules/dashboard/service.test.ts src/app/page.test.ts -- --run --no-file-parallelism`: 2 files / 4 tests), `npm run typecheck` passed, and full gates passed: `npm run lint`, `npm run typecheck`, `npm run test:serial` (74 files / 343 tests), `npm run build`, and `git diff --check` exit 0 with CRLF normalization warnings only. Production smoke on fresh `npm run start:local` served `/` at HTTP 200, loaded CSS asset `ce4e2c53d7f284cb.css` with HTTP 200, rendered all graph markers, and had 0 browser console/JS errors. Browser visual smoke confirmed the new graph section is styled/readable in dark mode with no material layout issue.
- Safety: this slice is read-only dashboard summarization/visualization only. No stock movement, receiving, purchase request/PO mutation, invoice approval/payment/reconciliation, Alibaba capture/import, delivery confirmation, or supplier messaging was performed.

## Previous session — supplier/AP/purchasing/receiving app-improvement finalization
- Request: continue the app-improvement implementation, finish verification, and checkpoint the current Lambenti inventory app state.
- Implemented/verified safe high-impact slice: supplier cleanup/quarantine for unreferenced Alibaba UI/import junk, supplier dropdown junk filtering, AP duplicate invoice guardrail/auto-void action, low-stock recommendation → draft purchase request queue, useful empty approval state, and duplicate receiving-line packing-slip summary while preserving individual human-count receiving forms.
- Live data cleanup performed under audit: archived 15 cleanup-eligible supplier junk rows as actor `hermes-supplier-cleanup-2026-06-15`; voided 27 unapproved duplicate Winnie XU supplier-invoice rows with no evidence/journals/payments as actor `hermes-invoice-dedupe-2026-06-15`; no hard deletes were used.
- Current live DB evidence after cleanup: 21 active suppliers; 0 supplier cleanup candidates; 0 active duplicate invoice clusters; 27 duplicate-invoice audit rows; supplier invoices are 9 PAID, 1 RECEIVED, 27 VOID. The remaining received AP invoice is USD145.50 for Winnie XU and still needs due-date/approval decisions by a human.
- Verification on 2026-06-15 after final edits/data cleanup: targeted page/module suite passed (8 files / 28 tests); `npm run lint` passed; `npm run typecheck` passed; `npm run test:serial` passed (73 files / 340 tests); `npm run build` passed; `git diff --check` exit 0 with CRLF normalization warnings only. Production smoke with `npm run start:local` returned HTTP 200/no error page for `/suppliers`, `/accounting/invoices`, `/purchasing/recommendations`, `/purchasing/requests`, and `/incoming`; browser rendered the same routes with cleanup-clear, duplicate-clear/voided count, recommendation queue, approvals empty-state, and grouped receiving-lines markers; browser console/JS errors were 0.
- Safety: no stock receiving, purchase approval, purchase-order placement, invoice approval, payment/reconciliation, Alibaba capture/import, delivery confirmation, or supplier messaging was performed. Data changes were limited to audited supplier archive and audited duplicate-invoice `VOID` status.

## Previous session — app-wide route/data connectivity hardening and verification pass
- Request: repeat the improvement/hardening pass, implement necessary fixes, and verify every app section plus data connections are functional.
- Implemented broader deterministic app coverage: `scripts/smoke.mjs` now checks all 21 page routes, 5 high-signal UI contracts, 4 read-only JSON agent APIs, and 3 accounting CSV export APIs; `scripts/smoke.test.mjs` and `src/app/app-sections.test.ts` guard route/section coverage so new pages do not silently fall out of smoke. Added `scripts/browser-section-smoke.mjs` plus `npm run smoke:browser` to render all 21 sections in Playwright and fail on page/console errors.
- Fixed defects found during verification: mounted the `/accounting/invoices` duplicate-invoice guardrail instead of leaving the new service/action/UI code unused; wired `/suppliers` cleanup candidates to a human-gated `archiveSupplierCleanupCandidatesAction`; tightened supplier junk filtering so Alibaba UI/deep-link/legal-entity labels such as `Alibaba.com Singapore E-Commerce Private Limited` cannot enter confirmed supplier dropdowns; updated stale smoke copy for current supplier confirmation UI; and changed browser smoke from strict `networkidle` navigation to `domcontentloaded` plus bounded network-idle wait so long-lived Next/data requests do not produce false section failures.
- Verification on 2026-06-15 after final edits: targeted smoke/section/supplier/accounting tests passed; full `npm run quality` passed (`lint`, `typecheck`, serial Vitest 73 files / 340 tests, and Next production build). `npm run smoke` passed after its own production build with 21 pages, 5 UI contracts, 4 JSON APIs, and 3 CSV exports. Fresh `npm run start:local` plus `npm run smoke:browser` passed all 21 pages with no Playwright console/page errors. `npm audit --omit=dev` and full `npm audit` both returned 0 vulnerabilities. `git diff --check` returned exit 0 with only CRLF normalization warnings on touched files.
- Runtime notes: an intermediate `/incoming` production smoke showed the generic Server Component error while a stale/corrupt `.next`/port-5173 state was present; clearing/rebuilding `.next`, killing the stale port owner, and rerunning the production and browser smokes verified `/incoming` and all other sections green from the current tree.
- Safety: verification was read-only at app-runtime level (GET/CSV/API smokes and browser rendering only). No stock receiving, inventory mutation, purchase approval, invoice approval/payment, bank reconciliation, Alibaba capture/import, delivery confirmation, or supplier-message side effect was performed. Integration-test fixtures created expected temporary rows and cleaned them through the test suite.

## Previous session — tracking page simplification/hardening pass
- Request: clear context, simplify the `/tracking` page, make the UI better/more intuitive, harden behavior, implement, run tests, browser-smoke, and update checkpoints.
- Implemented `/tracking` as a simpler operator-first workbench: hero explains the evidence-only safety boundary, summary cards show open/due/failed/delivered/saved counts, `What needs attention` now appears before detailed rows, open non-delivered shipments are scan-friendly cards instead of a dense active table, failed refreshes get explicit retry actions, and manual entry / delivered history / lead-time learning are collapsed behind clear progressive-disclosure sections. The page still excludes the old automatic Alibaba capture card/action.
- Hardened tracking display/service behavior: dashboard rows normalize stored/provider status before display, Ship24 `data` / information-received milestones normalize to `PENDING` instead of leaking provider jargon, junk supplier labels such as `to ship` are hidden from display rows, unlinked email-import rows are labeled `Evidence import …`, and dark-theme coverage now includes orange utilities plus emerald success/delivered states so the simplified tracking cards remain readable.
- Regression coverage: `src/app/tracking/page.test.ts` now asserts the simplified sections and Alibaba-capture absence; `src/modules/tracking/service.integration.test.ts` covers Ship24 data/info normalization and junk supplier-label suppression.
- Verification: targeted tracking tests passed (`src/app/tracking/page.test.ts`, `src/modules/tracking/service.integration.test.ts`: 2 files / 18 tests). Full gates passed: `npm run lint`; `npm run typecheck`; `npm run test:serial` (66 files / 315 tests); `npm run build` (Next production build lists `/tracking`). Passing integration tests still print expected Prisma duplicate/deadlock retry messages.
- Runtime/browser smoke: restarted a clean `npm run start:local` after killing stale port-5173 owners; live app is listening on `127.0.0.1:5173` PID 35164. HTTP `/tracking?smoke=clean-server` returned 200. Browser `/tracking?smoke=clean-server-browser` confirmed the expected markers, 2 active tracking rows, 1 delivered row, CSS asset loaded with 349 rules, dark body background `rgb(2, 6, 23)`, failed-card background mapped to dark red, and 0 browser console/JS errors. Final visual QA showed the attention queue, open shipment cards, failed refresh state, success/delivered badges, and collapsed Add tracking / Delivered / Advanced sections are legible in dark mode.
- Safety: no stock receiving, purchase approval, invoice approval/payment, bank reconciliation, Alibaba capture/import, delivery confirmation, or supplier message side effect was performed. The live failed refresh for `7321315589070429` remains Ship24 HTTP 400 provider data, surfaced clearly for operator review; it is not a stock/accounting blocker.

## Previous session — accounting command-center simplification/functionality pass
- Request: research ways to make the accounting section more functional and simple, look at common accounting-app patterns, replicate best practices where safe, and verify it works flawlessly.
- Research evidence: inspected current accounting repo state/checkpoints, direct DB state, and public accounting-app references. Zoho Books Home emphasizes a dashboard with current/overdue receivables/payables, cash flow, income/expense, bank/credit-card panels, and account watchlists; Xero/FreshBooks/Wave emphasize all accounting in one place, bank reconciliation/transaction management, clear reports/metrics, cash-flow/business-health visibility, and tax-readiness. The centralized Lambenti Agent Control Plane ran read-only audit `C:/LambentiAI/lambenti-agent/runs/run_20260615_064205`, but it again returned hallucinated JS file names (`/accounting/AccountingService.js`), so implementation decisions used direct repo/browser evidence.
- Implemented `/accounting` Simple accounting command center: open payables total, received/approved counts, overdue and due-next-7-day aging, bank reconciliation unmatched totals, default GL posting setup health, next supplier bills, and a five-step daily bookkeeping routine (capture → review → setup → approve → pay). The existing Attention queue now also surfaces overdue payables and missing required GL mappings. This dashboard functionality does not create invoices, approve/post journals, reconcile payments, receive stock, or mutate inventory.
- Implemented `src/modules/accounting/overview.ts` with tested pure summaries for payables aging, bank reconciliation, and required default GL mappings (`INVENTORY_ASSET`, `TAX_RECOVERABLE`, `ACCOUNTS_PAYABLE`, `BANK_CASH`). Added `src/modules/accounting/overview.test.ts` and updated `/accounting` page source-contract coverage.
- Implemented one-click default AP posting setup on `/accounting/accounts` via `installDefaultApPostingSetup()`: creates/keeps safe starter accounts 1000 Operating bank, 1060 GST/HST recoverable, 1300 Inventory asset, 2000 Accounts payable only when no active default mapping exists. Ran it once on the live local DB as actor `hermes-accounting-command-center`; it created the 4 default mappings/accounts and left invoice/payment status unchanged.
- Implemented `/accounting/invoices` approval workbench: received/ready/missing-evidence/missing-due-date/payment-queue metrics, compact approval/payment queues, primary-source-evidence labels, visible `Approve from queue` buttons, review-readiness column, and per-invoice `Save due date` form. Added transactional/audited `updateSupplierInvoiceTerms()` / `updateInvoiceTermsAction()` so due dates/notes can be fixed without posting journals, paying invoices, or touching stock.
- Verification: targeted accounting tests passed (`overview`, `gl.integration`, `invoices`, invoice page/action tests, accounts page, accounting page: 7 files / 13 tests); `npm run lint` passed; `npm run typecheck` passed; full `npm run test:serial` passed (66 files / 313 tests); `npm run build` passed. Fresh `npm run start:local` restarted on `127.0.0.1:5173`; HTTP smokes for `/accounting`, `/accounting/invoices`, and `/accounting/accounts` returned 200 with the command center, invoice approval workbench, queue approval controls, due-date controls, and default AP setup content. Browser `/accounting/invoices?smoke=approval-workbench-final2-browser` rendered the approval workbench with `Approve from queue`, `primary source evidence saved`, and no console/JS errors; visual dark-mode smoke found the queue/cards/buttons legible. Re-verified after context compression on 2026-06-15 08:00 EDT: `npm run lint`, `npm run typecheck`, full `npm run test:serial` (66 files / 313 tests), and `npm run build` all passed; HTTP smokes for `/accounting`, `/accounting/invoices`, `/accounting/accounts`, and `/inventory/movements` returned 200 with expected accounting/movement markers; browser invoice-workbench smoke found expected controls and 0 console/JS errors.
- Runtime/data evidence after GL setup: live `/accounting` currently shows 37 supplier invoices, 28 open/received invoices, USD4074.00 open AP, 0 approved invoices, 0 unmatched bank transactions, 0 posted journals, and default posting setup `readyForPosting:true` with no missing mappings. No live invoice approval/payment/reconciliation/stock mutation was performed; only default GL setup and test-scoped fixture rows mutated data.

## Previous session — inventory hardening research and stock-ledger running balance
- Request: research fragile sections of the inventory app, identify improvements, harden it, increase functionality, and implement.
- Research evidence: inspected repo checkpoints, `AGENTS.md`, package scripts, recent state files, route/test coverage, TODO/throw hotspots, and current source. Baseline `npm run lint` and `npm run typecheck` passed before implementation. The centralized Lambenti Agent Control Plane was available and ran read-only audit `C:/LambentiAI/lambenti-agent/runs/run_20260615_020130`, but its report was generic and referenced hallucinated files (`inventory.js`, `auditLog.js`), so implementation decisions used direct repo evidence.
- Implemented inventory movement audit hardening: `/inventory/movements` recent ledger rows now show signed ledger impact (`+`, `-`, or reserve) and per-item balance after each visible ledger entry (`on hand`, `available`, `reserved`). `getMovementPageData()` now computes those balances from the full visible movement history for the items represented in the recent table while continuing to hide operator-voided originals and `VOID:` compensating rows from the operator list.
- Added coverage: page source-contract assertions for the new ledger-impact/balance columns and integration coverage proving RECEIVE/RESERVE/CONSUME rows update per-item running balances independently across items.
- Verification: targeted movement tests passed (`src/app/inventory/movements/page.test.ts`, `src/app/inventory/movements/data.integration.test.ts`, 2 files / 7 tests); `npm run lint` passed; `npm run typecheck` passed; full `npm run test:serial` passed (62 files / 306 tests); `npm run build` passed; HTTP `/inventory/movements?smoke=running-balance` returned 200 with `Balance after entry` and `Ledger impact`; browser smoke rendered `/inventory/movements?smoke=running-balance-browser` with those columns and 0 console/JS errors.
- Safety/runtime: no stock movement, receiving, invoice, payment, Alibaba capture, purchase approval, or accounting mutation was performed by this pass beyond test-scoped `TEST-MOVEMENT-DATA-*` rows that were cleaned by the integration tests.

## Previous session — email-triggered shipment capture and lead-time learning
- Request: when mailbox sync sees shipment confirmations, use the Alibaba order-detail link in the email to trigger the Alibaba capture path, save tracking numbers, add them to `/tracking`, link them to the order created from the initial-payment email, calculate order-to-receipt lead time, update item lead-time days, and show an expandable per-item lead-time log for reorder forecasting buffers.
- Implemented mailbox flow: `syncAlibabaMailbox()` now counts shipment confirmations, extracts Alibaba order/detail/message URLs only from shipment/shipping/tracking emails, runs targeted tracking-only Alibaba portal capture for those exact links, then backfills saved Alibaba/email tracking evidence into `TrackingNumber` rows. The sync action now returns the sync summary so the UI reports shipment-confirmation counts, target-link counts, portal snapshots/imports, tracking saved/updated, and warnings. Non-shipment initial-payment emails do not trigger portal capture.
- Implemented linking flow: shipment import tracking can now resolve back to an earlier initial-payment/order import with the same Alibaba external order ID and an applied purchase order, while preserving the shipment email as evidence. Email-order dedupe now prefers an existing import already linked to a purchase order when matching by external order ID.
- Implemented lead-time learning: `getLeadTimeLog()` builds item-level samples from purchase order order/payment dates, tracking delivery evidence, and receipt stock movements. `/tracking` now has `Lead-time learning log` under `Reorder forecasting input`, with expandable item rows showing current item lead time, weighted average order-to-receipt days, quantity ordered/received, start/end dates, endpoint source, shipping-time evidence, and tracking numbers. Existing `syncLeadTimeAveragesForPurchaseOrder()` still updates supplier/item `leadTimeDays` from completed samples; reorder automation now includes the learned lead-time buffer in shortage findings and draft-PR rationale.
- Implemented targeted portal-agent support: `scripts/alibaba-portal-agent.mjs` accepts/dedupes `--tracking-target-url` / `LAMBENTI_ALIBABA_TRACKING_TARGET_URLS`, uses those email-provided URLs as first tracking-only targets, and falls back to the existing broad Delivering/Completed/Message Center sweep when no target URL is provided. Removed the script shebang because Vitest's inlined ESM evaluator put transformed code before it; npm scripts already invoke it through `node`.
- Verification: `npm run lint` passed; `npm run typecheck` passed; targeted suite passed (`scripts/alibaba-portal-agent.test.mjs`, mailbox tests, capture-agent tests, tracking page test, tracking service integration, automation integration: 6 files / 56 tests); full `npm run test:serial` passed (62 files / 305 tests); `npm run build` passed. HTTP smoke `/tracking?smoke=lead-time-log` returned 200 with lead-time learning content; browser smoke `/tracking?smoke=lead-time-log-browser` rendered `/tracking`, confirmed `document.body.innerText` contains `Lead-time learning log`, and had 0 console/JS errors.
- Safety/runtime: no live Alibaba browser capture, purchase approval, payment, invoice approval, stock receiving, or message sending was triggered during this pass. A temporary `npm run start:local` smoke server was started and then stopped; port 5173 has no LISTENING owner after cleanup.

## Previous session — tracking capture UI removal and Order Email Agent queue repair
- Request: remove the automatic Alibaba capture section from `/tracking` and fix the Order Email Agent, which had stopped showing recognized supplier/item matches from imported emails.
- Root cause found with live DB/page evidence: recent tracking-only Alibaba portal Message Center snapshots were being persisted as `EmailOrderImport` rows (`source=ALIBABA_PORTAL`, subjects such as `Alibaba portal message thread`, many with fallback `Unparsed order email line`). Because `/integrations/email-import` read the newest active imports, those shipment/tracking evidence rows dominated the active Order Email Agent queue and made it look like email item recognition had failed. Existing actual order imports still had correct matches (for example LED strip aliases, 5952 manual matches, Micro-Fit SKU, PSU alias, cable/clip portal applied rows).
- Implemented fixes: `/tracking` no longer imports or renders `captureAlibabaTrackingAction` / `Automatic Alibaba capture`; it keeps manual tracking drop box plus automatic provider refresh. `getEmailOrderImports()` and `reassessRecentEmailOrderImports()` now exclude tracking-only Alibaba portal message/tracking evidence from the active Order Email Agent queue while leaving real actionable email/order imports visible and leaving tracking metadata available to `/tracking`.
- Regression coverage: `src/app/tracking/page.test.ts` now asserts the Alibaba capture card/action is absent from the tracking page. `src/modules/email-imports/alibaba-email.archive.integration.test.ts` now covers hiding a tracking-only Alibaba portal message snapshot while preserving a real actionable supplier email in `getEmailOrderImports()`.
- Verification: targeted tests passed (`npm run test -- src/app/tracking/page.test.ts src/modules/email-imports/alibaba-email.archive.integration.test.ts --run --no-file-parallelism`, 2 files / 16 tests); `npm run lint` passed; `npm run typecheck` passed; full serial Vitest passed (62 files / 299 tests); `npm run build` passed. Runtime checks on fresh `npm run start:local`: HTTP `/tracking?smoke=remove-alibaba-capture` returned 200 with no `Automatic Alibaba capture` / `Capture Alibaba tracking`; HTTP `/integrations/email-import?smoke=order-agent-filter` returned 200 with `Order Email Agent`, matched LED item text, and no `Alibaba portal message thread` / fallback tracking noise. Browser `/tracking` rendered manual drop box + automatic refresh with 0 console/JS errors; browser `/integrations/email-import` rendered matched imports including `0805_CLIP`, `5952_DIE_CUT_*`, `LED-COB-12V-*`, and had 0 console/JS errors.
- Runtime state: killed stale port-5173 PID 28388 and restarted `npm run start:local` from the rebuilt tree; current tracked process is `proc_ce5a89357cf8` / PID 3628 and is intentionally left running on `127.0.0.1:5173` for operator review.

## Previous session — Hermes runtime readiness interruption repair and tracking-workbench resume verification
- Request: the Hermes GUI repeatedly interrupted resumed work with `request timed out: setup_runtime_check` followed by `Inference not ready`; fix the cause, then resume the interrupted Lambenti work.
- Hermes root cause/fix: live logs showed repeated `setup.runtime_check` WebSocket response-send failures after transient status/readiness RPC timeouts while long-running turns were active. Patched Hermes desktop readiness polling to avoid overlapping status refreshes and to preserve the last valid inference-ready state when the only failure is a transient transport timeout/connection flap. Also hardened Hermes Windows log rollover so a locked log file no longer emits repeated stdlib logging tracebacks during busy multi-process desktop/profile/gateway use. Rebuilt the Hermes desktop renderer and copied the patched production `dist` into `apps/desktop/release/win-unpacked/resources/app.asar.unpacked/dist` for next launch.
- Hermes verification: runtime provider resolution for the Lambenti profile returned `ok:true`, provider `openai-codex`, source `device_code`, provider configured true; Hermes targeted Vitest for runtime readiness passed (6 tests), desktop typecheck passed, targeted ESLint on changed desktop files passed, desktop production build passed, `git diff --check` passed after LF normalization, and logging tests passed (`58 passed, 5 skipped` on Windows-specific logging suite).
- Resume context: latest Lambenti checkpoint/session showed the interrupted work was the Alibaba tracking capture saved-row recognition/idempotency repair. Checkpoint files said that repair was complete, but current-tree verification found two drifts: TypeScript inference in `src/modules/tracking/service.ts` failed (`leadTimeEndSource` widened to `string | null`, `earliestDate` nullability), and `/tracking` source had lost the `Capture Alibaba tracking` action while its test still expected capture.
- Lambenti fixes this pass: added explicit tracking lead-time result typing and safer `earliestDate` Date narrowing; restored `/tracking` `captureAlibabaTrackingAction` server action and operator card/button using the existing tracking-only Alibaba portal capture module, saved-evidence backfill, old-Alibaba-row prune, and action summary. Capture copy again states dedicated automation profile use, saved-row hydration, no repeat order candidates, generic Logistics Services skip, Message Center stale-chat behavior, last-3-month scope, and no message sending.
- Lambenti verification: `npm run typecheck` passed after the type fix; targeted `npm run test -- src/app/tracking/page.test.ts src/modules/tracking/alibaba-capture-agent.test.ts --run --no-file-parallelism` passed (2 files / 8 tests); full `npm run quality` passed (`lint`, `typecheck`, `62 files / 298 tests`, and Next production build). Restarted `npm run start:local`; HTTP `/tracking?smoke=resume-runtime-fix` returned 200 with `Capture Alibaba tracking` and `SHIP24 configured`; browser `/tracking?smoke=resume-runtime-fix-browser` rendered the capture card, saved tracking rows, Ship24 status, and had 0 console/JS errors.
- Safety/open risk: no live Alibaba capture/import was triggered during this resume verification; browser smoke was read-only. The current `start:local` server is running on `127.0.0.1:5173` for operator review.

## Previous session — Alibaba tracking capture saved-row recognition/idempotency repair
- Request: after a Capture Alibaba run clicked Delivering, opened tracking pages, scanned Completed & In Review orders, failed to find lower-page Track Order lines, repeated sections, went to unnecessary Logistics Services, and failed to open messages, make sure tracking numbers are actually saved/recognized and that already-saved order elements are not reprocessed.
- Root cause/confirmation: the stale-scan memory only knew tracking found during prior browser-agent reads; it did not hydrate from persisted `TrackingNumber` rows before clicking Alibaba order candidates. Current DB has 3 tracking rows, including Alibaba order `304716450001023166` with tracking `888071620741`, but an empty/stale `var/alibaba-portal-agent-state.json` memory could still allow that order card to be reopened.
- Implemented fixes in `scripts/alibaba-portal-agent.mjs`: tracking-only capture now hydrates runtime scan memory from saved `TrackingNumber` rows before visiting Alibaba, records `savedTrackingRowsHydrated`/`savedTrackingOrdersHydrated`, treats those hydrated orders as `tracking-already-captured`, skips generic Alibaba Logistics Services marketplace links before click/navigation, and reports generic-logistics skips separately. This preserves existing Completed & In Review Track Package/Track Shipment(s) prioritization, nested tracking-button following, stale no-tracking fingerprint memory, and unchanged Message Center thread skipping.
- Operator-visible updates: `/tracking` copy now explicitly states that capture first hydrates from saved tracking rows, skips generic Alibaba Logistics Services marketplace links, and does not reopen already-captured order candidates. The capture summary now reports saved-tracking orders recognized before clicking plus generic logistics-service candidates skipped.
- Runtime state repair: `var/alibaba-portal-agent-state.json` was hydrated from the current DB after the code patch. Verification output showed `trackingRows:3`, `savedTrackingRowsHydrated:1`, `savedTrackingOrdersHydrated:1`, `rememberedOrders:8`, `order304TrackingNumbers:["888071620741"]`, and a simulated Completed & In Review Track Package candidate for order `304716450001023166` returned `{ skip:true, reason:"tracking-already-captured", key:"order:304716450001023166" }`.
- Verification gates: `node --check scripts/alibaba-portal-agent.mjs`; targeted tests `npm run test -- scripts/alibaba-portal-agent.test.mjs src/modules/tracking/alibaba-capture-agent.test.ts src/app/tracking/page.test.ts --run --no-file-parallelism` passed (3 files / 25 tests); full `npm run lint && npm run typecheck && npm run test:serial && npm run build` passed (62 files / 298 tests + Next production build). Browser smoke of `/tracking?smoke=tracking-capture-idempotency-browser` rendered the new copy and current rows, including `888071620741` linked to Alibaba order `304716450001023166`; browser console/JS errors were 0. The smoke server was stopped afterward and port 5173 was cleared.
- Safety/open risk: no live Alibaba capture/import was triggered in this repair pass; verification used source tests, current DB rows, runtime memory hydration, and `/tracking` browser smoke. The patched capture path remains read-only for portal navigation and still uses `autoApply:false`/`autoCreateInvoices:false`; it must not receive stock, approve/pay invoices, confirm delivery, or send Alibaba messages.

## Previous session — resumed stale-aware Alibaba tracking capture repair
- Request resumed from the latest user message: the Capture Alibaba tracking function still rechecked stale entries, needed a real memory of what order/chat surfaces had already been checked, had to follow visible `Track Package` / `Track Shipment(s)` controls through multiple safe read-only layers, and had to stop skipping entries in `Completed & In Review` while still avoiding `Waiting for supplier to ship` items.
- Root cause found in `scripts/alibaba-portal-agent.mjs`: order candidates were only remembered when a relevant snapshot was produced, so no-tracking/null detail reads could be retried forever; candidate memory could store a mismatched `orderId` when an opened page/list contained another order number; Completed & In Review detail candidates were selected first-come rather than prioritizing tracking/logistics controls; a generic Alibaba logistics-services link could be treated as an order candidate; Message Center nested DOM elements could create duplicate thread-memory keys; and `trackingMemory.messageThreadsRemembered` in JSON output was being overwritten by the per-run stat value.
- Implemented fixes in `scripts/alibaba-portal-agent.mjs`: no-tracking order attempts are recorded as checked with their current fingerprint; `order:<id>` memory keys now keep the keyed order id even when page text contains another order id; stale memory normalization repairs old mismatched entries; Completed & In Review gets a larger first-pass candidate budget and candidate scoring/deduping that prioritizes `Track Package` / `Track Shipment(s)` / logistics controls; generic logistics-services marketplace links are filtered out; nested tracking buttons are still followed safely; Message Center candidates are deduped by visible-row/text family; and JSON memory totals now report remembered message threads correctly.
- Regression coverage updated in `scripts/alibaba-portal-agent.test.mjs` for no-tracking attempt memory, keyed order-id preservation, nested Track Package/Shipment source contract, completed-review candidate budget/prioritization, generic logistics-link filtering, and Message Center duplicate-thread dedupe. Existing app/module tracking tests still cover the `/tracking` summary/copy and server-action wrapper.
- Verification after final edits: `node --check scripts/alibaba-portal-agent.mjs`; `npm run test -- scripts/alibaba-portal-agent.test.mjs src/modules/tracking/alibaba-capture-agent.test.ts src/app/tracking/page.test.ts --run --no-file-parallelism` passed (3 files / 23 tests); full gates passed with `npm run lint && npm run typecheck && npm run test:serial && npm run build` (62 files / 296 tests + Next production build).
- Live portal dry-run evidence: a first unbounded dry-run with the expanded previous-order scan exceeded the 600s terminal cap but did update local memory; the dedicated automation Chrome profile it left open was then closed by killing only Chrome processes using `var/alibaba-chrome-profile`. A bounded controlled dry-run then completed with `configured:true`, `loginRequired:false`, `securityChallengeRequired:false`, `capturedSnapshots:3`, no imports, no invoices, no apply, `ordersRemembered:8`, `messageThreadsRemembered:27`, and corrected stale-scan memory output. This was dry-run only, so it did not create portal imports or inventory/accounting side effects.
- Runtime/browser smoke: started fresh `npm run start:local`; HTTP `/tracking?smoke=resume-stale-memory` returned 200; browser rendered `/tracking?smoke=resume-stale-memory-browser` with copy explicitly describing Track Package / Track Shipment(s), no-tracking stale-card memory, unchanged-chat skipping, and the Delivering → Completed & In Review → Message Center sequence; browser console/JS errors were 0. Smoke server PID 13072 was stopped afterward; `netstat` shows no remaining `LISTENING` owner on 5173.

## Previous session — Capture login handoff showed stale Work-profile text and timed out
- Request: user pressed `/tracking` Capture Alibaba and got a message saying a manual Chrome/login handoff was opened for the configured Work profile, while the detailed warning referred to the dedicated Alibaba automation profile and still required login.
- Root cause reproduced: the dedicated automation profile was open on Alibaba's trusted-account confirmation page (`Continue as` + `Yes`), but the app could not click Alibaba's `Yes` confirmation because the dedicated profile has no Chrome profile email metadata and the capture child did not inherit a trusted confirmation email fallback. The already-open-profile fallback also had stale `configured Chrome Work profile` wording even when the selected profile was the dedicated automation profile. After login succeeded, a full tracking sweep took ~170s dry-run and the server action's 180s child timeout killed the real app capture before JSON returned.
- Implemented fix: clicked the visible trusted Alibaba `Yes` confirmation in the dedicated automation profile to finish the current login handoff; capture env now carries `LAMBENTI_EMAIL_IMAP_USER` as `LAMBENTI_ALIBABA_ACCOUNT_CONFIRM_EMAIL` fallback so future dedicated-profile captures can click trusted Alibaba account confirmations safely. Portal-agent fallback messages now use `formatBrowserProfile(browserProfile)` instead of hard-coded Work-profile text. Interactive capture timeout increased from 180s to 420s to allow the Delivering → Delivered → Message Center sweep to finish under current Alibaba latency. `/tracking` page copy now explains the trusted-account prompt and manual close/retry step.
- Live portal verification: forced dedicated-profile dry-run completed with `configured:true`, `loginRequired:false`, `securityChallengeRequired:false`, `capturedSnapshots:12`, warning only `orders-delivered: could not identify the delivered order tab; scanned the visible orders page as fallback.` Runtime non-dry dedicated capture completed with `configured:true`, `loginRequired:false`, `securityChallengeRequired:false`, `capturedSnapshots:12`, `imported:11`, `duplicates:1`, `needsReview:11`.
- App/runtime verification: rebuilt and restarted `npm run start:local`; HTTP `/tracking?smoke=after-app-capture` returned 200 with trusted-account copy present, stale `configured Work profile` text absent, timeout text absent, and tracking `7321315589070429` present. Browser console/JS errors were 0 after reload. DB after app capture: `trackingRows:3`, `alibabaPortalImports:35`, `deliveredWithNextRefreshAt:0`, `stockMovementSideEffects:0`, `invoiceSideEffects:0`.
- Verification gates: targeted checks passed (`node --check scripts/alibaba-portal-agent.mjs`; `npm run test -- scripts/alibaba-portal-agent.test.mjs scripts/alibaba-portal-login-assist.test.mjs src/modules/tracking/alibaba-capture-agent.test.ts src/app/tracking/page.test.ts --run --no-file-parallelism`, 4 files / 20 tests). Full gate passed after final edits: `npm run lint && npm run typecheck && npm run test:serial && npm run build` (62 files / 288 tests + Next build).
- Open watch: Alibaba's Delivered tab selector still falls back to scanning visible orders when it cannot identify the delivered tab; current capture still imports existing tracking evidence, but the selector should be hardened if delivered-only evidence is missed. Ship24 currently returns HTTP 400 for `7321315589070429`; this is a carrier/provider refresh issue, not an Alibaba capture/import blocker.

## Previous session — Alibaba orders Delivering/Delivered + Message Center exhaustive tracking capture
- Request: make the `/tracking` Capture Alibaba button quickly check every required source for available tracking numbers: the Alibaba orders tab under Delivering, the Alibaba orders tab under Delivered, then the Alibaba Messages page by individually clicking/scrolling each conversation; store all qualifying tracking evidence.
- Implemented required capture sequence: `scripts/alibaba-portal-agent.mjs` now builds tracking-only targets in order: `orders-delivering`, `orders-delivered`, then `messages`. Controlled capture navigates to the orders URL for each order target, clicks the matching Delivering/Delivered order-status tab when visible, scrolls lazy-loaded order surfaces, opens safe read-only order/detail/logistics candidates, then opens `https://message.alibaba.com/message/messenger.htm` and clicks/scrolls each visible supplier conversation thread.
- Implemented speed improvements for repeated button clicks: tracking-only capture skips invoice-document download passes (`autoCreateInvoices:false` remains enforced), uses tracking-specific shorter network/settle waits for order detail and message-thread scans, caps snapshot imports at the existing 200 tracking-only limit, and still backfills saved Alibaba/email evidence after the live portal pass.
- Implemented evidence filtering/durability: message thread scans still emit one portal snapshot per thread only when shipping/tracking context or tracking numbers are present; `conversationContext` stores nearby shipping/tracking lines while filtering composer/send UI. Order/status snapshots retain source URL, status/order/supplier/tracking fields, text, and import provenance. `/tracking` copy and action summary now describe the exact Delivering → Delivered → Message Center sequence.
- Preserved safety boundaries: the capture excludes send/reply/inquiry/chat/pay/confirm-receipt/release-payment/delete/refund/dispute controls, uses `autoApply:false` and `autoCreateInvoices:false`, and never receives stock, approves/pays invoices, or confirms delivery. Delivered tracking rows stay `nextRefreshAt:null` during backfill so delivered history does not get re-polled.
- Live portal dry-run: `npm run agent:alibaba-portal -- --json --tracking-only --deep --recent-months=3 --dry-run` opened `https://biz.alibaba.com/order/list.htm` in the configured Chrome Work/Lambenti profile for the required orders-first handoff, then deferred controlled scraping because that Chrome profile is already open. Close Work-profile Chrome windows before running Capture for the full controlled scrape, or complete CAPTCHA/2FA/security checks manually if Alibaba presents them.
- Verification: RED tests first failed for missing `buildPortalCaptureTargets`, missing order-status sequencing, missing fast tracking-only scan behavior, and missing `/tracking` copy. Current targeted checks passed: `node --check scripts/alibaba-portal-agent.mjs`; `npm run test -- scripts/alibaba-portal-agent.test.mjs scripts/alibaba-portal-extraction.test.mjs src/modules/tracking/alibaba-capture-agent.test.ts src/modules/tracking/service.integration.test.ts src/app/tracking/page.test.ts src/app/api/integrations/integration-auth.test.ts --run --no-file-parallelism` (6 files / 40 tests). Full current-tree gate passed: `npm run lint && npm run typecheck && npm run test:serial && npm run build` (62 files / 282 tests + Next build).
- Runtime/browser smoke: killed stale `5173`, restarted `npm run start:local`, HTTP `/tracking?smoke=delivering-delivered-messages` returned 200 and contained the Delivering/Delivered/Message Center copy; browser rendered `/tracking` with `SHIP24 configured`, 2 saved rows, 1 active row, 1 delivered history row, and browser console/JS errors were 0.
- Safety/data check: live DB has tracking rows `1Z675EW60490310023` (DELIVERED, `nextRefreshAt:null`) and `888071620741` (PENDING/SUCCESS linked to PO/import); `deliveredWithNextRefreshAt:0`, `stockMovementSideEffects:0`, `invoiceSideEffects:0`. No live portal snapshots were imported during dry-run because controlled scrape was blocked by the already-open Chrome profile.

## Previous session — delivered tracking history split + ship-time display
- Request: remove delivered tracking numbers from the active tracking list while retaining their tracking information, and indicate total ship time for each delivered order.
- Implemented service split: `getTrackingDashboard()` now returns `rows` as active/non-delivered rows and `deliveredRows` as retained delivered history. Delivered rows still count in `summary.total`/`summary.delivered`, preserve linked order/import/source/latest-event metadata, and expose `shipTimeStartedAt`, `shipTimeEndedAt`, `shipTimeMs`, and `shipTimeLabel`.
- Ship-time rule: total ship time is computed from the earliest carrier event timestamp to `deliveredAt` (or delivered `lastEventAt` fallback). If no usable carrier event range exists, the delivered history shows the retained record with an unavailable-range note instead of inventing a duration.
- Implemented UI split: `/tracking` now shows `Active tracking numbers` for only open/non-delivered rows with refresh actions, plus a separate `Delivered tracking history` table with delivered date, total ship time, latest event, linked order, and source evidence. Delivered rows no longer render with `data-testid="tracking-row"` and do not show refresh actions.
- Data cleanup: cleared stale `nextRefreshAt` for delivered tracking `1Z675EW60490310023` and wrote audit action `CLEAR_DELIVERED_TRACKING_REFRESH`; underlying tracking/events/status evidence remains retained.
- Verification: RED tests first failed for delivered active-list exclusion and page copy; GREEN targeted test passed (`src/modules/tracking/service.integration.test.ts` + `src/app/tracking/page.test.ts`, 13 tests). Full gate passed: `npm run lint`, `npm run typecheck`, `npm run test:serial` (62 files / 276 tests), `npm run build`.
- Runtime/browser smoke: restarted `npm run start:local`; `/tracking?smoke=delivered-history-after-cleanup` renders `Active tracking numbers` with 1 active row (`888071620741`) and `Delivered tracking history` with 1 retained delivered row (`1Z675EW60490310023`) showing total ship time `3d 17h`; browser console/JS errors: 0.
- Safety: DB side-effect check remained `stockMovementSideEffects:0` and `invoiceSideEffects:0`; this remains shipment metadata only.

## Previous session — Alibaba tracking scrape hardening: 3-month cutoff + message-context capture
- Request: for `/tracking` Capture Alibaba, exclude tracking numbers older than 3 months, check Alibaba order/logistics/message-conversation evidence thoroughly, save important message context, never send messages, and link tracking for completed/recent orders to existing orders.
- Implemented capture hardening: the wrapper now launches `scripts/alibaba-portal-agent.mjs --json --tracking-only --deep --recent-months=3`; tracking-only still forces `autoApply:false` and `autoCreateInvoices:false` under actor `alibaba-tracking-capture-agent`.
- Implemented portal extractor hardening: read-only candidate allow/deny patterns now allow order/message/logistics evidence and reject send/reply/inquiry/chat/contact controls; deep mode scrolls page/feed/list/grid containers before marking candidates so lazy-loaded order/message evidence is considered; message snapshots store shipment-relevant `conversationContext` while filtering composer/send UI text.
- Implemented date guardrails: portal snapshot capture and saved-evidence backfill use order/evidence dates and skip numbers linked to evidence older than 3 months. Added `pruneOldAlibabaTrackingNumbers()` and wired `/tracking` Capture to remove old Alibaba portal tracking rows whose linked order evidence predates the cutoff.
- Implemented route/schema/import support: portal snapshot Zod/API/schema/import text now preserves `orderStatus`, `orderDate`, and `conversationContext` so important conversation context is durable evidence.
- Data cleanup: the old Alibaba portal row `LL270153423CN` linked to 2019 order `13569030001023166` has been removed from active tracking. Live DB now has 2 saved rows: UPS/email `1Z675EW60490310023` and recent Alibaba portal `888071620741`; `oldLinkedCount` query returned 0.
- Browser/runtime smoke: rebuilt and restarted `npm run start:local`; `/tracking?smoke=final` returns 200 and browser renders `SHIP24 configured`, 2 saved rows, `LL270153423CN` absent, `888071620741` present, delivered UPS `Next: —`, and 0 console/JS errors. Pressing Capture after restart completed without UI error; direct portal dry-run showed the saved-login assist clicked both `Continue as Musashi` and Alibaba trusted-account `Yes`, but controlled scraping deferred because the Work Chrome profile was already open.
- Verification: targeted tests passed for 3-month cutoff, old-row pruning, recent completed-order message-context linking, API schema preservation, read-only no-send filters, deep lazy-load scroll source contract, page copy, and wrapper args. Full current-tree gate passed after final edits: targeted tracking service test, `npm run lint`, `npm run typecheck`, `npm run test:serial` (62 files / 276 tests), and `npm run build`.
- Safety: DB side-effect query returned `stockMovementSideEffects:0` and `invoiceSideEffects:0`; tracking capture remains metadata/evidence only and does not send Alibaba messages, receive stock, approve/pay invoices, or confirm delivery.

## Previous session — tracking provider research/setup
- Request: research the best tracking provider, determine the safest setup path, and implement it in the Lambenti tracking app.
- Provider decision: Ship24 is now the default/recommended provider for Lambenti because current official Ship24 docs/pricing showed global parcel API/webhook tracking, China Post and UPS coverage, courier auto-detection, OpenAPI docs, bearer API keys, a free low-volume path, and a $39/mo 1,000-shipment Pro tier if Lambenti outgrows free usage. TrackingMore, EasyPost, 17TRACK, and AfterShip were reviewed as alternatives; TrackingMore is strong but higher minimum API cost for API/webhook use, EasyPost is cheaper per shipment but less Alibaba/China-origin focused, 17TRACK had less directly inspectable setup/pricing here, and AfterShip's current site was blocked by Cloudflare in this environment.
- Implemented Ship24 adapter: `refreshTrackingNumber()` now supports `LAMBENTI_TRACKING_STATUS_PROVIDER=SHIP24`, sends `POST /public/v1/trackers/track` to `LAMBENTI_TRACKING_SHIP24_BASE_URL`, uses `TrackingNumber.id` as Ship24 `clientTrackerId`, sends Alibaba order/PO/import metadata as `shipmentReference`, defaults destination country to `CA`, preserves custom-HTTP fallback, normalizes Ship24 shipment/events into `TrackingNumber`/`TrackingEvent`, and keeps delivery as metadata only.
- Implemented setup/docs/UI: `.env.example` and ignored `.env` now include non-secret Ship24 defaults (`provider`, base URL, destination `CA`, 240-minute interval) with blank `LAMBENTI_TRACKING_STATUS_AUTH_TOKEN`; `/tracking` service card now explains the Ship24 token setup; `docs/local-runbook.md` and `docs/tracking-provider-research.md` document the provider decision, setup steps, scheduler command, and fallback custom-HTTP path.
- Verification: added RED/GREEN coverage for the Ship24 provider adapter, scheduler provider detection, dashboard service metadata, and `/tracking` setup copy. `node --check scripts/tracking-refresh.mjs`, targeted tracking/scheduler/page tests, `npm run typecheck`, and full `npm run quality` passed; final full gate was lint + typecheck + serial Vitest 60 files / 263 tests + Next production build listing `/tracking` and `/api/tracking/refresh`.
- Runtime smoke: `npm run agent:tracking-refresh -- --json` skips with `configured:false` and asks for `LAMBENTI_TRACKING_STATUS_AUTH_TOKEN` because no real Ship24 API key is installed. After killing a stale `5173` listener and starting the rebuilt app, `/tracking?smoke=ship24` returned HTTP 200 and rendered `SHIP24 not configured`, Ship24 setup copy, 3 saved tracking rows, and no browser console/JS errors. Direct loopback `POST /api/tracking/refresh` returned HTTP 207 with 3 config-required rows because the manual API call intentionally attempted refresh without a token; the quiet scheduler avoids that until configured.

## Previous session — tracking workbench integration
- Request: recover the halted tracking-number work and integrate it into the Lambenti tracking app: automatically save tracking numbers, link them to the appropriate order/import, and provide a page that refreshes each tracking number through a tracking-service connection with as much autonomy as is safe.
- Recovery source: session search showed the prior run had scanned 13 Alibaba orders and persisted evidence for `888071620741`, `LL270153423CN`, and UPS email tracking `1Z675EW60490310023`; current repo already contained a partial unverified `TrackingNumber`/`TrackingEvent` migration, tracking service module, `/tracking` page, and `/api/tracking/refresh` route.
- Implemented tracking persistence: migration `20260614000000_tracking_numbers` adds `TrackingNumber` and `TrackingEvent`; `captureTrackingNumbersFromPortalSnapshot()` upserts numbers idempotently, links by `externalOrderId`/`EmailOrderImport.purchaseOrderId`, writes `CAPTURE_TRACKING_NUMBER` audit logs, and does not create stock movements or receiving.
- Integrated import automation: Alibaba portal imports now always run tracking capture, even when numbers are present only in snapshot text; email/import backfill scans active and archived tracking/logistics evidence, preserves source (`ALIBABA_PORTAL`, `SYNCED_EMAIL`, etc.), filters false positives such as Alibaba order IDs/phone-like numbers, and excludes the source order ID from tracking rows.
- Implemented tracking refresh: `refreshTrackingNumber()` and `refreshDueTrackingNumbers()` call a configurable JSON tracking service via `LAMBENTI_TRACKING_STATUS_URL_TEMPLATE`, persist normalized status/events, stop polling delivered shipments, and mark rows `CONFIG_REQUIRED` when no tracking-service URL is configured. `/api/tracking/refresh` supports local loopback auto-refresh through `start:local` and bearer-secret agent refresh for non-local callers. Added `npm run agent:tracking-refresh` as a quiet scheduler hook for Windows Task Scheduler/Hermes cron/other schedulers; it stays silent and skips the API until a provider URL is configured.
- Implemented `/tracking`: sidebar link, service connection card, saved/due/delivered/config/failed metrics, Alibaba capture action, due refresh action, per-row refresh, linked order/import/source/evidence columns, and client-side automatic polling while the page is open when a tracking-service URL is configured. Workspace revalidation now includes `/tracking`.
- Data backfill/repair: applied the tracking migration and generated Prisma Client; backfilled current evidence into tracking rows for `888071620741` (linked to Alibaba order `304716450001023166` and PO `cmq60abrf000llh40c01xxprm`), `LL270153423CN` (linked to Alibaba import/order `13569030001023166`), and `1Z675EW60490310023` (linked to archived UPS synced-email evidence). Removed four false-positive numeric rows created during the first backfill attempt and reset live rows to evidence-only `UNKNOWN`/`PENDING` or `CONFIG_REQUIRED` when refresh was smoked without a service URL.
- Tooling hardening: ESLint and Vitest now ignore gitignored `var/**` runtime/browser-profile artifacts so Alibaba Chrome-session captures do not poison lint/test discovery. Added `.env.example` tracking-service env placeholders and `docs/local-runbook.md` scheduler instructions.

## Verification snapshot — tracking workbench integration
  - RED tests observed: portal import text-only tracking capture initially did not call tracking capture; context extraction initially treated Alibaba order IDs/phone/product numbers as tracking; archived email evidence was skipped; source preservation for synced email initially stored `ALIBABA_PORTAL`; recovering this session added a RED regression for preserving existing manual tracking provenance before linking order metadata.
  - Schema/client: `npx prisma migrate deploy` reported no pending migrations; `npx prisma generate` initially hit a locked Windows Prisma query-engine DLL, then succeeded after killing stale local `5173` PID `31240`.
  - Targeted tracking/scheduler checks passed during recovery: `node --check scripts/tracking-refresh.mjs`; tracking scheduler helper tests; tracking service/API/page/import tests. The final full suite below includes the same coverage.
  - Full current-tree gate passed after final edits: `npm run quality` (`lint`, `typecheck`, serial Vitest 60 files / 262 tests, and production build listing `/tracking` and `/api/tracking/refresh`).
  - Runtime smoke: fresh `npm run start:local` served `/tracking` with HTTP 200. `POST /api/tracking/refresh` on loopback authorized successfully and returned HTTP 207 with `{scanned:3, refreshed:0, failed:3, skipped:0}` because no `LAMBENTI_TRACKING_STATUS_URL_TEMPLATE` is configured yet. `npm run agent:tracking-refresh -- --json` now returns a skipped diagnostic instead of calling the refresh API when no provider URL is set. Browser rendered `/tracking?smoke=final` with 3 saved numbers, the sidebar Tracking link, CONFIG_REQUIRED refresh messaging, and 0 console messages/JS errors. The local `5173` listener was killed after smoke.
  - Safety: no stock receiving, purchase approval, payment, invoice approval, or Alibaba ordering side effects were added; tracking capture/refresh is metadata/evidence only.

## Previous session — Alibaba portal navigation/tracking hardening
- Request: continue Alibaba portal scrape/browser-agent work so it can log in through the existing Alibaba session, read messages/orders, capture tracking numbers, and navigate autonomously where safe. The explicit CAPTCHA-bypass request was handled as a safety constraint: do not bypass Alibaba CAPTCHA/slider/security/2FA checks; detect them and hand off to the operator in headed Chrome.
- Implemented `scripts/alibaba-portal-extraction.mjs` with tested pure helpers for read-only navigation candidate filtering, relevant evidence detection, conservative order/supplier extraction, tracking-number extraction from English/Chinese tracking/logistics/waybill text, and stable message-thread IDs when Alibaba message pages do not expose order IDs.
- Hardened `scripts/alibaba-portal-agent.mjs`: read-only candidate selection now shares the tested allow/deny patterns, excludes side-effect controls such as pay/confirm receipt/release payment/send/delete/refund/dispute/logout, captures tracking numbers into snapshots, creates distinct hash-backed message IDs for message threads without order IDs, and propagates login/security challenges encountered during detail/invoice navigation instead of silently swallowing them.
- Hardened auth and import boundaries: `detectAlibabaAuthState()` now catches broader SMS/multi-factor/account-protection/risk-control/Chinese slider/security wording, and the Alibaba portal import route preserves `trackingNumbers` through Zod request validation.
- About:blank/login repair after operator feedback: added `scripts/alibaba-portal-browser.mjs` helper coverage, sanitized blank/about:blank portal URL env values back to the Alibaba defaults, made setup-login open a fresh controlled tab and print the actual opened URL/manual paste URL, and added a fallback that opens the target Alibaba URL in the already-running Work Chrome profile when Playwright cannot control the profile because it is already open.
- Saved Google/login confirmation assist after operator screenshots: added `scripts/alibaba-portal-login-assist.mjs` with tested `Continue as Musashi` matching, trusted-account `Continue as team@lambenti.com` confirmation matching, and Windows UIAutomation fallback for Chrome/FedCM/browser UI. The setup/login flow now waits for/clicks saved Google `Continue as Musashi` when it appears, clicks Alibaba's `Continue with Google` provider button to trigger it, clicks Alibaba's `Yes` confirmation only when the page matches the trusted Lambenti account, and in controlled headed setup can prompt for one-time Alibaba email/password without storing/logging credentials. CAPTCHA/2FA/security checks still stop for manual completion.
- Live tracking check after Alibaba access: because Chrome/Playwright now refused DevTools control of the default Work profile (`DevTools remote debugging requires a non-default data directory`), I opened a normal headed Work-profile Chrome session and used read-only Windows UIAutomation against Alibaba's accessibility tree. The scan covered 13 Trade Assurance orders, clicked safe `Track shipment(s)`/tracking surfaces only, and found portal tracking numbers for order `304716450001023166` (`888071620741`) and old order `13569030001023166` (`LL270153423CN`). A mailbox tracking scan also found UPS tracking `1Z675EW60490310023` in the synced import-fees email.
- Tracking persistence: posted the two portal tracking snapshots through `/api/integrations/alibaba-portal/import` with `autoApply:false`/`autoCreateInvoices:false` (result HTTP 200, imported 1 old portal snapshot, duplicate/refreshed 1 existing order, 0 invoices); created `CAPTURE_ALIBABA_TRACKING_NUMBERS` audit logs for all three found tracking numbers; appended `888071620741` to existing import evidence `cmq5ywx0e000klhuogoa4vbqk` because that applied order's raw import text did not otherwise contain the portal tracking header. Verification query confirmed the three tracking numbers are present in `EmailOrderImport.rawText` or audit-backed import evidence. Raw local evidence files: `var/alibaba-uia-tracking-scan-latest.json` and `var/alibaba-tracking-capture-latest.json`.

## Verification snapshot
- RED verified: new `scripts/alibaba-portal-extraction.test.mjs` initially failed because `./alibaba-portal-extraction.mjs` did not exist; the new portal route tracking-provenance test initially failed because Zod stripped `trackingNumbers` from snapshots.
- Targeted checks passed: `node --check scripts/alibaba-portal-agent.mjs`; `node --check scripts/alibaba-portal-auth.mjs`; `node --check scripts/alibaba-portal-extraction.mjs`; `node --check scripts/alibaba-portal-browser.mjs`; `npm run test -- scripts/alibaba-portal-browser.test.mjs scripts/alibaba-portal-auth.test.mjs scripts/alibaba-portal-extraction.test.mjs src/modules/alibaba-portal/snapshot.test.ts src/app/api/integrations/integration-auth.test.ts --run --no-file-parallelism` (5 files / 24 tests after the about:blank repair).
- Full gates passed after the saved-Google/account-confirm login-assist repair: `npm run lint`; `npm run typecheck`; `npm run test:serial` (55 files / 245 tests; expected/user-facing Prisma error logs were printed by passing integration tests for duplicate/deadlock retry paths); `npm run build` (Next production build succeeded and listed `/api/integrations/alibaba-portal/import`). Targeted Alibaba/API checks also passed: `node --check scripts/alibaba-portal-agent.mjs`; `node --check scripts/alibaba-portal-browser.mjs`; `node --check scripts/alibaba-portal-login-assist.mjs`; `npm run test -- scripts/alibaba-portal-login-assist.test.mjs scripts/alibaba-portal-browser.test.mjs scripts/alibaba-portal-auth.test.mjs scripts/alibaba-portal-extraction.test.mjs src/modules/alibaba-portal/snapshot.test.ts src/app/api/integrations/integration-auth.test.ts --run --no-file-parallelism` (6 files / 29 tests).
- Portal profile/login/tracking smoke: `npm run agent:alibaba-portal -- --profile-info --json` reports Chrome Work profile `lambenti.com · Profile 1` at `C:\\Users\\musas\\AppData\\Local\\Google\\Chrome\\User Data`; direct `npm run agent:alibaba-portal -- --json` currently cannot DevTools-control that default Chrome user-data dir because Chrome returns `DevTools remote debugging requires a non-default data directory`; the live tracking run therefore used normal headed Chrome + Windows UIAutomation, scanned 13 Alibaba orders, and persisted found tracking evidence (`888071620741`, `LL270153423CN`, plus UPS email `1Z675EW60490310023`). `printf '\\n' | npm run agent:alibaba-login -- --verbose --no-prompt-credentials` opens/targets `https://biz.alibaba.com/order/list.htm`, waits for saved `Continue as Musashi`, and then checks trusted-account `Yes` confirmation. Public HTTP probing confirmed `https://www.alibaba.com/trade/order/list.htm` redirects to Alibaba's 404 page while `https://biz.alibaba.com/order/list.htm` redirects to Alibaba login with a buyer order-list return URL.
- Safety check: `git diff --check` passed with only pre-existing line-ending warnings; no physical stock receiving, purchase approval, payment, invoice approval, or Alibaba ordering side effect was added.

## Previous session — LED strip supplier persistence repair
- Request: diagnose why LED strip items were showing no set supplier again after manual supplier assignment, fix all root causes, and test.
- Root cause confirmed with DB/runtime evidence: both LED strip items had `preferredSupplierId = null` while their latest `UPDATE_ITEM_SUPPLIER_ENTRY` audit logs showed the operator-saved supplier `jason zhou` (`cmq2p9yrk0016lhbwu4uzswa3`). The overwrite path was Alibaba email order apply/reassessment: it updated matched `Item` cost metadata and forced `preferredSupplierId` to the email import / PO supplier. Existing integration tests used the real LED seed SKUs with temporary `TEST-EMAIL-ARCHIVE-*` suppliers; cleanup deleted those suppliers, leaving the real LED items with no supplier.
- Implemented fix in `src/modules/email-imports/alibaba-email.ts`: applying a matched email order now preserves an item's existing `preferredSupplierId` and only fills from the order supplier when the item has no preferred supplier; the same preservation is applied during reassessment of already-applied email imports.
- Hardened `src/modules/email-imports/alibaba-email.archive.integration.test.ts`: added regression tests for manual supplier preservation during auto-apply and reassessment, snapshots/restores protected real LED seed item supplier/cost metadata around the archive test file, and cleans test-created items/locations so fixture cleanup no longer corrupts production seed rows.
- Data repair: restored `LED-COB-12V-3000K` and `LED-COB-12V-6500K` preferred supplier to audited supplier `jason zhou`, replaced stale `TEST-EMAIL-ARCHIVE-*` cost provenance with real applied order `300174506001023166`, and wrote `RESTORE_ITEM_PREFERRED_SUPPLIER` / `RESTORE_ITEM_COST_PROVENANCE` audit logs; no stock movements, receiving, invoice approval, or payment state was changed.

## Verification snapshot
- RED reproduced: targeted regression initially failed because applying an email order from another supplier changed a manually selected preferred supplier to the email supplier.
- Targeted regression passed after fix: `npm run test -- src/modules/email-imports/alibaba-email.archive.integration.test.ts --run --no-file-parallelism -t "preserves a manually selected preferred supplier"` (2 preservation tests passed).
- Full email archive suite passed after repair: `npm run test -- src/modules/email-imports/alibaba-email.archive.integration.test.ts --run --no-file-parallelism` (14 tests passed).
- Quality gates passed from current tree: `npm run lint`; `npm run typecheck`; `npm run test:serial` (52 files / 229 tests, run both before and after data repair); `npm run build`.
- Runtime/browser verification: existing local app was already bound on `127.0.0.1:5173` (`npm run start:local` returned EADDRINUSE), so browser smoke used the running app. `/inventory/items` rendered, and DOM inspection of the LED edit dialogs showed both `LED-COB-12V-3000K` and `LED-COB-12V-6500K` preferred supplier dropdowns selected `jason zhou` (`cmq2p9yrk0016lhbwu4uzswa3`). Post-suite DB query confirmed both rows still point to `jason zhou` and reference real applied order `300174506001023166` for cost provenance.

## Previous session — accounting workbench usability/effectiveness pass
- Request: research best ways to make the accounting section more usable/effective and implement changes.
- Research result: with web_search unavailable, live research used reachable NN/g UX sources for progressive disclosure, recognition over recall, and visibility of system status, plus current repo/accounting checkpoints. The actionable gap was not new accounting mutation logic; it was operator scanability: the page had strong capabilities but forced the user to infer priorities from feature cards and a raw document table.
- Central Lambenti Agent Control Plane was tried for read-only accounting UX audit at `C:\\LambentiAI\\lambenti-agent\\runs\\run_20260613_171813`; it routed as high-risk accounting work but returned generic/hallucinated file names, so implementation decisions were based on direct repo/browser evidence instead.
- Implemented `/accounting` Attention queue: surfaces nonzero next actions before the feature grid, including clean documents ready to apply, documents needing manual review, received invoices waiting approval, approved invoices ready for payment reconciliation, and incoming POs missing invoice evidence.
- Implemented source-document triage metrics and per-row readiness badges: Ready to apply, Needs manual review, Attach only, Linked evidence, Unreadable/no text, Applied to AP, Attached evidence, Evidence only, and Saved evidence. These preserve the AP/stock boundary and make OCR/manual review blockers visible.
- Added a concise Accounting control trail: `Upload → review → apply/attach → approve/pay → post/export`, reinforcing that AP creation, approval, payment, posting, and physical receiving remain separate human-gated steps.
- Dark-mode browser smoke exposed a CSS coverage gap for new blue/100/900 tone classes; patched `src/app/globals.css` dark-theme overrides so accounting triage cards/status badges remain readable.

## Verification snapshot
- RED verified: `npm run test -- src/app/accounting/page.test.ts --run --no-file-parallelism` failed before implementation because `Attention queue` was missing.
- Targeted tests passed after implementation: `npm run test -- src/app/accounting/page.test.ts src/modules/accounting/documents.integration.test.ts src/app/accounting/invoices/page.test.ts --run --no-file-parallelism` (3 files / 13 tests).
- Full gate passed from current tree after CSS fix: `npm run quality` passed (`lint`, `typecheck`, serial Vitest 52 files / 227 tests, and production build).
- Runtime smoke: restarted a fresh `npm run start:local` after killing stale PID 28644; HTTP `/accounting`, `/accounting/invoices`, and `/accounting/payments` returned 200.
- Browser smoke: `/accounting?smoke=triage2` rendered Attention queue, Source document triage, control trail, feature cards, review queue, and uninvoiced PO queue; browser console had 0 messages/errors; CSS asset loaded with 322 rules and dark body background `rgb(2, 6, 23)`; screenshot review confirmed readable dark-mode triage/status UI.

## Previous session — approved purchase request to draft PO conversion
- Request: research next safe/high-value Lambenti inventory-app step and implement it end-to-end.
- Research result: the backlog item `Approved purchase request to draft PO conversion` was the highest-value safe continuation because purchase approvals existed but approved requests had no human-controlled path into draft purchase orders.
- Implemented `convertApprovedPurchaseRequestToDraftPurchaseOrder()` in `src/modules/purchasing/service.ts`: only actors with `purchaseOrder:create` can convert; source request must be `APPROVED`, have a supplier, at least one line, and line unit-price evidence from `targetUnitPrice` or item `estimatedUnitCost`; conversion creates a `DRAFT` PO, marks request `CONVERTED`, writes audit log `CONVERT_PURCHASE_REQUEST_TO_DRAFT_PO`, and does not receive stock.
- Implemented server action/UI on `/purchasing/requests`: approved requests now show unit-price evidence and a `Create draft PO` action; converted requests show the linked draft PO. Existing approve/reject flow is unchanged.
- Added integration coverage for service and server-action authorization/state machine: agent cannot convert; purchasing actor can convert; missing supplier/price blocks; no stock movement is created.
- Local database note: the existing `lambenti-postgres` container was healthy but not publishing host port 5432. Docker refused 5432 as unavailable, so `docker-compose.yml` now defaults `LAMBENTI_DB_HOST_PORT` to `55432`, `.env.example` and the ignored `.env` `DATABASE_URL` point to `localhost:55432`, the compose-managed `db` service was recreated against the same named volume with `55432:5432`, and docs now include `docs/local-runbook.md`.

## Verification snapshot
- Targeted tests: `npm run test -- src/modules/purchasing/service.integration.test.ts src/app/purchasing/requests/actions.integration.test.ts --run --no-file-parallelism` passed (2 files / 11 tests).
- Full gates after the docker-compose fix: `npm run lint && npm run typecheck && npm run test:serial && npm run build` passed; serial Vitest reported 52 files / 227 tests and Next production build succeeded.
- Compose/runtime DB: `docker compose create db && docker compose start db` recreated `lambenti-postgres` as the compose `db` service; `docker compose ps` shows healthy `0.0.0.0:55432->5432/tcp`; socket check to `127.0.0.1:55432` passed; `docker compose config --quiet` passed; `.env.example` and `.env` DB host-port metadata both align on `55432`.
- Runtime smoke: `npm run start:local` served `/`, `/purchasing/requests`, and `/incoming` with HTTP 200; browser rendered `/purchasing/requests` and browser console had 0 messages/errors; local `5173` server was stopped after smoke.
- Browser mutation smoke: created temporary `TEST-UI-PR-CONVERT-*` approved request, browser rendered `Create draft PO`, clicked it, UI changed to `CONVERTED` with `Draft PO linked`, DB verified `{requestStatus:CONVERTED,purchaseOrderStatus:DRAFT,purchaseOrderLineCount:1,stockMovementCount:0}`, then cleanup removed 1 test request/order/item/supplier.

## Previous session — two-tier Hermes/local-Qwen orchestration default
- Request: organize the two-tier setup so Hermes remains controller and delegates low-effort Lambenti inventory-app work to local Qwen by default, with GPT-5.5 as senior model and tuned parameters for efficiency/capability.
- Researched active Hermes docs/source: `delegation.base_url/api_key/api_mode/model` supports direct OpenAI-compatible subagent endpoints; SOUL.md is stable profile identity; `.hermes.md`/`AGENTS.md` are project context; subagents are self-reports that Hermes must verify.
- Updated active Lambenti Hermes profile: migrated config to version 29, set `delegation.model=qwen2.5-coder-7b-instruct`, `base_url=http://localhost:1234/v1`, `api_key=lm-studio`, `api_mode=chat_completions`, `reasoning_effort=none`, `max_iterations=40`, `child_timeout_seconds=900`, `max_concurrent_children=2`, `max_spawn_depth=1`, and kept `subagent_auto_approve=false`.
- Reloaded LM Studio Qwen worker from 8,192 context to 32,768 context with `parallel=2` using `lms unload qwen2.5-coder-7b-instruct` then `lms load qwen2.5-coder-7b-instruct --context-length 32768 --parallel 2 --identifier qwen2.5-coder-7b-instruct -y`.
- Updated Lambenti profile `SOUL.md` with the default controller/worker policy: Qwen drafts, GPT-5.5 reviews, Hermes executes tools, tests decide, high-risk effects require user approval.
- Added repo project context `.hermes.md`, expanded `AGENTS.md` Preferred Hermes workflow, and added `docs/hermes-two-tier-orchestration.md` plus `scripts/hermes-local-worker-smoke.mjs` / `npm run agent:local-worker-smoke`.
- Tuned `C:/Users/musas/hermes-llm-orchestrator`: local output/time/top-p parameters, Lambenti accounting/ledger high-risk routing terms, and local-worker prompt guardrails for inventory/accounting boundaries.
- Updated centralized control plane `C:/LambentiAI/lambenti-agent` to default LiteLLM local calls to LM Studio (`openai/qwen2.5-coder-7b-instruct`) instead of Ollama, updated `C:/LambentiAI/lambenti-agent.env`, and documented the runner in README.

## Verification snapshot
- Hermes config: `hermes config check` reports `Config version: 29 ✓`; fresh config resolver maps delegation to `{model:qwen2.5-coder-7b-instruct, provider:custom, base_url:http://localhost:1234/v1, api_mode:chat_completions}`.
- LM Studio: `/v1/models` lists `qwen2.5-coder-7b-instruct`; `lms ps` shows context `32768` and `PARALLEL 2`.
- Inventory smoke: `npm run agent:local-worker-smoke` returned JSON `ok:true` and sample `HERMES_LOCAL_WORKER_OK`.
- Inventory checks: `node --check scripts/hermes-local-worker-smoke.mjs` passed; `npm run lint` passed.
- Standalone orchestrator: `py_compile` passed; `pytest tests -q` passed (`8 passed`); local Qwen smoke returned the required structured response.
- Central control plane: `uv run python -m py_compile lambenti_agent.py` passed; `uv run python lambenti_agent.py ... --no-frontier --run-git-status` created `C:\\LambentiAI\\lambenti-agent\\runs\\run_20260613_154641` and returned local analysis.
- Current GUI-session caveat: this already-running Hermes session's `delegate_task` tool kept its startup delegation cache and reported a one-off smoke child as model `gpt-5.5`; new Hermes sessions/gateway restarts load the updated profile config. Do not rely on this current session's delegate default until restarted.

## Previous session — inventory/accounting dashboard, purchasing, supplier UI, Alibaba portal
- Request: fix item edit preferred-supplier dropdown persistence, add accounting source-document deletion, remove stale dashboard email catalog-matching notice for order `303671327001023166`, stop suggesting finished goods for purchase/reorder, simplify archive-supplier UI, and improve Alibaba portal automation using the existing Work Chrome profile with CAPTCHA-safe handoff.
- Implemented item supplier persistence: create/edit item forms now preserve dropdown `preferredSupplierId` through the server action path, active supplier options remain aligned with `/suppliers`, and custom supplier creation still creates audited supplier records without stock changes.
- Implemented accounting document deletion: `/accounting` source-document rows expose a guarded delete action that removes the saved file plus `AccountingDocument` analysis row while preserving accounting/stock boundaries; tests cover deletion and source-contract UI.
- Removed the unnecessary dashboard email catalog-matching nag function. Email import/manual-matching review stays on Order Agent/accounting/invoice workflows; dashboard human review now focuses on purchase recommendations, invoice actions, automation findings, and failures.
- Fixed purchase/reorder suggestions: `getPurchaseRecommendations()` and purchase-request creation reject `FINISHED_GOOD` items; automation reorder scans skip finished goods because Lambenti finished goods/subassemblies are built internally, not purchased.
- Simplified supplier archive control: removed amber/yellow outer box and warning wrapper; archive is now minimalist collapsed text with the reason field/action inside.
- Improved Alibaba portal agent: auto-selects the existing Work/Lambenti Chrome profile (`Profile 1`/`lambenti.com` via explicit `.env` or Chrome Local State), added `--profile-info`, clearer profile-in-use diagnostics, broader security/slider-CAPTCHA detection including Chinese slider text, tracking/logistics link/text capture, tracking-number provenance in portal snapshot imports, and docs explaining manual CAPTCHA/2FA handoff rather than bypass.
- Alibaba CAPTCHA/security decision: do not bypass Alibaba slide CAPTCHA or security checks. The app detects them, launches headed Chrome with the signed-in Work profile for human completion, then resumes reading messages/orders/tracking after the session is valid. Direct automated ordering remains future work and must keep human confirmation/payment boundaries until explicitly approved.

## Verification snapshot
- Targeted tests passed: `npm run test -- src/modules/dashboard/service.integration.test.ts src/modules/purchasing/service.integration.test.ts src/modules/automation/service.integration.test.ts src/app/suppliers/page.test.ts src/modules/suppliers/service.integration.test.ts src/modules/accounting/documents.integration.test.ts src/app/accounting/page.test.ts src/app/inventory/items/actions.integration.test.ts src/app/inventory/items/page.test.ts src/app/inventory/items/items-catalog.test.ts --run --no-file-parallelism` (10 files / 54 tests).
- Alibaba portal tests/checks passed: `node --check scripts/alibaba-portal-agent.mjs`; `node --check scripts/alibaba-portal-auth.mjs`; `npm run test -- scripts/alibaba-portal-auth.test.mjs src/modules/alibaba-portal/snapshot.test.ts --run --no-file-parallelism` (2 files / 7 tests).
- Full gates passed: `npm run lint && npm run typecheck && npm run test:serial && npm run build` (serial Vitest 52 files / 223 tests; Next build succeeded and listed `/accounting`, `/purchasing/recommendations`, `/integrations/alibaba-email`, `/automation`, and Alibaba portal import API).
- Runtime smoke: killed stale `127.0.0.1:5173` owner PID 35544, started `npm run start:local`, and HTTP-smoked `/`, `/inventory/items`, `/accounting`, `/suppliers`, `/purchasing/recommendations`, `/integrations/alibaba-email`, `/automation` with 200 responses.
- Browser smoke: dashboard no longer contains `303671327001023166` or `unmatched/uncertain line(s) need catalog matching`; `/purchasing/recommendations` lists only component housing recommendations and no `LAMBENTI_PACKAGE`/`LAMBENTI_MAIN_UNIT`/`LED_CONN`; `/accounting` shows `Delete source document` plus permanent-delete warning; `/suppliers` archive control has no amber/yellow archive classes; browser console checks showed 0 JS errors/messages.
- Alibaba profile smoke: `npm run agent:alibaba-portal -- --profile-info --json` reports Chrome Work profile `lambenti.com · Profile 1` under `C:\\Users\\musas\\AppData\\Local\\Google\\Chrome\\User Data`. `--dry-run --json` correctly reports the profile is already open in an existing browser session instead of failing opaquely.
- Safety checks: `git diff --check` passed with only CRLF normalization warnings; redacted secret-pattern scan over tracked diffs reported `secret_scan_findings=none`.

## Previous session — posted accounting journals
- Request: research the next accounting steps and implement the next highest-value feature with usable/polished operator flows.
- Research result: repo checkpoints plus three read-only audits identified true balanced journal-entry posting as the remaining highest-value accounting gap. Payment-page polish also needed because received invoices were shown as reconcilable even though the action rejected them.
- Implemented schema migration `20260609012000_accounting_journal_entries`: `JournalEntryStatus`, `JournalEntryKind`, immutable source-linked `JournalEntry`, `JournalEntryLine`, account snapshots, idempotency keys, AP invoice/payment links, balance checks, and indexes.
- Implemented posting services in `src/modules/accounting/journals.ts`: AP invoice approval posts debit invoice lines/recoverable tax/landed extras and credit accounts payable; payment reconciliation posts debit AP and credit bank/cash; posting is idempotent and rolls back if required mappings are missing or an entry is unbalanced.
- Integrated journals into supplier invoice approval/payment paths: approving a received supplier invoice posts the AP invoice journal in the same transaction; direct invoice-ledger paid marking creates a manual payment allocation and AP payment journal; bank imports remain non-posting evidence until explicit reconciliation.
- Polished operator UI: added `/accounting/journals` with trial balance, entry ledger, setup guidance, and CSV download; added `/api/accounting/exports/journals`; linked posted journals from `/accounting`, `/accounting/invoices`, and `/accounting/payments`; updated GL mapping page with required default purposes; payment reconciliation now separates approved reconcilable invoices from received invoices waiting for approval and hides mutation controls from view-only roles.
- Guardrails preserved: journal posting does not create stock movements, stock lots, received quantities, source-document applies, bank imports, or PO receiving; missing GL mappings block approval/reconciliation with clear setup errors instead of posting bad ledger entries.

## Verification snapshot
- RED verified: initial targeted run failed for missing `./journals`, missing `/accounting/journals/page.tsx`, missing `/accounting` journal link, and missing `approvedInvoices`/`receivedInvoices` dashboard split.
- Schema/runtime: `npx prisma migrate deploy` applied `20260609012000_accounting_journal_entries`; first `npx prisma generate` hit a locked Windows query-engine DLL, then succeeded after killing stale port `5173` owner PID 32484.
- Targeted tests passed: `npm run test -- src/modules/accounting/journals.integration.test.ts src/modules/accounting/payments.integration.test.ts src/app/accounting/page.test.ts src/app/accounting/journals/page.test.ts src/app/accounting/invoices/actions.integration.test.ts src/app/accounting/invoices/page.test.ts --run --no-file-parallelism` (6 files / 15 tests).
- Gates passed: `npm run typecheck`; `npm run lint`; `npm run test:serial` (52 files / 216 tests); `npm run build` (build lists `/accounting/journals` and `/api/accounting/exports/journals`).
- Runtime smoke: `npm run start:local` served `/accounting`, `/accounting/journals`, `/accounting/invoices`, `/accounting/payments`, `/accounting/accounts`, and `/api/accounting/exports/journals` with HTTP 200. Browser rendered `/accounting/journals`, `/accounting`, and `/accounting/payments` with expected journal/payment UI and 0 console JS errors. Journal CSV returned `content-type: text/csv`, `cache-control: private, no-store`, and header `entryNumber,entryDate,kind,status,lineNo,accountCode,accountName,debit,credit,sourceReference,memo`.
- Server cleanup: killed local `5173` listener after smoke; only transient TIME_WAIT/CLOSE_WAIT connections remained.

## Previous session — full automatic accounting expansion
- Request: implement all proposed automatic-accounting features from the previous backlog.
- Implemented schema migrations `20260609001000_accounting_expansion` and `20260609004000_customer_invoices`: supplier-scoped invoice identity (`invoiceNumberKey` + `@@unique([supplierId, invoiceNumberKey])`), non-unique PO invoice links for deposit/partial/final invoices, payment reconciliation tables, supplier/customer tax fields, customer/AR invoice tables, GST/HST support fields, GL accounts/mappings, invoice-line PO/GL links, and landed-cost component fields.
- Implemented invoice behavior: `createInvoiceFromPurchaseOrder()` now supports multiple distinct invoices per PO when an explicit invoice number/source is supplied, dedupes same-supplier invoice numbers case/spacing-insensitively, permits the same invoice number across different suppliers, and still avoids stock/receiving side effects.
- Implemented payment reconciliation: `/accounting/payments` imports bank transactions idempotently by source hash and explicitly allocates them to approved supplier invoices; only full allocation marks invoices `PAID`, over-allocation is blocked, and payment receipts from document OCR remain evidence-only.
- Implemented customer invoices / AR: `/accounting/customer-invoices` can draft customer invoices with item-linked lines, mark them sent, and mark sent invoices paid only with payment reference; AR invoice creation/status changes do not consume stock, reserve inventory, or ship products.
- Implemented accounting outputs: `/accounting/exports` plus CSV API for GST/HST support rows; `/accounting/accounts` for lightweight GL account/mapping metadata; `/accounting/landed-cost` plus CSV API for read-only freight/duty/brokerage/non-recoverable-tax allocation while excluding recoverable GST/HST from inventory landed cost.
- Implemented UI links and invoice-ledger support: `/accounting` now links to customer invoices, payments, exports, accounts, and landed-cost reports; `/accounting/invoices` shows existing invoice counts per PO, accepts optional invoice numbers/totals for staged invoices, and shows payment allocation totals.

## Verification snapshot
- RED verified: new accounting integration tests initially failed for missing modules/functions/schema and current global invoice-number / one-invoice-per-PO constraints.
- Schema/runtime: `npx prisma migrate deploy` applied `20260609001000_accounting_expansion` and `20260609004000_customer_invoices`; first `npx prisma generate` failed with a locked Windows query-engine DLL from stale local Node process, then succeeded after killing the `5173` owner.
- Targeted accounting tests passed before full gates: supplier invoice identity/multi-PO invoices, payment reconciliation, GST/HST/GL/landed-cost reports, customer invoices / AR, accounting document guards, and accounting page/action source contracts.
- Gates passed: `npm run lint && npm run typecheck && npm run test:serial && npm run build` (50 files / 211 tests; build lists `/accounting/customer-invoices` plus all accounting expansion routes).
- Runtime smoke: `npm run start:local` served `/accounting`, `/accounting/customer-invoices`, `/accounting/payments`, `/accounting/exports`, `/accounting/accounts`, `/accounting/landed-cost` with HTTP 200; browser rendered `/accounting/customer-invoices` with AR heading, create form, and no-stock guard, then rendered `/accounting` with the AR link/upgraded accounting feature list. Browser console had 0 messages/errors. CSV APIs returned expected headers for GST/HST and landed-cost downloads.

## Previous session — automatic accounting evidence bundles
- Request: research the next steps for the automatic accounting function and implement the next safe improvement.
- Research result: repo/checkpoint audit and two read-only subagent audits pointed to multi-document accounting bundles as the highest-value next step. It uses existing `AccountingDocument.supplierInvoiceId`/`purchaseOrderId` and `AccountingDocumentStatus.ATTACHED`, requires no schema migration, and is safer than bank reconciliation or multi-invoice-per-PO schema work.
- Implemented: accounting documents now suggest existing supplier invoices by source hash, PO, or supplier + invoice number; payment receipts/packing/customs/order evidence can be attached to an invoice/PO bundle through `attachAccountingDocumentEvidence()` and a new `/accounting` action/UI form.
- Implemented guardrails: only clean `SUPPLIER_INVOICE` analyses with no required-review blockers may create/update supplier invoices; receipts, packing slips, customs docs, quotes, order notices, and unknown documents must be attached as evidence instead of posting AP.
- Implemented invoice-ledger visibility: `/accounting/invoices` now shows evidence bundle counts and attached document classification/status/download links. The `/accounting` dropzone now refreshes the server-rendered queue after successful upload.
- Guardrail preserved: attaching evidence does not receive stock, approve invoices, mark paid, set payment references, or overwrite the invoice primary source document.

## Verification snapshot
- RED verified: new tests initially failed for existing-invoice match suggestions, missing attach service, non-invoice apply rejection, `/accounting` attach UI, invoice evidence bundle display, and dropzone refresh.
- Targeted tests passed: `npm run test -- src/modules/accounting/documents.integration.test.ts src/app/accounting/page.test.ts src/app/accounting/invoices/page.test.ts --run --no-file-parallelism` (3 files / 11 tests).
- Gates passed: `npm run typecheck`; `npm run lint`; `npm run test:serial` (46 files / 197 tests); `npm run build`.
- Runtime smoke: killed stale `127.0.0.1:5173` owner, started `npm run start:local`, HTTP `/accounting` returned 200, browser rendered `/accounting` evidence-bundle attach controls and `/accounting/invoices` evidence bundle columns with no console JS errors observed.
- Browser mutation smoke: created temporary `TEST-ACCOUNTING-BUNDLE-SMOKE-*` invoice/document, submitted the attach evidence form, verified DB row became `ATTACHED` with invoice status still `RECEIVED` and no payment reference/paid timestamp, verified `/accounting/invoices` rendered `Evidence bundle · 1 document(s)` / `ATTACHED · PAYMENT_RECEIPT`, then cleaned up temp document/invoice/supplier/file (`docs:0`, `invoices:0`, `suppliers:0` remaining).
- Server cleanup: killed local `5173` Next process after smoke; no `LISTENING` process remained.

## Previous session — accounting OCR/readability hardening
- Request: two documents uploaded to `/accounting` failed to read; diagnose and make the feature more functional.
- Root cause: both uploads were image-only evidence. `TA_CONTRACT_1780971366313.pdf` was a jsPDF/image PDF with no embedded text; `219fca435f711555edb42b00f4660bf2.png` required OCR. The host had `pdftoppm` but no native `tesseract`, and the app had no JS OCR fallback, no scanned-PDF page OCR, and no operator path to reprocess/paste extracted text after a failed upload.
- Implemented: added `tesseract.js` OCR fallback for screenshots/images, scanned-PDF OCR by rendering PDF pages with `pdftoppm`, manual paste/re-analysis and retry extraction server actions, reviewable no-text analysis records instead of dead `FAILED` rows, extracted text previews, and stricter OCR-noise parsing for supplier names, invoice numbers, payment totals, and known currencies.
- Data repair: re-ran extraction for the two uploaded documents. `TA_CONTRACT_1780971366313.pdf` is now `NEEDS_REVIEW` / `PAYMENT_RECEIPT`, supplier `Huizhou Shengye Electronics Co., Ltd`, subtotal USD50.00, shipping USD95.50, total USD145.50. `219fca435f711555edb42b00f4660bf2.png` is now `NEEDS_REVIEW` / `QUOTE_OR_PRO_FORMA`, supplier `Huizhou Shengye Electronics Co., Ltd`, with no invented invoice number or bogus total.
- Guardrail preserved: upload/analyze/retry/manual text are evidence-only and did not receive stock, approve payment, or create AP without explicit apply.

## Verification snapshot
- RED verified: new extraction/accounting/page tests initially failed for scanned-PDF OCR fallback, unreadable-review/manual text behavior, retry/manual UI, OCR pro forma supplier/invoice-number parsing, and Alibaba paid-order totals.
- Targeted tests passed: `npm run test -- src/modules/documents/extract.test.ts src/modules/accounting/documents.integration.test.ts src/app/accounting/page.test.ts --run --no-file-parallelism` (3 files / 9 tests).
- Gates passed: `npm run typecheck`; `npm run lint`; `npm run test:serial` (46 files / 194 tests); `npm run build`.
- Runtime smoke: killed stale `127.0.0.1:5173` node process, started `npm run start:local`, HTTP `/accounting` returned 200, browser rendered both repaired document rows with retry/manual re-analysis controls and no console JS errors.
- Audit note: `npm audit --omit=dev --json` reports 2 moderate advisories through Next/PostCSS; npm's suggested fix is an invalid major downgrade to Next 9.3.3, so not applied in this pass.

## Previous session — order email line detection
- Request: the Order Email Agent was only detecting two lines; make it detect as many supplier email lines as necessary.
- Root cause: `appendUniqueLines()` used fuzzy duplicate detection within the same Alibaba graphical product-card extraction batch. In order `304716450001023166`, item 1 and item 3 had the same quantity and subtotal plus overlapping cable-clip wording, so the third explicit `View details` card was collapsed as a duplicate.
- Implemented: `parseAlibabaEmail()` now preserves every explicit Alibaba product-card segment within that extraction batch while still deduping against lines found by earlier/later parser strategies.
- Implemented: added a regression test for a three-item Alibaba email where two distinct card lines share quantity and price.
- Data fix: re-imported/reparsed existing order `304716450001023166` without auto-apply; stored lines changed from 2 to 3, status remains `NEEDS_REVIEW`, purchase order remains null, and stock movement count stayed 25 before/after.
- Incidental quality fix: regenerated Prisma Client after existing accounting-document schema changes and fixed small accounting-document TypeScript issues so the current tree quality gate can pass.

## Verification snapshot
- RED verified: `npm run test -- src/modules/email-imports/alibaba-email.test.ts --run --no-file-parallelism` failed before the parser fix with expected 3 lines but got 2.
- Email tests: `npm run test -- src/modules/email-imports/alibaba-email.test.ts src/modules/email-imports/alibaba-email.archive.integration.test.ts src/modules/email-imports/mailbox.test.ts --run --no-file-parallelism` passed (3 files / 34 tests).
- Full quality: `npm run quality` passed after `npx prisma generate`: lint, typecheck, serial Vitest (45 files / 189 tests), and production build.
- Browser smoke: `http://127.0.0.1:5173/integrations/email-import` rendered order `304716450001023166` with `Lines: 3`; browser console had 0 messages/errors.

## Current session — accounting workbench and document ingestion
- Request: research the best way to track invoices/order notices for accounting; implement comprehensive accounting tooling with drag/drop PDF/email/screenshot analysis, apply/save behavior, and improved invoice functionality.
- Repo: `C:/Users/musas/Desktop/lambenti-inventory`; profile: `lambenti`.
- Research used: Canadian CRA/CBSA record-retention, GST/HST ITC evidence, electronic/imaged records, audit trails, importer records, Peppol/e-invoicing, IAS 2 inventory, IFRS 15 revenue, Incoterms, W3C PROV, and idempotency/hash practices.
- Implemented: Prisma migration `20260608213000_accounting_documents` adds `AccountingDocumentStatus` and `AccountingDocument` with private stored path, SHA-256, MIME/size, extracted text, analysis JSON, status/error, uploader, and optional Supplier/PO/SupplierInvoice/EmailImport links.
- Implemented: `src/modules/documents/storage.ts` and `extract.ts` save/dedupe source files under `var/accounting-documents`, support PDFs, `.eml`, text/HTML/CSV, and image OCR via Tesseract seam, and preserve original source evidence separately from OCR/analysis.
- Implemented: `src/modules/accounting/documents.ts` analyzes uploaded documents into AP-focused accounting fields/classification, Canadian GST/HST/audit notes, required-review gates, duplicate keys, suggested PO/invoice links, and apply-to-invoice behavior that does not receive stock.
- Implemented: root `/accounting` workbench with drag/drop upload client, source document review queue, download route, serious accounting checklist, invoice-functionality upgrade notes, AP summary, and PO apply controls. Sidebar now links to `/accounting`; workspace revalidation includes `/accounting`.
- Improved invoices: `createInvoiceFromPurchaseOrder()` can accept source-document invoice number, dates, due date, subtotal/shipping/tax/total/currency, and document provenance when applying reviewed accounting documents.
- Guardrail preserved: upload/analyze is evidence-only; apply creates/updates supplier invoice records only; no supplier/PO/stock movement/receiving side effects during analysis, and no stock receipt during invoice application.

## Verification snapshot
- RED: new accounting document/page tests failed before implementation (`./documents` and `/accounting/page.tsx` missing).
- Schema/runtime: `npx prisma format && npx prisma migrate deploy && npx prisma generate` succeeded; migration applied to local PostgreSQL.
- Targeted tests: `npm run test -- src/modules/accounting/documents.integration.test.ts src/app/accounting/page.test.ts --run --no-file-parallelism` passed (4 tests).
- Gates: `npm run lint` passed; `npm run typecheck` passed; `npm run test:serial` passed (45 files / 189 tests); `npm run build` passed and listed `/accounting` plus `/api/accounting/documents/[documentId]/download`.
- Production smoke: started `next start` on `127.0.0.1:5173`; HTTP `/accounting` returned 200; browser rendered Accounting workbench/sidebar/dropzone/checklists/review queue with no console JS errors.
- Browser mutation smoke: programmatically uploaded `TEST-BROWSER-ACCOUNTING.txt`; UI reported `Saved and analyzed 1 accounting document` with invoice classification/amount; test document row appeared with source hash/path/download link; temporary document/audit/file were cleaned up (`deletedDocuments:1`, `removedFiles:1`).
- Server cleanup: killed local `5173` Next process after smoke; no `LISTENING` process remained.

## Previous session — dashboard package/build card
- Request: hide the `8 total BOM quantity per assembled package` text from the dashboard build-capacity card.
- Implemented: dashboard build-capacity helper no longer renders `componentsRequiredPerBuild` or the `total BOM quantity per assembled package` phrase. It now shows only package SKU plus bottleneck, e.g. `LAMBENTI_PACKAGE · bottleneck LAMBENTI_MAIN_UNIT`.
- Preserved: dashboard service still targets `LAMBENTI_PACKAGE`, still sums BOM line quantities internally, and still keeps zero-capacity package BOMs visible for logic/tests; only the card helper text was simplified.
- Verification: RED page test, targeted tests, lint, typecheck, full serial Vitest, build, production HTTP smoke, and browser checks passed.

## Previous session — supplier dropdown/custom supplier
- Request: supplier dropdowns should include all active supplier options shown on `/suppliers`, and operators need a custom supplier input.
- Root cause: item and sourcing dropdowns were fed by `getConfirmedSupplierOptions()`, which filtered out active supplier profiles that were visible on `/suppliers` but not marked `confirmedByHuman`.
- Implemented: added `getActiveSupplierOptions()` sourced from the same filtered/deduped active supplier profiles as `/suppliers`; `/inventory/items` add/edit dropdowns and `/suppliers` item-sourcing dropdowns now use it.
- Implemented: added `customSupplierName` to item create/edit and supplier sourcing forms. A typed custom supplier is created/used server-side with human-confirmed provenance, then assigned as the preferred supplier without changing inventory quantity.

## Previous verification snapshot
- Targeted supplier/item tests, lint, typecheck, full serial Vitest, and production build passed.
- Production/browser verification confirmed `/suppliers` and `/inventory/items` rendered active supplier options and custom supplier inputs without JS errors.

## Earlier session — receiving workbench
- Request: research and implement ways to improve the Lambenti inventory app.
- Implemented: `/incoming` now exposes line-level receiving forms that create auditable RECEIVE ledger entries linked to PO lines, update received quantities/status, and explicitly state that email imports/invoices do not receive stock.
- Verification: incoming tests, lint, typecheck, full serial Vitest, build, production HTTP smoke, and browser checks passed.
