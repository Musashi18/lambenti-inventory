# ISSUES

## Open / watch
- `LAMBENTI_PACKAGE` is correctly unresolved in the current local item catalog until required direct package components have cost evidence: `NOTE`, `GUIDE`, and `RETAIL_BOX` presently have no unit price. Add auditable unit-cost evidence later through Items or Suppliers; dynamic nested rollups will update package pricing automatically without stock/accounting mutation.
- Build note: plain/4096 MB `npm run build` crashed intermittently in the Next/TypeScript worker after successful compile on this Windows/Node runtime; rerun with `NODE_OPTIONS='--max-old-space-size=6144' npm run build` passed. Keep using the 6144 MB build for large current-tree builds if the worker exits with code `2147483651`.
- Supplier cleanup watch: the active DB still retains archived/cleanup-candidate historical supplier rows from prior Alibaba/email imports, but `/suppliers` no longer renders their raw junk labels as active supplier names/dropdown options. Use the visible cleanup quarantine action if you want to archive the retained evidence rows; do not hard-delete rows with email/purchasing history.
- `/accounting` default AP posting setup is now configured on the live local DB (`INVENTORY_ASSET`, `TAX_RECOVERABLE`, `ACCOUNTS_PAYABLE`, `BANK_CASH`). Continue treating invoice approval/payment as explicit human actions; the new queue/buttons make them easier to run but do not authorize automatic approval or payment.
- `/accounting/invoices` currently surfaces 1 received invoice missing a due date. This is expected live data, not a code blocker; use the visible `Save due date` control as supplier terms become known before/around approval.
- Tracking-only Alibaba portal Message Center/tracking snapshots remain in the database as evidence rows from prior captures, but they are now excluded from the active Order Email Agent queue/reassessment so they do not obscure real supplier email matches. If future portal automation adds a different subject/source pattern and the queue fills with `Unparsed order email line` again, extend `TRACKING_ONLY_PORTAL_EVIDENCE_WHERE` in `src/modules/email-imports/alibaba-email.ts` rather than weakening item matching.
- `/tracking` Capture Alibaba now has two layers of stale/idempotency protection: it remembers no-tracking/null order reads by fingerprint and hydrates memory from saved `TrackingNumber` rows before clicking. Verified current state recognizes order `304716450001023166` / tracking `888071620741` as already captured. Watch item: this repair did not rerun a live Alibaba capture/import; if Alibaba changes DOM labels again, inspect the next capture JSON for `savedTrackingOrdersHydrated`, `orderCandidatesSkippedKnownTracking`, `orderCandidatesSkippedGenericLogistics`, and Message Center errors. The previous Ship24 HTTP 400 for `7321315589070429` was resolved by using a row-id-prefixed request-signature client tracker ID; continue watching for future Ship24 `tracker_conflict`/validation errors now that provider messages are surfaced in the UI.
- The UPS import-fees email tracking row `1Z675EW60490310023` is linked to archived synced-email evidence but not to a PO/order because the source email itself has no Alibaba order ID/PO link; treat it as evidence-only until manually associated with a supplier order if needed.
- The already-running Hermes GUI session can cache `delegation.*` at startup. After changing local-worker defaults, restart Hermes/open a new session before relying on `delegate_task` to use local Qwen; this session's one-off smoke child still reported model `gpt-5.5` even though fresh config resolution points to LM Studio Qwen.
- Local PostgreSQL is currently published on host port 55432, not 5432, because Docker/Windows refused binding 5432 during this pass; `docker-compose.yml` defaults `${LAMBENTI_DB_HOST_PORT:-55432}:5432`, `.env.example` and ignored `.env` point `DATABASE_URL` at `localhost:55432`, `docs/local-runbook.md` documents the verified local run path, and the current compose-managed `lambenti-postgres` service is healthy on that mapping.
- Full Vitest file-parallel mode can be noisy/flaky around DB integration tests; serial mode (`npm run test -- --run --no-file-parallelism` or `npm run test:serial`) is the reliable local gate.
- Some passing integration tests intentionally print Prisma retry/unique-constraint messages to stdout; not a current blocker.
- App repo has a large verified-but-uncommitted checkpoint state; future agents should inspect `git status` before new feature work.
- CSP still allows `script-src 'unsafe-inline'` for current Next app-router runtime compatibility even though app-authored inline theme injection has been removed; tightening to nonce/hash-only CSP is a separate deliberate migration, not a current blocker.
- Accounting image OCR now has a `tesseract.js` fallback when native `tesseract` is missing. Scanned-PDF OCR also needs `pdftoppm`/Poppler (`LAMBENTI_PDFTOPPM_BIN` can override); if OCR still returns no text, use the manual paste/re-analysis control.
- Posted journals currently cover AP supplier invoice approval and AP payment reconciliation only. Customer/AR revenue/receivable/tax-payable journals remain future scope.
- AP invoice approval/payment reconciliation now require active GL mappings for relevant purposes (`INVENTORY_ASSET`, `TAX_RECOVERABLE`, `ACCOUNTS_PAYABLE`, `BANK_CASH`); missing mappings intentionally block posting with an operator setup error.
- Alibaba portal automation defaults to a dedicated non-default automation Chrome profile at `var/alibaba-chrome-profile` because current Chrome refuses DevTools remote debugging against the normal/default Work profile (`DevTools remote debugging requires a non-default data directory`). Use the Work/Lambenti profile only as an explicit override/manual reference path after validating Chrome allows it. Normal Capture may use safe saved-login assist, but CAPTCHA/slider/security/2FA remains manual handoff.

## Resolved this session
- Atlas exact Phase I goal-position ambiguity is resolved for this slice. `/atlas` and `/atlas/overlay` now expose a sanitized physical-truth bridge from dashboard launch readiness: exact 25-unit package target, ledger-built output, direct buildability, material coverage, separate build/input gaps, bottleneck/status, human-gated next workflow, and milestone evidence state. Current local evidence is `0/25` ledger-built, `0` direct builds, `0/25` material coverage, `25` build/input gaps, blocked by `LAMBENTI_MAIN_UNIT`; no part availability, graph baseline, activity, or forecast is counted as a built unit. Focused/full gates, build/runtime route probes, UI contract smoke, and browser DOM/API/layout/privacy checks passed with 0 errors/overflow/clipping and no raw source/path leak. No operational mutation occurred.
- Ship24 refresh parity/tracking cards/Mission Control card reliability is resolved for this pass. Root cause was a combined behavior gap: the scheduler used due-only refresh by default, Ship24 payload hints used display-oriented `courierName`, `out_for_delivery` collapsed into `IN_TRANSIT`, and existing saved events were skipped rather than upgraded when provider status semantics improved. Active refresh now checks open rows by default, Ship24 payloads use supported `courierCode`, `OUT_FOR_DELIVERY` is persisted/displayed, and saved events update. `/tracking` and `/atlas` card layouts have 0 top-level overlap in browser probes, `/tracking` shows `Out For Delivery` for the current UPS row, and Mission Control command cards are live-data/read-only workflow links. Verification passed targeted tests, lint, typecheck, full serial tests, build, live API/browser smokes, and 0 console/JS errors.
- Atlas AI Layer functional layout repair is resolved for this slice: the six AI actions now update a selected-node readout from sanitized Atlas data; the map legend is no longer an absolute overlay; Phase I cards have enough vertical clearance; AI/dependency/evidence cards fill the former empty map area left of the inspector; and the fixed theme toggle no longer covers those cards in the `/atlas` layout probe. Verified targeted/full gates, managed runtime, UI contracts, Playwright AI/layout/private-leak checks, and no operational side effects.
- `/inventory/valuation` value concentration visual and Atlas suggested UI changes are implemented for this slice. The valuation page now shows top-5 share, largest position, long-tail value, stacked concentration strip, ranked share bars, and a read-only concentration reading from ledger-derived values. Browser evidence showed `Top 5 Share 62%`, largest `LOOSE_EC`, long tail `USD 695.17`, no horizontal overflow, and 0 console/JS errors. `/atlas` now has an `Atlas Command Dock`, `Atlas Projection Navigation`, Ctrl+K `Command Palette`, `Search / Zoom / Path Controls`, `Founder Focus Mode`, `Strategic Playback`, selected/hover/focus dimming, `Done So Far`/`Needs Development` summary panels, layered strategic-score node cards, an `Executive Intelligence Panel`, Node Inspector `Why This Node Is Here` / `What Would Move It` explanations, and a read-only `Atlas AI Layer`; browser/API checks found no raw `sourceRef`, private accounting path, or `C:/Users` leakage.
- Atlas interactive progress-map UI is resolved for this slice: `/atlas` now includes a polished interactive `Atlas Progress Map` that defaults to all 18 mapped nodes and adds `Everything`, `Launch Path`, `Needs Development`, `Done So Far`, `Trust Gaps`, and `Next Move` lenses; Mission Focus Strip; Development Area Summary; richer dependency node cards; SVG dependency arrows; node inspector; dependency/unlock pulse; readable legend; and sanitized evidence stream. Browser interaction checks verified `Done So Far`, `Needs Development`, and focus-strip selection, no raw `sourceRef`/private path leakage in body/API, no horizontal overflow, and 0 console/JS errors. Targeted tests, typecheck, lint, 6144 MB build, managed runtime probes, full `test:serial`, and `smoke:ui-contracts` passed.
- Atlas roadmap implementation pass is resolved for this slice: evidence privacy tiering/sanitized DTOs, stale-evidence confidence/risk handling, overlay next-action/risk/velocity/confidence cards, `/atlas` Decision Timeline, and authenticated read-only `/api/atlas/mission-control` are implemented and verified. API probe confirmed no `sourceRef`/raw accounting path leakage and `Cache-Control: private, no-store`; full tests/build/runtime/UI smoke passed.
- Atlas route authorization and simulator input hardening are resolved for this pass: Atlas routes now require explicit `atlas:view`, `AGENT` lacks that permission, human operator roles retain access, and `/atlas/simulator` bounds focused-hours query input server-side. Verified targeted auth/Atlas tests, full lint/typecheck/serial tests/build, runtime route probes, UI-contract smoke, and browser `/atlas` console/DOM checks.
- Atlas disabled activity-coverage messaging is resolved: Atlas now reads Founder OS activity blocks by default and the minute-level snapshot cron is enabled/running every minute. Current live `/atlas` no longer contains `passive/validated activity coverage is not enabled`; it correctly reports that coverage is enabled but the current validated high-leverage sample is only about `0.3h`, below the `~2h` gate for a credible velocity date.
- Atlas functionality/visual gap is resolved for this pass: `/atlas`, `/atlas/overlay`, and `/atlas/simulator` were verified operational, and `/atlas` now has a read-only `Atlas Visual Command Deck` with mission/probability orbit, Phase I dependency spine, and probability bands instead of relying only on dense text cards. Verification passed targeted Atlas/smoke tests, typecheck, lint, 6144 MB build, fresh runtime, UI-contract smoke, 24-page browser-section smoke, and `/atlas` browser visual/DOM/console checks with `0` console/JS errors and no horizontal overflow.
- Project Atlas MVP is implemented and verified as a read-only Founder OS slice inside the inventory app. `/atlas` renders Mission Control from live operational evidence; `/atlas/overlay` renders a minimal Daily Mission Ring overlay view; `/atlas/simulator` renders scenario/counterfactual comparisons; smoke/QA route manifests include all three routes; dark-mode toggle no longer overlaps the longer sidebar at desktop width. Verification passed targeted tests, typecheck, lint, full serial Vitest, 6144 MB build, managed runtime probes, UI-contract smoke, HTTP smoke, browser-section smoke, and browser console/DOM checks. No operational side effects occurred.
- Sidebar logo insertion/editor is implemented and verified, including the distortion fix and edge-handle removal: left-menu logo click opens a clean edit modal; hover/focus overlay exposes only `Edit`; no-logo and replacement flows use the compact dropbox inside the edit path; logo images preserve natural aspect ratio instead of being stretched into the frame; visible live-preview edge/corner handles were removed because they duplicated image-size behavior; crop/resize/edge-blend sliders remain available; save persists to localStorage and closes. Final verification passed lint, typecheck, targeted/full tests, 6144 MB build, managed runtime, UI-contract smoke, Playwright no-edge-handles/aspect smoke, and browser visual/console checks with no operational side effects.
- Current UI compactness request is resolved: `/tracking` now suppresses the `What Needs Attention` section when there is no actual attention signal, keeps archived tracking numbers collapsed by default, removes the three requested title descriptions, orders bottom disclosures as add tracking evidence → reorder forecasting input → delivered history → archived numbers, and hides bottom-card descriptions until expansion. `/incoming` no longer renders the `Packing Slip Duplicate Check` section. Organized item/row lists now use alternating dark-mode-safe row backgrounds. Verified targeted tests, lint, typecheck, full serial Vitest, 6144 MB build, runtime probes, `smoke:ui-contracts`, and browser DOM evidence with no stock/accounting/purchasing/Alibaba side effects.
- UI-contract marker drift is resolved for `/inventory/items` and `/incoming`. Root cause was data-dependent visible text: live `/inventory/items` had no row labeled `Needs Cost`, and live `/incoming` had no duplicate SKU group, so marker smoke failed even though the source features existed. `/inventory/items` now renders a Stock Health legend containing `No Supplier`, `Needs Cost`, `Below Reorder`, and `OK`; `/incoming` now always renders `Packing Slip Duplicate Check` with duplicate details or a no-duplicates confirmation. Verified targeted tests, lint, typecheck, full serial Vitest, 6144 MB build, managed runtime probes, and full `npm run smoke:ui-contracts` passing with 0 failures.
- Accounting OCR upload failure is resolved for the observed local document path. Root cause/evidence: live `8574184905.pdf` had a saved `AccountingDocument` row with `textLen:0`/`NEEDS_REVIEW`, while direct extraction from the stored PDF produced `1197` characters once `pdftotext` was available. The fix resolves Windows Git-for-Windows Poppler binaries when the Next server PATH lacks Git Bash paths and re-analyzes stale empty duplicate uploads. Live reprocessing made `8574184905.pdf` `ANALYZED` customs evidence with stock movement delta `0` and supplier invoice delta `0`.
- Tracking supersession leak is resolved for the current linked shipment and future refreshes. Live old same-order tracking `888071620741` is now `ARCHIVED`/`nextRefreshAt:null` after delivered replacement `1ZA6D5820419152616`; `/tracking` active rows no longer show it. Future refreshes that mark an active associated stream delivered auto-archive older active same-order/same-PO numbers, and active cards now expose Archive/Delete actions. Verified targeted/full tests, build, runtime probes, and browser console smoke; no stock/accounting/purchasing/Alibaba/supplier-message side effects occurred.
- `/inventory/items` active catalog sectioning and `/tracking` information-received status are resolved for this pass. Active items now render under shared item-use groups with row-level audited `Catalog Section` overrides, and the current UPS label-created shipment `1ZV90R362090867759` renders as `Info Received` instead of `Pending`. Verification passed migration/generate, targeted tests, typecheck, lint, full serial Vitest, build, HTTP smokes, and browser DOM checks. Browser-console tooling still reports an empty-source exception during smoke even though console messages are empty; keep an eye on the next automated browser-section smoke before treating that as an app regression.
- Dashboard/valuation/items unit-cost drift is now fixed by the unified item cost resolver. Landed-cost evidence from email-created invoices plus attached duty/fee/shipping receipts drives item display and valuation first; manual item cost remains editable and overrides finished-good BOM defaults; finished-good costs auto-roll up from complete active BOM component costs when manual cost is blank. Live browser evidence showed dashboard Inventory Valuation and `/inventory/valuation` both at USD $1237.48, with dashboard value mix summing to the same total. BOM component rows now avoid light-only odd/even backgrounds in dark mode.
- Large dirty-tree context overhead is now mitigated by `npm run context:snapshot`, which summarizes counts/top files and suppresses repetitive line-ending warning output before any broad diff/status inspection.
- Local runtime/UI verification drift is now mitigated by first-class tooling: `lambenti:serve:verified` reconciles stale `5173` owners, builds with the 6144 MB guard, records build/source/PID metadata, and probes routes; `smoke:ui-contracts` verifies sidebar alignment, dark-mode table hover/sticky-cell behavior, route UI markers, and browser errors; `verify:tiny-ui` provides a smaller verification ladder for precise UI/theme fixes.
- Implemented the UI graphics-audit recommendation set without adding decorative charts or new mutation paths. The changed routes now expose the requested workflow signals (`Launch Next Action`, ordered tracking actions, accounting workflow rail/evidence banner, item Stock Health, BOM Build Constraint, incoming receiving progress, valuation concentration, movement ledger guide, automation severity grouping). Browser marker smoke across the 9 changed routes and full 21-page browser smoke passed with 0 console/page errors.
- `/accounting` retry OCR/manual re-analysis for source documents is now locked after apply/attach/link. The UI hides the controls for linked evidence and the service rejects retry/manual text edits on `APPLIED`, `ATTACHED`, `ARCHIVED`, or invoice-linked accounting documents. Regression tests verify no post-apply OCR mutation and no stock movement side effect.
- App-wide title/button/label casing sweep completed: final rendered audit across 21 routes found 0 title-case issues among 671 title/button/label candidates and 0 browser console/page errors. Audit artifacts are under `var/hermes-ui-audit/`.
- `/tracking` now has a premium visual command layer without changing shipment semantics: `Live shipment radar`, stage distribution bars, `Risk spectrum`, `Provider heartbeat`, and `Carrier mix`/`Evidence constellation` render from existing tracking dashboard data. Verified targeted/full gates plus HTTP/browser smokes, dark-mode CSS, no horizontal overflow, and 0 console/JS errors. No stock/accounting/purchasing/Alibaba/delivery/supplier-message side effects.
- `/tracking` full package tracking data is now expandable without changing shipment semantics: active and delivered rows expose the existing linked-order metadata, refresh schedule, full event timeline, raw provider status, and raw event payloads behind disclosure panels. The dashboard also has a useful read-only `Phase I coverage bridge` segmenting assembled/buildable/gap units. Verified targeted/full gates plus HTTP/browser smokes with 0 console errors and no stock/accounting/purchasing/Alibaba side effects.
- Accounting approval usability/safety gate implemented and verified: `/accounting` now surfaces blocked-before-approval vs ready-for-approval invoice counts, `/accounting/invoices` splits ready/fix/payment queues, the live missing-due-date Winnie XU invoice shows `Approval blocked` plus inline `Set due date`, and source-evidence-less approvals are rejected service-side before AP journal posting. Targeted accounting tests, lint, typecheck, full serial Vitest, build, HTTP/browser smokes, and browser console checks passed. No live accounting/stock/purchasing side effects occurred.
- `/tracking` Ship24 refresh failure for `7321315589070429` is fixed. Root cause was Ship24 `tracker_conflict` caused by reusing the bare local tracking row id as `clientTrackerId` after provider request parameters changed; live refresh now returns `SUCCESS`/`PENDING` with `refreshError:null`, failed refresh summary is 0, due refresh summary is 0, and side-effect checks show no stock or supplier-invoice effects. `/tracking` also now shows a dark-mode-readable shipment-progress rail on open shipment cards.
- Launch Control card copy/goal corrected: title is `Phase I Launch Readiness`, description under the title is removed, Phase I launch-readiness target is 25 units, and browser smoke shows no visible old-target launch text.
- Dashboard launch/value-mix card orientation corrected: Launch target meter now appears first and Value mix below within the same card; the adjacent Operations Flow card still matches the launch card height. Browser DOM smoke and console checks passed.
- Dashboard layout/card consolidation is implemented and verified: `Open next actions` is collapsed by default, visual signals are above the KPI card grid, `Value mix` is inside the launch target card, the adjacent operations-flow card matches the launch card height in browser bounding-box smoke, and no console/JS errors were observed.
- Added and verified the useful root dashboard graph section: launch target meter, package bottlenecks, stock pressure, operations flow, and value mix now render under `Useful visual signals` with dark-mode-readable cards and no new mutation path. Targeted dashboard tests, full lint/typecheck/serial Vitest/build, production `/` HTTP/CSS/browser smoke, visual smoke, and console checks passed; no stock/accounting/purchasing/Alibaba side effects occurred.
- Implemented and verified the read-only Phase I launch-readiness dashboard slice: `/` now shows 25-unit target, ready-now units, remaining gap, `LAMBENTI_MAIN_UNIT` bottleneck, and workflow links to BOM review, purchasing recommendations, receiving, and invoice review. Direct LM Studio Qwen local-model retry/review succeeded; `delegate_task` remains blocked in this running Hermes session because the currently loaded LM Studio model reports an 8,192-token context window below Hermes delegation's 64,000-token minimum. Targeted/full gates and 21-page browser smoke passed; no stock/accounting/purchasing/Alibaba side effects occurred.
- Supplier/AP cleanup is now complete for the safe subset: 15 cleanup-eligible unreferenced supplier junk rows were archived under audit; 27 duplicate unapproved supplier invoices were moved to `VOID` under audit; live checks now show 0 supplier cleanup candidates and 0 active duplicate invoice clusters. The PO invoice summary reports active invoice counts/totals separately from `voided duplicates`, so historical voids no longer inflate the workbench.
- Purchasing/receiving operator gaps are closed for this slice: `/purchasing/recommendations` can draft reviewable PRs only, `/purchasing/requests` has a useful empty state linking back to recommendations, and `/incoming` shows grouped duplicate PO-line summaries while preserving individual human-count receiving forms.
- App-wide route/data connectivity is now verified head-to-toe from the current tree: all 21 sections pass production HTTP smoke and rendered browser smoke, high-signal UI contracts pass, agent JSON APIs and accounting CSV exports return expected payload types/markers, Playwright saw no page/console errors, and stale `.next`/port-5173 drift that briefly caused an `/incoming` Server Component error was cleared by rebuilding and restarting from a clean port. Verification-discovered code drifts were fixed: `/accounting/invoices` now renders the duplicate-invoice guardrail, `/suppliers` cleanup candidates have a real human-gated archive action, Alibaba UI/deep-link/legal-entity supplier junk is rejected, and browser smoke no longer false-fails on strict `networkidle`.
- App-wide security/code-cleanliness hardening is implemented and verified: Next.js now serves centralized security headers plus private/no-store API cache headers, theme bootstrapping moved from app-authored `dangerouslySetInnerHTML` to static `public/theme-init.js`, local-production loopback auth rejects spoofed/contradictory host headers, API/source security contracts are tested, npm audit prod/full report 0 vulnerabilities after Next/PostCSS/esbuild lock/override cleanup, and tracked secret-pattern scan reports 0 findings. Full lint/typecheck/serial Vitest/build, HTTP route/header smokes, and browser `/accounting`/`/tracking`/`/inventory/movements` smokes passed with 0 console/JS errors. No stock/accounting/payment/receiving/Alibaba side effects occurred.
- Simplified and hardened `/tracking` UI: active shipments are now card-based with an attention queue up front, manual tracking/delivered history/lead-time audit are collapsed behind clear labels, failed refresh rows get retry controls, provider/status labels are normalized, junk supplier labels are hidden, and dark-mode tracking colors are readable. Verified targeted tracking tests, lint/typecheck, full serial Vitest, build, HTTP/browser smoke, and 0 browser console/JS errors. No stock/accounting/Alibaba capture side effects occurred.
- Improved `/accounting` scanability/functionality with a read-only command center, one-click default AP GL setup, and an invoice approval workbench. Live default AP mappings/accounts were installed once and now show posting setup ready. `/accounting/invoices` now surfaces ready/missing-evidence/missing-due-date/payment queues, approve-from-queue actions, due-date save controls, and review-readiness labels. Verified targeted tests, lint, typecheck, full serial Vitest, build, HTTP/browser smoke, and dark-mode visual legibility. No live invoice approval/payment/reconciliation or stock side effect was performed.
- Hardened `/inventory/movements` auditability: recent movement rows now show signed ledger impact and per-item balance after each visible ledger entry. Tests verify balance math across RECEIVE/RESERVE/CONSUME and separate items; full lint/typecheck/serial-test/build and HTTP/browser smokes passed.
- Removed the `/tracking` Automatic Alibaba capture UI and repaired Order Email Agent visibility. Root cause was tracking-only Alibaba portal message/tracking evidence being shown as active order-email imports; active import queue/reassessment now filters those portal evidence rows out, restoring visible matched supplier emails/items. Verified targeted tests, lint/typecheck, full serial tests, build, HTTP smokes, and browser smokes with 0 console/JS errors.
- Fixed the Hermes runtime-readiness interruption path that was blocking resumed work: transient `setup.runtime_check` transport timeouts are no longer allowed to overwrite a valid ready state with `Inference not ready`, and Windows locked-log rollover is now non-fatal/noisy-backoff instead of repeated logging tracebacks. Rebuilt/copied the Hermes desktop renderer for next launch. During Lambenti resume verification, fixed current-tree `/tracking` drift: lead-time typing/null narrowing now passes `tsc`, and the `Capture Alibaba tracking` action/card is restored on `/tracking`. Full Lambenti `npm run quality` and browser smoke passed; no live Alibaba capture/import was run.
- Fixed Capture Alibaba saved-tracking recognition: tracking-only capture hydrates `var/alibaba-portal-agent-state.json` from persisted `TrackingNumber` rows before portal clicks, so already-saved order `304716450001023166` / tracking `888071620741` is recognized as `tracking-already-captured` and will not be reopened as a repeated Completed & In Review order candidate. Generic Alibaba Logistics Services marketplace links are now skipped before navigation. The current runtime state was proactively hydrated and verified with current DB data (`trackingRows:3`, `savedTrackingOrdersHydrated:1`, `rememberedOrders:8`). Targeted tests, full lint/typecheck/serial Vitest/build, HTTP/browser `/tracking` smoke, and browser console check passed. No live Alibaba import/capture, stock movement, invoice, payment, receiving, delivery confirmation, or message-send side effect occurred.
- Resumed stale-aware `/tracking` Capture Alibaba hardening: order candidates are now remembered even when no tracking/snapshot is found, `order:<id>` memory keys prevent page-text order-id drift, Completed & In Review candidates are prioritized/deduped around Track Package / Track Shipment(s) controls, generic logistics-services links are ignored, Message Center duplicate thread-card elements are deduped, and stale-scan JSON reports total remembered chats correctly. Verified with targeted tests, full gates, bounded controlled portal dry-run, HTTP/browser `/tracking` smoke, and 0 browser console/JS errors. Dry-run only; no portal imports, stock movements, invoice creation, message sending, payment, or receipt confirmation occurred.
- Fixed `/tracking` Capture Alibaba login-handoff regression: dedicated profile trusted-account confirmation now uses Lambenti email fallback, profile-open fallback messages use the actual profile label instead of Work-profile text, app capture timeout is 420s, and current logged-in dedicated-profile capture imports successfully. No stock/invoice/receiving/message-sending side effects.
- Hardened `/tracking` Capture Alibaba for the required orders-first exhaustive sweep: tracking-only capture now checks Alibaba Delivering orders, checks Alibaba Delivered orders, then individually clicks/scrolls visible Message Center supplier conversations for shipping/tracking evidence. Tracking-only capture skips invoice download passes for speed, uses shorter tracking-specific waits, preserves message `conversationContext`, rejects side-effect/message-send controls, and keeps `autoApply:false`/`autoCreateInvoices:false`. Targeted tests, full lint/typecheck/serial Vitest/build, HTTP/browser `/tracking` smoke, portal dry-run, and DB side-effect checks passed. Full live scraping remains blocked until Chrome Work profile control/manual login handoff is available.
- Hardened `/tracking` Capture Alibaba for the screenshot Message Center flow: the portal agent opens `https://message.alibaba.com/message/messenger.htm`, scrolls/reads each visible supplier conversation thread, saves only shipping/tracking-relevant message context, rejects composer/send/reply/inquiry controls, and preserves tracking-only `autoApply:false`/`autoCreateInvoices:false` boundaries. Targeted tests, full lint/typecheck/serial Vitest/build, HTTP/browser `/tracking` smoke, portal dry-run, and DB side-effect checks passed. Full live message scraping remains blocked until Chrome Work profile control/manual login handoff is available.
- Split delivered shipment records out of the active `/tracking` table while retaining evidence/history: delivered `1Z675EW60490310023` now appears only under `Delivered tracking history` with total ship time `3d 17h`, linked evidence/latest event/source retained, and stale `nextRefreshAt` cleared with audit action `CLEAR_DELIVERED_TRACKING_REFRESH`. Full lint/typecheck/serial-test/build gates and browser smoke passed; no stock/invoice side effects.
- Hardened Alibaba tracking scrape for the 3-month cutoff and message-conversation context: tracking-only capture now runs `--deep --recent-months=3`, scrolls lazy-loaded evidence containers, rejects send/reply/inquiry/chat controls, preserves shipment-relevant `conversationContext`, skips/prunes older Alibaba tracking rows, and removed old 2019 row `LL270153423CN` from active tracking. Browser `/tracking?smoke=final` shows 2 current rows, old row absent, recent `888071620741` linked to the existing PO/import, UPS delivered `Next: —`, Ship24 configured, and no console errors; DB side-effect check returned 0 stock movements and 0 supplier-invoice effects.
- Installed the user-provided Ship24 API token into ignored `.env` without echoing the secret; restarted `npm run start:local`; `npm run agent:tracking-refresh -- --json` returned `configured:true`, scanned 3, refreshed 3, failed 0. Browser `/tracking?smoke=ship24-live` now shows `SHIP24 configured`, due refresh 0, delivered 1, needs config 0, failed 0, and 0 console/JS errors. DB shows all three tracking rows at refresh `SUCCESS`; UPS is `DELIVERED`, the two Alibaba portal numbers are `PENDING`. No stock movement or supplier-invoice side-effect rows were found for the tracking numbers/actors.
- Integrated the tracking-number workbench: tracking numbers from Alibaba portal/email evidence are now persisted in dedicated `TrackingNumber` rows, linked to matching order/import/PO metadata, shown on `/tracking`, refreshed through a configurable tracking-service endpoint, and auto-polled from the page when the provider is configured. Backfilled current evidence for `888071620741`, `LL270153423CN`, and `1Z675EW60490310023`; fixed false-positive extraction; verified full quality and browser smoke with no stock/payment/receiving side effects.
- Hardened Alibaba portal navigation/tracking agent: safe read-only candidate filtering is now tested/shared with the browser agent, tracking numbers survive extraction and portal import validation, message-thread snapshots get stable distinct IDs without order IDs, and CAPTCHA/security/login challenges encountered after clicking detail/invoice links are propagated as manual handoff instead of being swallowed.
- Verified Alibaba portal hardening with RED helper/API tests, `node --check` for portal scripts, targeted Alibaba/API tests (4 files / 20 tests), full lint/typecheck/serial Vitest/build gates (53 files / 236 tests), profile-info smoke, and expected Work Chrome profile already-open dry-run diagnostic.
- CAPTCHA/slider/security/2FA bypass remains intentionally unimplemented; the safe behavior is detection plus human completion in headed Chrome, then saved-session reuse for read-only extraction.
- Fixed LED strip preferred supplier loss: DB/audit evidence showed both LED strip SKUs had null suppliers despite latest manual save audits pointing to `jason zhou`; Alibaba email apply/reassessment and a test fixture cleanup path were overwriting/deleting supplier references. Code now preserves existing manual suppliers, tests snapshot/restore protected LED rows, and the live LED rows were restored to `jason zhou` plus real applied-order cost provenance with `RESTORE_ITEM_PREFERRED_SUPPLIER` / `RESTORE_ITEM_COST_PROVENANCE` audit logs.
- Verified LED supplier persistence with targeted RED/GREEN regression, full email archive integration suite, lint, typecheck, serial Vitest (52 files / 229 tests), build, DB post-suite check, and browser DOM smoke confirming both LED edit dialogs select `jason zhou`.
- Improved `/accounting` workbench scanability with an Attention queue, source-document triage metrics, per-row readiness badges, and a control-trail reminder; verified with targeted accounting tests, full `npm run quality`, HTTP route smoke, and browser dark-mode render/console checks.
- Fixed the dark-mode styling gap for new accounting triage/status color classes (`bg-blue-50/100`, `bg-*-100`, `text-*-900`, `border-blue-200`) after browser smoke briefly exposed unhelpful contrast.
- Central Lambenti Agent Control Plane read-only audit run `C:\\LambentiAI\\lambenti-agent\\runs\\run_20260613_171813` routed correctly but produced generic/hallucinated file names; current implementation was based on direct repo/browser inspection instead.
- Implemented approved purchase request → draft purchase order conversion from `/purchasing/requests`: purchasing actors can create a draft PO from an approved request; agents are blocked; missing supplier/unit-price evidence is blocked; request becomes `CONVERTED`; no stock movements or receiving side effects occur.
- Verified conversion with targeted service/action integration tests (2 files / 11 tests), full lint/typecheck/serial Vitest/build gates (52 files / 227 tests), HTTP `/purchasing/requests` smoke, and browser mutation smoke using a temporary request that was cleaned up.
- Fixed the current inventory/accounting pass: preferred supplier saves from item edit dropdowns, `/accounting` source-document deletion, stale dashboard order-review notice removal, finished-good purchase/reorder exclusion, minimalist supplier archive control, and Work Chrome-profile Alibaba portal automation improvements.
- Verified current pass with targeted tests (10 files / 54 tests), Alibaba portal tests (2 files / 7 tests), full lint/typecheck/serial Vitest/build gates (52 files / 223 tests), route/browser smoke, diff check, and redacted secret scan.
- Dashboard no longer shows email catalog-matching notices such as order `303671327001023166`; manual matching/review remains available in the dedicated Order Agent/accounting workflows.
- Purchase recommendations and automation reorder scans now exclude `FINISHED_GOOD` records because Lambenti finished goods are assembled internally.
- Alibaba slide CAPTCHA/security behavior is handled by detection + human handoff in headed Work Chrome profile; bypass/circumvention was not implemented.
- Implemented posted balanced AP journals: `JournalEntry`/`JournalEntryLine` migration, account snapshots, idempotent AP invoice approval posting, AP payment reconciliation posting, `/accounting/journals`, journal CSV export, and GL mapping setup guidance.
- Payment reconciliation UI now only offers approved invoices for reconciliation and shows received invoices as waiting for approval; bank imports remain non-posting until explicit human allocation.
- Verified journal work with targeted tests (6 files / 15 tests), full lint/typecheck/serial Vitest/build gates (52 files / 216 tests), HTTP route checks, browser render smoke, and CSV header smoke.
- Implemented full automatic-accounting expansion: supplier-scoped invoice numbers, multiple invoices per PO, bank/payment reconciliation, AR/customer invoices, GST/HST CSV export, GL account mapping metadata, and landed-cost allocation reports.
- Verified expansion with targeted accounting tests (8 files / 29 tests), full lint/typecheck/serial Vitest/build gates (50 files / 211 tests), HTTP/browser route smoke including `/accounting/customer-invoices`, and CSV API header checks.
- Implemented automatic-accounting evidence bundles: existing invoice suggestions by hash/PO/supplier+invoice number, attach-only evidence workflow, invoice ledger bundle display/downloads, and server-side guardrails so non-invoice documents cannot post AP or payment/stock effects.
- Browser-smoked a temporary `TEST-ACCOUNTING-BUNDLE-SMOKE-*` payment receipt attach to a supplier invoice; verified attached status/audit, invoice still `RECEIVED`, no payment reference/paid timestamp, then cleaned up temporary DB rows/file.
- Diagnosed and repaired the two failed accounting reads: the PDF had no embedded text and the PNG needed OCR; both now have extracted text/analysis rows in `NEEDS_REVIEW` with source files retained.
- Added OCR/readability hardening: JS OCR fallback, scanned-PDF page OCR, retry extraction, manual pasted-text re-analysis, no-text records staying reviewable, extracted text previews, and OCR-noise parsing guards for supplier names/invoice numbers/payment totals.
- Verified `/accounting` production smoke after killing stale port `5173`; browser rendered the repaired rows and action controls with no console JS errors.
- Added accounting document ingestion/apply flow without inventory side effects: upload/analyze stores evidence only, apply creates/updates supplier invoices, and tests prove no stock movement/received quantity change.
- Browser production smoke initially returned 500 when a stale/incorrect local `5173` server was running without loopback local auth; killed stale processes and verified the rebuilt server with `LAMBENTI_ALLOW_LOCAL_PROD_AUTH=true` returned 200 for `/accounting`.
- Order Email Agent collapsed the third line of Alibaba order `304716450001023166` because fuzzy duplicate detection ran within a single explicit product-card batch. Fixed parser/test/data; affected order now stores 3 lines and no stock movement was created.
- Full quality was initially blocked by stale Prisma Client/accounting-document TypeScript issues from the current uncommitted tree. Regenerated Prisma Client after killing the stale `5173` Node server and fixed the small TS issues; `npm run quality` now passes.

## Resolved previous profile-tuning session
- Lambenti profile memory was near capacity and missing the receiving-workbench fact. Compacted memory and added `/incoming` receiving, supplier-dropdown, package-dashboard, and serial-test operating facts.
- `current-app-feature-briefing.md` was stale relative to receiving, supplier dropdown/custom supplier, and dashboard package behavior. Rewritten as the current app briefing.
- `dashboard-finished-good-bom-capacity.md` conflicted with the newer hidden-BOM-text rule. Added supersession banner and corrected wording.
- Future app verification guidance still used non-serial Vitest in `AGENTS.md`, `package.json` quality, and weekly health cron. Updated those to serial-test guidance and verified `npm run quality` passes with lint, typecheck, 43 serial test files / 182 tests, and build.
- The Windows Lambenti CLI profile still enabled macOS-only `computer_use`. Disabled it and patched the profile-audit reference so future audits preserve core Lambenti tools while pruning unusable toolsets.

## Resolved previous session
- Supplier dropdowns only showed `confirmedByHuman` supplier options while `/suppliers` displayed additional active profiles. Fixed by introducing active supplier options from the same profile source and using them in item/sourcing dropdowns.
- Operators could not assign a supplier that was not already in the list. Fixed by adding a custom supplier input that creates/uses an audited supplier record and assigns it without changing stock.
- Fresh receiving forms initially rendered blank defaults because the global initial action-state values overwrote line-specific defaults. Fixed by only merging submitted action-state values after a real submission.
