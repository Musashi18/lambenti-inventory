# TASK_QUEUE

## Done
- Atlas Momentum Engine worked-time refresh repair: diagnosed frozen `/atlas` worked-hours as stale `activity_blocks.jsonl` despite healthy minute snapshots. `src/modules/atlas/activity-events.ts` now merges a fresher current-day daily export into the latest weekly export and dedupes duplicate daily/weekly block rows by block identity; regression coverage added in `src/modules/atlas/activity-events.test.ts`. Profile exporter `founder_os_activity_export.py` now upserts current period/label rows instead of appending duplicates; new `founder_os_activity_export_tick.py` refreshes current daily+weekly exports; cron `25d050f6ccef` runs it every 5 minutes and has passed a manual run. Live `/api/atlas/mission-control` reports `dailyTotalHours: 3.5`; browser `/atlas` renders `3.5h WORKED TODAY` with 0 console/JS errors. Verified targeted Atlas tests, typecheck, lint, 6144 MB build, full `test:serial` (87 files / 428 tests), managed Atlas route/API probes, and `smoke:ui-contracts`.
- Atlas interactive progress map upgrade: `src/app/atlas/atlas-progress-map.tsx` now makes `/atlas` a clearer visual company map instead of a mostly static command deck. It defaults to every mapped development area, adds six Phase Lens filters (`Everything`, `Launch Path`, `Needs Development`, `Done So Far`, `Trust Gaps`, `Next Move`), a Mission Focus Strip, Development Area Summary, richer node cards with area/horizon/completion/risk/trust/blockers, SVG dependency arrows, selected/dependency/unlock highlighting, readable legend, Node Inspector, Dependency Pulse, and sanitized Evidence Stream. It is fed by `buildAtlasMissionControlView`, so raw `sourceRef` and private accounting paths are not serialized to the client. Updated `smoke:ui-contracts` markers. Verified targeted Atlas/smoke tests (3 files / 13 tests), typecheck, lint, 6144 MB build, managed route/API probes, full `test:serial` (87 files / 427 tests), `smoke:ui-contracts`, API sanitization probe, and browser `/atlas` visual/interaction/console/layout checks.
- Atlas roadmap implementation pass: implemented evidence privacy tiering/sanitized presentation helpers, stale-evidence scoring/confidence discounting, upgraded daily overlay brief, visible `/atlas` Decision Timeline, and authenticated read-only `/api/atlas/mission-control` with `private, no-store` caching and sanitized payloads. Verified targeted tests (4 files / 15 tests), typecheck, lint, full serial Vitest (87 files / 427 tests), 6144 MB build, managed route/API probes, `smoke:ui-contracts`, direct API sanitization probe, and browser `/atlas` DOM/console evidence. No operational side effects occurred.
- Atlas security and recursive next-step research: audited current Atlas routes/modules and OWASP guidance; added explicit `atlas:view` permission for human operator roles while excluding `AGENT`; changed `/atlas`, `/atlas/overlay`, and `/atlas/simulator` from generic `item:view` to `atlas:view`; server-side bounded `/atlas/simulator` focus-hour input to 1–12h/day in 0.5h increments with safe fallback. Verified targeted Atlas/auth tests (5 files / 20 tests), typecheck, lint, 6144 MB build, full serial Vitest (86 files / 423 tests), managed runtime route probes, full `smoke:ui-contracts`, browser `/atlas` console/DOM check, and HTTP clamp evidence for `hours=999`. No operational side effects occurred.
- Atlas Founder OS activity coverage bridge: `/atlas` now reads profile-local Founder OS passive activity blocks (`activity_blocks.jsonl`) by default through `src/modules/atlas/activity-events.ts`, maps high-confidence/high-leverage blocks into velocity evidence, and still prevents time-only activity from increasing graph completion. Current live data proves coverage is enabled but too sparse for a credible date (`~0.3h` validated high-leverage sample, below the `~2h` gate), so Atlas shows a coverage-enabled sample-size caveat instead of the old disabled-coverage phrase or a misleading far-future projection. Verified targeted Atlas tests (3 files / 6 tests), typecheck, lint, 6144 MB build, local production HTTP probes for `/atlas`, `/atlas/overlay`, `/atlas/simulator`, and `/atlas` browser console check with 0 messages/errors.
- Atlas visual command deck check: verified the existing `/atlas` functionality and found it operational but too text/card-heavy for the intended Atlas/Founder OS feel. Added a read-only `Atlas Visual Command Deck` with mission/probability orbit, Phase I critical-path dependency spine, and probability interval bands while preserving Atlas no-mutation boundaries. Verified typecheck, lint, targeted Atlas/smoke tests (`5` files / `13` tests), 6144 MB build, fresh local runtime, `smoke:ui-contracts`, 24-page browser-section smoke, and `/atlas` browser visual/DOM/console checks with `0` console/JS errors and no horizontal overflow. No stock/accounting/purchasing/receiving/Alibaba/payment/delivery/supplier-message side effects occurred.
- Project Atlas Founder OS MVP: implemented `/atlas` Mission Control, `/atlas/overlay` Daily Mission Ring, and `/atlas/simulator` as a functional read-only Atlas Kernel over existing Lambenti operational evidence. Atlas now renders an evidence-backed dependency graph, mission/company completion, launch probability interval, projected date/remaining hours with explicit unknowns when velocity evidence is absent, bottleneck/risk ranking, Opportunity Engine, Reality Engine, Strategic Radar, Progress Galaxy, company-health intervals, minimal overlay view, and scenario simulator. Added `src/modules/atlas/*` typed graph/scoring/evidence/scenario modules plus tests; integrated Atlas into sidebar and smoke/QA route manifests; adjusted the theme toggle so it no longer covers the expanded desktop sidebar. Verified targeted Atlas/theme/smoke tests, typecheck, lint, full serial Vitest (85 files / 417 tests), 6144 MB build, managed runtime `/atlas` + `/atlas/overlay` + `/atlas/simulator` probes, UI-contract smoke, HTTP smoke (24 pages / 5 UI contracts / 4 JSON APIs / 3 CSV exports), browser-section smoke (24 pages), and browser console/DOM evidence. No stock/accounting/purchasing/receiving/Alibaba/delivery/supplier-message side effects occurred.
- Sidebar logo insertion/editor: added a polished client-side left-menu logo workflow and repaired image distortion. Current logo click opens an edit modal; hover/focus overlay exposes only `Edit`; no-logo state renders a small image dropbox; dropped/chosen images enter the same editor; the image now preserves natural aspect ratio instead of being stretched into the frame; the follow-up removed visible preview edge/corner resize handles because they duplicated image-size behavior and made the editor noisier; crop, image size, logo height, edge blend width, and edge opacity controls preview live; save persists settings/data URL to localStorage and closes. Image replacement happens by dropping/choosing a file in the edit modal, so no separate swap button is rendered. Verified lint, typecheck, targeted sidebar/smoke tests, full serial Vitest (81 files / 409 tests), 6144 MB build, managed runtime probes, full UI-contract smoke, Playwright no-edge-handles/aspect smoke, and browser click/visual/console checks. No stock/accounting/purchasing/receiving/Alibaba side effects occurred.
- Tracking/incoming UI compactness pass: `/tracking` hides `What Needs Attention` when no provider/due/failure/unlinked issue exists, archived tracking numbers are collapsed by default, title descriptions under `Tracking Workbench`/`Active Tracking Numbers`/`Live Shipment Radar` are removed, bottom cards order as add tracking evidence → reorder forecasting input → delivered history → archived numbers, and bottom-card descriptions are visible only when expanded. `/incoming` no longer renders `Packing Slip Duplicate Check`. Organized item/row lists now alternate `bg-white`/`bg-slate-50` rows across item catalog, shared dashboard tables/valuation, incoming lines, and tracking lead-time/delivered/archive rows. Verified targeted 16-test suite, typecheck, lint, full serial Vitest (81 files / 407 tests), 6144 MB build, runtime HTTP `/tracking` `/incoming` `/inventory/items` `/inventory/valuation`, full `smoke:ui-contracts`, and browser DOM checks. No stock/accounting/purchasing/Alibaba/delivery/supplier-message side effects occurred.
- UI-contract drift repair: made `/inventory/items` Stock Health status labels visible via a data-independent legend and made `/incoming` `Packing Slip Duplicate Check` visible for every incoming PO with either duplicate details or a no-duplicates confirmation. Added source-contract tests. Verified targeted 8-test suite, lint, typecheck, full serial Vitest (81 files / 405 tests), 6144 MB build, managed runtime HTTP `/inventory/items` `/incoming` `/inventory/movements`, and full `smoke:ui-contracts` with no failures. No stock/accounting/purchasing/Alibaba/supplier-message side effects occurred.
- Tracking/accounting/dashboard request: moved `/tracking` Archive/Delete controls into collapsed `Shipment Details`, added bottom-page `Archived Tracking Numbers`, excluded/rejected `VOID` invoice bundles from `/accounting` source-document attach dropdown/matching/attachment, and made dashboard low-stock filtering BOM-aware so components for sufficiently stocked finished builds move out of current low stock into `Not Currently Needed Low Stock Components`. Verified targeted 27-test suite, typecheck, lint, full serial Vitest (81 files / 404 tests), 6144 MB build, runtime HTTP `/` `/tracking` `/accounting`, and browser console/DOM smokes. Full `smoke:ui-contracts` route markers passed for changed routes but still reports unrelated marker drift on `/inventory/items` and `/incoming`.
- Accounting OCR/tracking archive repair: fixed Windows local-production PDF text extraction by resolving Git-for-Windows Poppler utilities when PATH is missing, made duplicate uploads re-analyze stale unreadable `AccountingDocument` rows, reprocessed live `8574184905.pdf` into `ANALYZED` customs evidence with no stock/invoice side effects, added automatic archiving of same-order/same-PO active tracking numbers when the associated active stream is marked delivered, added `/tracking` active-card Archive/Delete actions, and archived live superseded tracking `888071620741` after delivered replacement `1ZA6D5820419152616`. Verified targeted 48-test suite, typecheck, lint, full serial Vitest (81 files / 402 tests), 6144 MB build, runtime HTTP `/accounting` + `/tracking`, and browser console smokes with 0 errors.
- Firmware V22 organic-magic actuation pass: created and compacted `.hermes/desktop-attachments/Lambenti_V22_organic_magic.ino` plus Desktop copy `C:/Users/musas/Desktop/Lambenti_V22_organic_magic.ino`. Added an AVR-safe online critically/slightly-over-damped organic target spring after mapping/guards/target conditioning and before brightness smoothing, with adaptive slow/fast omega constants, low-PWM calming, overshoot prevention, settle snap, and full removal bypass. Compression steps: removed optional top-end spring-speed boost and velocity clamp branches, then removed all remaining `powf()` usage by switching field linearization to `sqrtf(x)`, pre-glow to direct `smootherstep01(preX)`, and gamma pass to `clamp01(u)`. Preserved V7/V20/V21 Timer1 31.25 kHz PWM, hard-off, full-triangle fusion, fractional smoothing, low-PWM latch, robust calibration gates, V21 top-end reach assist, and raw-removal release. Arduino/MiniCore bootloader compile now passes: `31784 bytes (98%)` flash of `32384`, `928 bytes (45%)` RAM of `2048`. SHA-256 `0555e5265cbd002bb174884c05cc34ba322f7455265298aaad081103db3be8bf`.
- Firmware V21 magic-smooth actuation pass: created `.hermes/desktop-attachments/Lambenti_V21_magic_smooth.ino` and Desktop copy `C:/Users/musas/Desktop/Lambenti_V21_magic_smooth.ino`. Added bounded top-end reach assist for the high-80/low-90% → near-full region, stricter full-triangle production fusion, and more robust boot calibration sample/spike gates while preserving V7/V20 Timer1 31.25 kHz PWM, hard-off, triple fusion, fractional smoothing, low-PWM latch, and raw-removal release. Static verification passed; Arduino compile was blocked by missing local Arduino/AVR/C++ toolchains. SHA-256 `9ce5c49fb80661f0101750f4d8a3b286ed3bb823f3f2f699a438d0b25f65f7cf`.
- Hardened valuation robustness: priced valuation rows now carry typed price-source provenance from the resolved unit-cost engine, `/inventory/valuation` shows a `Price Source` column, and tests cover source propagation plus landed-cost precedence over manual finished-good overrides/BOM rollups. Verified targeted tests (4 files / 15 tests), lint/typecheck, full serial Vitest (81 files / 399 tests), 6144 MB build, diff check, and browser smokes for `/inventory/valuation` + `/` with 0 console/JS errors.
- Repaired the valuation regression caused by the previous receipt-lot weighting change: priced item valuation is restored to the resolved unit-cost engine rather than stale `StockLot.unitCost`; original Alibaba supplier order/payment documents with `Incoterms and duties` no longer count as extra attached landed-cost evidence. Live valuation now shows `0805_CLIP` as `2500.00 / USD 0.06 / USD 145.50`, total `USD $1764.79`, and dashboard valuation matches. Verified targeted tests (4 files / 15 tests), lint, typecheck, full serial Vitest (80 files / 396 tests), 6144 MB build, diff check, and browser smokes with 0 console/JS errors. Patched the accounting/purchasing skill guidance so future work does not reintroduce lot-cost precedence over accounting-landed/manual pricing.
- Fixed the current receiving/valuation prompt: `/incoming` confirmed receipt rows now collapse/fade smoothly before refresh; shared quantity formatting prevents long floating-point stock tails and displays common quantities at 2 decimals; same-item/same-lot receipt creation reuses matching lots or returns a clear different-cost error instead of Prisma unique-constraint noise; manual item price edits sync only the most recent PO receipt lot cost so older stock keeps historical valuation; original Alibaba supplier order/payment receipts are excluded from extra landed-cost evidence unless explicit customs/duty/additional-freight signals are present. Verified targeted regression tests (6 files / 26 tests), lint, typecheck, full serial Vitest (80 files / 396 tests), 6144 MB build, diff check, and browser smokes for `/`, `/incoming`, and `/inventory/valuation` with 0 console/JS errors. No live stock receiving/manual price edit/accounting posting/payment/purchasing/Alibaba/delivery/supplier-message side effects occurred.
- Made manual lead-time inputs primary and implemented the follow-up lead-time/tracking/dashboard/purchasing UX pass. Migration `20260621120000_manual_item_lead_time` adds `Item.manualLeadTimeDays`; manual saves now remain primary over observed averages, sync preferred supplier lead time, and keep observed weighted averages as quantity-weighted evidence only. `/` Lead-Time Horizon orders longest-first with green observed vs blue manual-primary colors and inline observed-average explanation; Operations Flow cards show hover summaries and Review Actions routes to `/#human-approval-queue`. `/purchasing/recommendations` is priority-sorted by supply gap + lead time with 1-day lead-time rows forced lowest. `/tracking` now has `Refresh`, refreshes all active rows, resets counters, archives superseded same-order tracking numbers out of API polling, and renders `What Needs Attention` below active tracking numbers. Verified migration/generate, targeted tests (6 files / 38 tests), typecheck, lint, full serial Vitest (79 files / 386 tests), 6144 MB build, route probes, Playwright DOM/hover smoke, and 0 console/page errors on `/`, `/tracking`, `/purchasing/recommendations`, `/suppliers`; no stock/accounting/purchasing/Alibaba/delivery/supplier-message side effects occurred.
- Set up local embedded Hindsight for the Lambenti Hermes profile, ported Hermes memory/profile plus LambentiMemoryVault markdown into the `hermes-lambenti` Hindsight bank, and verified retain/list/recall/reflect. Fixed this prompt's app requests: no light flash during dark refresh, item catalog section row dark theme, dashboard stock/shortage type labels tied to item-page component-use overrides, `/tracking` every-item lead-time log with audited manual lead-time save, root-dashboard `Lead-Time Horizon`, and live USD conversion rates with fallback. Verified typecheck, targeted tests (6 files / 37 tests), lint, full serial Vitest (79 files / 382 tests), 6144 MB build, live FX HTTP 200, and fresh runtime/browser DOM smokes. No stock/accounting/purchasing/Alibaba/delivery/supplier-message side effects occurred.
- Grouped `/inventory/items` active catalog by the shared item-use component sections and added manual catalog-section overrides. Migration `20260620213000_item_use_group_override` adds nullable `Item.useGroupOverride`; the row-level `Catalog Section` selector writes audited overrides and shared item dropdown/page grouping honors them, while blank override returns to automatic classification. Tracking status normalization now treats carrier-information-received, `info_received`, label-created, and shipment-information-sent cases as `INFO_RECEIVED`; the current UPS label-created package renders as `Info Received` instead of `Pending`. Verified migration/generate, targeted tests (4 files / 35 tests), typecheck, lint, full serial Vitest (79 files / 380 tests), build, HTTP smokes, and browser DOM evidence for 26 section controls plus tracking `Info Received` status.
- Implemented the unified item unit-cost engine and valuation parity slice: order-level shipping/tax/duty/brokerage/other charges allocate by merchandise value with exact cent totals and 4-decimal landed unit costs; item cost resolution now prefers accounting landed-cost evidence, then manual item unit cost, then finished-good BOM rollup when no manual override exists. `/inventory/items`, `/inventory/valuation`, and dashboard inventory valuation/value mix use the same resolved costs; BOM component rows are dark-mode compatible. Follow-up audit resolved all 26 active component/consumable/finished-good items; 14 accounting landed-cost component item-master records were backfilled to the resolved 4-decimal USD costs with audit logs, while BOM-rollup finished goods intentionally remain dynamic unless manually edited. Verified with targeted tests, typecheck, lint, full serial Vitest (79 files / 375 tests), 6144 MB build, route probes, browser console smokes, and read-only valuation/dashboard parity audit.
- Implemented the additional shipping/duties receipt valuation chain: attached/applied payment receipts with explicit additional shipping/freight/carrier/logistics charge signals now count as landed-cost evidence alongside customs/duty/brokerage/clearance receipts, accumulate across repeated receipt imports/attachments, feed item landed-cost summaries, and drive inventory valuation without stock movement. Added regression coverage for customs + second shipping receipt accumulation, valuation propagation, no stock movement, and workspace revalidation for `/accounting/landed-cost`, `/inventory/items`, and `/inventory/valuation`. Verified targeted accounting/page tests (3 files / 17 tests), typecheck, lint, full serial Vitest (77 files / 362 tests), 6144 MB build, managed fresh runtime (PID 4308, build `SpTGF3b4t1CSJOf2SWzHE`), 21-page browser smoke, targeted DOM evidence for live `PaymentReceipt.pdf`, and diff check; no stock/accounting posting/payment/purchasing/Alibaba/delivery/supplier-message side effects occurred.
- Added the token-efficient context snapshot workflow: `npm run context:snapshot` summarizes dirty-tree counts, top changed files, checkpoint sizes, runtime metadata, and suppresses repetitive Git CRLF/LF warning spam; scoped usage supports `--paths=<files-or-dirs>` before narrow file inspection.

- Implemented the operator-performance architecture upgrade set from the performance review: a managed local runtime command (`npm run lambenti:serve:verified`) with stale-port cleanup/build/probes/runtime metadata, reusable UI contract smoke (`npm run smoke:ui-contracts`), tiny UI verification ladder (`npm run verify:tiny-ui`), semantic table hover design tokens, and local-runbook scope-classifier documentation. Verified with focused tests (3 files / 11 tests), typecheck, lint, managed production build/start (PID 38856, build ID `ImDEmvt75cTdXAGin8eM-`), UI contract smoke, tiny UI ladder, browser smoke (21 pages), and full serial Vitest (77 files / 361 tests). No stock/accounting posting/payment/purchasing/Alibaba/delivery/supplier-message side effects occurred.
- Implemented the workflow-first UI improvement recommendations from `var/hermes-ui-audit/ui-improvement-suggestions.md`: `/` Launch Next Action and buildable-now caption; `/tracking` ordered risk action list and Last Good Refresh; `/accounting` dominant bookkeeping workflow rail, prioritized source triage, and Evidence Only Until Apply guardrail; `/inventory/items` Stock Health chips; `/boms` Build Constraint mini-bars and launch-critical badge; `/incoming` receiving progress bars, packing-slip duplicate check, and primary human-count field; `/inventory/valuation` value concentration; `/inventory/movements` ledger guide/sticky SKU/signed chips; `/automation` severity grouping. Verified with targeted UI tests (9 files / 20 tests), typecheck, lint, full serial Vitest (75 files / 354 tests), 6144 MB Next build, 21-page browser smoke, and marker smoke across the 9 changed routes. No stock/accounting posting/payment/purchasing/Alibaba/delivery/supplier-message side effects occurred.
- Fixed `/accounting` retry OCR/manual re-analysis safety and completed the app-wide title-case sweep/UI graphics audit. Retry/manual OCR controls are hidden and service-rejected for applied/attached/linked accounting source documents; all 21 rendered routes now pass a title/button/label candidate audit with 0 title-case issues and 0 browser console/page errors. UI audit artifacts live under `var/hermes-ui-audit/`, including screenshots and `ui-improvement-suggestions.md`. Verified with lint, typecheck, full serial Vitest (75 files / 354 tests), 6144 MB build, and fresh rendered route audit. No stock/accounting posting/payment/purchasing/Alibaba/delivery/supplier-message side effects occurred.
- Run-now resume executed after canceling the 23:45 scheduled job: removed cron job `76a240ff00eb`, re-checked the crash-recovery/current Lambenti tasks from the current tree, and verified they are already implemented. Targeted run-now tests passed (8 files / 53 tests), `npm run typecheck`, `npm run lint`, full serial Vitest (75 files / 353 tests), 6144 MB Next build, HTTP smoke (21 pages / 5 UI contracts / 4 JSON APIs / 3 CSV exports), and browser-section smoke (21 pages) all passed. Targeted DOM/provider checks showed 5 local tracking rows / 0 local duplicates and live Ship24 5 active trackers / 5 unique tracking numbers / 0 duplicates. No stock, purchasing, accounting posting/payment, Alibaba capture/import, delivery confirmation, or supplier-message side effects were performed.
- Fixed the current OCR/tracking/order-email/supplier prompt. Accounting PDF extraction now OCRs directly embedded JPEG/JPX PDF image streams before requiring `pdftoppm`; `/tracking` Provider heartbeat owns the refresh button and uses persisted `nextRefreshAt` heartbeat data plus a yellow pending/no-update panel; `/integrations/alibaba-email` no longer shows the Direct Alibaba portal agent section; supplier display/dropdowns use stricter valid-identity parsing, canonical dedupe, embedded-company extraction, and invalid email/import supplier labels are no longer created or printed as supplier names. Verified targeted tests (5 files / 32 tests), full serial Vitest (75 files / 353 tests), typecheck, lint, 6144 MB build, production route smoke for `/tracking`, `/integrations/alibaba-email`, `/suppliers`, and live refresh API heartbeat. No stock/accounting/purchasing/Alibaba-capture/delivery/supplier-message side effects.

- Improved `/tracking` with a premium but useful visual command layer: `Live shipment radar` stage bars, open/due/failed radar numbers, a segmented `Risk spectrum`, `Provider heartbeat` conic status ring, and `Carrier mix` / `Evidence constellation` bars. Researched Linear/Vercel/Stripe/logistics dashboard patterns and translated them into dark-mode operational graphics rather than decoration. Verified targeted tracking page test, typecheck, lint, full serial Vitest (75 files / 348 tests), build, diff check, HTTP `/tracking` smoke, browser `/tracking` smoke with dark background/CSS/markers/no horizontal overflow, and 0 console/JS errors. No stock, purchasing, accounting, Alibaba capture/import, delivery confirmation, or supplier-message side effects occurred.
- Added expandable full package tracking data and a useful dashboard coverage graphic. `/tracking` active shipment cards and delivered-history rows now expose `Full package tracking data` disclosures with linked-order metadata, refresh schedule, complete carrier event timeline, raw provider status payload, and raw event payloads from existing `TrackingNumber`/`TrackingEvent` data. Root `/` now adds a read-only `Phase I coverage bridge` segmented bar for assembled units, buildable-now units, and remaining gap. Verified targeted tracking/dashboard tests (4 files / 23 tests), typecheck, lint, full serial Vitest (74 files / 345 tests), build, diff check, HTTP smokes for `/tracking` and `/`, and browser smokes with dark-mode CSS loaded / 0 console errors. No stock, purchasing, accounting, Alibaba capture/import, delivery confirmation, or supplier-message side effects occurred.
- Improved accounting usability for the current AP blocker workflow: `/accounting` now separates blocked-before-approval invoices from invoices ready for approval in the attention queue and daily bookkeeping routine; `/accounting/invoices` now splits received invoices into `Ready to approve`, `Fix before approval`, and `Payment queue`, shows blocker reasons, exposes inline `Set due date`, and hides approval buttons until blockers are clear. Added a service-layer approval guard that rejects supplier-invoice approval without source evidence before any AP journal is posted. Verified targeted accounting tests (4 files / 10 tests), lint, typecheck, full serial Vitest (74 files / 345 tests), build, HTTP/browser `/accounting` and `/accounting/invoices` smokes, and 0 browser console/JS errors. No live approval/payment/journal/stock/purchasing/Alibaba/supplier-message side effects were performed.
- Researched and improved `/tracking` using 5 local advisory research agents, 5 test-planning agents, and 5 independent check agents (saved under `var/hermes-tracking-agents/`) after Hermes `delegate_task` was initially blocked by the local Qwen context-window mismatch. Fixed the live Ship24 refresh failure for `7321315589070429`: root cause was Ship24 `tracker_conflict` from reusing the bare tracking row id as `clientTrackerId`; the adapter now sends a row-id-prefixed request-signature client tracker id and includes provider error messages. Added a scan-friendly `Shipment progress` rail to open shipment cards. Verified targeted tracking tests (2 files / 19 tests), lint, typecheck, full serial Vitest (74 files / 344 tests), build, live refresh success, scheduler JSON failed 0, HTTP/browser `/tracking` smoke, dark-mode visual smoke, and 0 browser console/JS errors. No stock/accounting/Alibaba/supplier-message side effects occurred. After the work, LM Studio Qwen was successfully reloaded at 65,536 context / parallel 1 / `--gpu 0.5` for future Hermes delegation.
- Updated the root dashboard Launch Control card: exact title `Phase I Launch Readiness`, removed the description paragraph under the title, changed the Phase I launch-readiness source target to 25 units, and updated visible launch metric/gauge/graph copy. Verified targeted dashboard tests, typecheck, lint, build, fresh local HTTP 200, browser visual/DOM smoke, and 0 console/JS errors.
- Adjusted the combined launch/value-mix graph card so Launch target meter and Value mix are stacked as full-width sections inside the same card, with Launch target meter first and Value mix below instead of a left/right split. Verified targeted dashboard tests, typecheck, lint, build, fresh local HTTP 200, browser visual/DOM layout smoke, and 0 console/JS errors.
- Adjusted the root dashboard layout per operator request: `Value mix` is now compactly embedded inside the `Launch target meter` graph card; the adjacent `Operations flow` card uses the same equal-height row sizing; `Open next actions` is collapsed by default; and `Useful visual signals` now appears above the 10 KPI stat-card grid. Verified targeted dashboard tests, lint, typecheck, build, fresh local HTTP 200, browser layout smoke, DOM bounding boxes, and 0 console/JS errors.
- Added useful, read-only graph cards to the root dashboard: launch target meter, package BOM bottleneck bars, low-stock pressure bars, operations-flow bars with existing workflow links, and ledger-derived valuation mix. Kept the design intentionally operational rather than decorative (`Useful visual signals` copy says no dashboard confetti), added `summarizeDashboardGraphs()` plus source/regression tests, and avoided new schema/chart dependencies/mutation flows. Verified targeted dashboard tests (2 files / 4 tests after final contrast patch), full lint/typecheck/serial Vitest (74 files / 343 tests)/build/diff-check gates, production `/` HTTP/CSS/browser smoke, dark-mode visual smoke, and 0 browser console/JS errors. No stock, purchasing, receiving, accounting, Alibaba, delivery-confirmation, or supplier-message side effects were performed.
- Implemented the simple Phase I launch-readiness dashboard slice: root `/` now shows a read-only 25-unit target summary with ready-now units (`assembled + buildable`), remaining gap, package BOM bottleneck, and linked next actions to existing human workflows (`/boms`, `/purchasing/recommendations`, `/incoming`, `/accounting/invoices`). Added `summarizeLaunchReadiness()` and tests for blocked/covered states. Used direct LM Studio `qwen2.5-coder-7b-instruct` as the local-model retry/review path; the centralized Lambenti Agent Control Plane still returned hallucinated JS-file names, so repo/DB/browser evidence drove implementation. Verified targeted dashboard tests (3 files / 8 tests), lint, typecheck, full serial Vitest (74 files / 342 tests), build, diff check, browser root smoke with 0 console/JS errors, and rendered browser smoke for all 21 pages. No stock, PO, purchase-request, invoice/payment, receiving, Alibaba, or supplier-message side effects were performed.
- Finalized the supplier/AP/purchasing/receiving app-improvement slice: archived 15 unreferenced Alibaba UI/import junk supplier rows under audit, voided 27 safe duplicate Winnie XU supplier-invoice rows under audit, kept duplicate detection clear afterward, enriched `/purchasing/recommendations` into a draft-PR queue, added a useful `/purchasing/requests` empty state, and added grouped duplicate-line receiving summaries without bulk receiving. Verified targeted tests (8 files / 28 tests), lint, typecheck, full serial Vitest (73 files / 340 tests), build, diff check, HTTP 200 smokes, rendered browser smokes for `/suppliers`, `/accounting/invoices`, `/purchasing/recommendations`, `/purchasing/requests`, and `/incoming`, and 0 browser console/JS errors. No stock receiving, order placement, invoice approval/payment, Alibaba capture/import, or supplier-message side effects were performed.
- Re-ran the app-wide improvement/hardening pass and added deterministic connectivity coverage: `npm run smoke` now verifies all 21 app sections, 5 UI contracts, 4 read-only JSON APIs, and 3 accounting CSV exports; `npm run smoke:browser` renders all 21 sections in Playwright and fails on console/page errors; route/section source tests prevent drift. Fixed verification-discovered drift by mounting the `/accounting/invoices` duplicate-invoice guardrail, wiring `/suppliers` cleanup candidates to a human-gated archive action, rejecting Alibaba UI/deep-link/legal-entity supplier junk, updating stale supplier smoke copy, and making browser smoke resilient to long-lived network requests. Verified with targeted tests, `npm run quality` (73 files / 340 tests), `npm run smoke`, `npm run smoke:browser`, npm audit prod/full at 0 vulnerabilities, and `git diff --check`.
- Hardened the app-wide security/code-cleanliness baseline: added Next.js security headers/CSP/HSTS/Permissions-Policy/COOP/CORP/no-sniff/frame-deny/referrer policy, API `Cache-Control: private, no-store`, source-contract tests for authenticated API routes/no query-string secrets/no high-risk dynamic rendering, removed app-authored inline `dangerouslySetInnerHTML` theme bootstrapping in favor of static `public/theme-init.js`, tightened loopback-only production auth against spoofed `Origin`/`X-Forwarded-Host`, updated dependency lock/overrides so `npm audit --omit=dev` and full `npm audit` both report 0 vulnerabilities, and cleaned docs placeholder secret examples. Verified targeted security/auth tests, full lint/typecheck/serial Vitest (69 files / 322 tests)/build, static dynamic-source and tracked-secret scans at 0 findings, HTTP route/header smokes, browser `/accounting`/`/tracking`/`/inventory/movements` smokes, and 0 browser console/JS errors. No stock/accounting/payment/receiving/Alibaba side effects were performed.
- Simplified and hardened `/tracking`: replaced the dense active-tracking table with an operator-first workbench (summary cards, `What needs attention`, scan-friendly open-shipment cards, explicit failed-refresh retry actions, and collapsed Add tracking / Delivered history / Advanced lead-time audit sections). Hardened display/status behavior by normalizing stored/provider statuses, mapping Ship24 `data`/information-received milestones to `PENDING`, hiding junk supplier labels, labeling unlinked imports as `Evidence import …`, and improving dark-theme orange/emerald/red contrast. Verified targeted tracking tests (2 files / 18 tests), lint, typecheck, full serial Vitest (66 files / 315 tests), build, HTTP `/tracking` smoke, browser smoke with CSS loaded / 2 active rows / 1 delivered row / 0 console/JS errors, and final dark-mode visual QA. No stock, accounting, Alibaba capture/import, delivery confirmation, or supplier-message side effects were performed; `7321315589070429` still surfaces as a Ship24 HTTP 400 refresh issue for operator review.
- Improved `/accounting` with a simple accounting command center modeled on accounting-app home dashboards: open payables, received/approved counts, overdue/due-next-7-day aging, bank-reconciliation unmatched totals, GL posting setup health, next supplier bills, and a daily bookkeeping routine. Added `/accounting/accounts` one-click default AP setup for safe starter accounts/mappings (1000 bank, 1060 tax recoverable, 1300 inventory, 2000 AP) and ran it once on the live local DB so posting setup is now ready. Added `/accounting/invoices` approval workbench with ready/missing-evidence/missing-due-date/payment metrics, primary-source evidence labels, visible approve-from-queue actions, review-readiness column, and audited due-date save action. Verified targeted accounting tests (7 files / 13 tests), lint, typecheck, full serial Vitest (66 files / 313 tests), build, HTTP `/accounting`/`/accounting/invoices`/`/accounting/accounts` smokes, browser invoice workbench smoke with 0 console/JS errors, and dark-mode visual legibility. Re-verified after context compression on 2026-06-15 08:00 EDT with lint, typecheck, full serial Vitest (66 files / 313 tests), build, HTTP smokes for `/accounting`/`/accounting/invoices`/`/accounting/accounts`/`/inventory/movements`, and browser invoice workbench smoke with 0 console/JS errors. No live invoice approval/payment/reconciliation/stock mutation was performed; only default GL setup and test fixtures mutated data.
- Added `/inventory/movements` signed ledger-impact and balance-after-entry columns so recent movement rows show per-item on-hand/available/reserved stock after each visible ledger entry. The balance computation uses full visible history for items in the recent table and still hides operator-voided originals plus `VOID:` compensating reversal rows. Verified targeted movement tests (2 files / 7 tests), lint, typecheck, full serial Vitest (62 files / 306 tests), build, HTTP smoke, and browser smoke with 0 console/JS errors. Central control-plane read-only audit was run but generic/hallucinated, so direct repo evidence drove the implementation.
- Added email-triggered shipment capture and lead-time learning: mailbox sync now detects shipment confirmations, extracts Alibaba order/detail/message URLs, launches targeted tracking-only Alibaba capture for those exact email links, backfills saved tracking evidence, links shipment tracking back to the initial-payment/applied PO by external Alibaba order ID, exposes a `/tracking` lead-time learning log with expandable per-item quantity/order/receipt/shipping samples, and feeds learned `leadTimeDays` into reorder automation findings/draft-PR rationale. Verified with lint, typecheck, targeted 6-file/56-test suite, full serial Vitest (62 files / 305 tests), build, HTTP `/tracking` smoke, browser `/tracking` smoke, and 0 browser console/JS errors. No live Alibaba capture, stock receiving, purchase approval, invoice/payment approval, or message send was triggered; temporary smoke server was stopped and port 5173 cleared.
- Removed the `/tracking` Automatic Alibaba capture card/action at the operator's request and repaired the Order Email Agent active queue. Root cause was tracking-only Alibaba portal Message Center/tracking snapshots being persisted as active `EmailOrderImport` rows and crowding out real supplier emails. Active `getEmailOrderImports()` / reassessment now exclude those tracking-only portal message/tracking evidence rows while preserving real matched order imports. Verified with targeted tracking/email tests (2 files / 16 tests), lint, typecheck, full serial Vitest (62 files / 299 tests), build, HTTP smokes for `/tracking` and `/integrations/email-import`, and browser smokes with 0 console/JS errors. Fresh `npm run start:local` is running on `127.0.0.1:5173` for review.
- Fixed Hermes readiness interruptions and resumed the interrupted `/tracking` work. Hermes patch: readiness polling no longer overlaps and no longer turns transient `setup.runtime_check` transport timeouts into a stale `Inference not ready` blocker; Windows log rollover now backs off on locked log files instead of emitting repeated logging tracebacks. Rebuilt Hermes desktop renderer for the release `dist`. Resumed Lambenti verification found and fixed current-tree drift: `src/modules/tracking/service.ts` lead-time typing/null narrowing now typechecks, and `/tracking` again exposes the `Capture Alibaba tracking` action/card wired to tracking-only portal capture, saved-evidence backfill, and old-row prune. Verified Hermes targeted readiness/logging checks, desktop typecheck/build, Lambenti targeted tracking tests, full `npm run quality` (62 files / 298 tests + build), HTTP `/tracking` 200, browser `/tracking` capture-card render, and 0 browser console/JS errors. No live Alibaba capture/import was triggered; the local app server remains running on `127.0.0.1:5173`.
- Fixed `/tracking` Capture Alibaba saved-row recognition/idempotency after the operator observed repeated Delivering → Completed & In Review cycles and an unnecessary Logistics Services detour: tracking-only capture now hydrates scan memory from persisted `TrackingNumber` rows before clicking Alibaba order candidates, reports saved rows/orders hydrated, treats saved order `304716450001023166` / tracking `888071620741` as `tracking-already-captured`, skips generic Alibaba Logistics Services marketplace links, and surfaces the new behavior in `/tracking` copy/action summaries. Proactively hydrated `var/alibaba-portal-agent-state.json`; verification showed `trackingRows:3`, `savedTrackingOrdersHydrated:1`, `rememberedOrders:8`, `order304TrackingNumbers:["888071620741"]`, and the Completed & In Review Track Package candidate skipped with reason `tracking-already-captured`. Verified with `node --check`, targeted tracking tests (3 files / 25 tests), full lint/typecheck/serial Vitest/build (62 files / 298 tests), HTTP/browser `/tracking` smoke, and 0 browser console/JS errors. No live Alibaba import/capture was triggered during this repair pass; no stock/invoice/payment/receiving side effects.
- Resumed and hardened `/tracking` Capture Alibaba stale-scan behavior after the user reported stale rechecks and skipped Completed & In Review entries. The portal agent now records no-tracking/null order attempts as checked, repairs order-memory id drift by trusting `order:<id>` keys, prioritizes/dedupes Track Package / Track Shipment(s) / logistics candidates, filters generic Alibaba logistics-services links, keeps following nested tracking controls, dedupes nested Message Center thread-card elements, and reports total remembered message threads correctly. Verified with `node --check`, targeted tracking tests (3 files / 23 tests), full lint/typecheck/serial Vitest/build (62 files / 296 tests), bounded controlled Alibaba dry-run (`configured:true`, `capturedSnapshots:3`, `ordersRemembered:8`, `messageThreadsRemembered:27`, dry-run/no import), and HTTP/browser `/tracking` smoke with 0 console errors.
- Fixed `/tracking` Capture Alibaba login-handoff regression after the user saw stale Work-profile wording and login-required output: clicked the current dedicated-profile trusted Alibaba `Yes` confirmation to complete login; Capture now passes the Lambenti email as trusted Alibaba account-confirmation fallback for the dedicated profile, fallback messages use the actual browser profile label instead of hard-coded Work-profile text, `/tracking` copy explains Continue as / Yes handoff, and the app capture timeout is now 420s because the full Delivering → Delivered → Message Center sweep currently takes ~170s dry-run. Verified dedicated dry-run and non-dry portal capture (`capturedSnapshots:12`, `imported:11`, `duplicates:1`), targeted 4-file/20-test suite, full lint/typecheck/serial Vitest/build (62 files / 288 tests), HTTP/browser smoke, and DB safety counts (`trackingRows:3`, `alibabaPortalImports:35`, `deliveredWithNextRefreshAt:0`, `stockMovementSideEffects:0`, `invoiceSideEffects:0`).
- Hardened `/tracking` Capture Alibaba for the required Delivering → Delivered → Messages sweep: tracking-only portal capture now checks Alibaba orders under the Delivering tab, checks Alibaba orders under the Delivered tab, then opens Message Center and individually clicks/scrolls each visible supplier conversation for shipping/tracking evidence. Tracking-only mode skips invoice download passes for speed, uses shorter tracking-specific waits, imports up to 200 evidence snapshots, preserves `conversationContext`, updates `/tracking` copy/action summary, keeps read-only/no-send/no-auto-apply/no-auto-invoice boundaries, and keeps delivered rows at `nextRefreshAt:null`. Verified with RED/GREEN targeted checks, full lint/typecheck/serial Vitest/build gate (62 files / 282 tests), HTTP/browser `/tracking` smoke, portal dry-run showing orders-first handoff, and DB side-effect checks (`stockMovementSideEffects:0`, `invoiceSideEffects:0`). Full controlled scrape remains deferred until the Work Chrome profile is closed or manual CAPTCHA/2FA/security handoff is completed.
- Hardened `/tracking` Capture Alibaba for the screenshot Message Center flow: tracking-only portal capture now opens `https://message.alibaba.com/message/messenger.htm`, scrolls/reads each visible supplier conversation thread individually, saves only shipment/tracking-relevant `conversationContext` from each message section, raises the portal import batch cap to 200 snapshots, updates `/tracking` operator copy, preserves read-only/no-send/no-auto-apply/no-auto-invoice boundaries, and keeps delivered rows at `nextRefreshAt:null` during capture backfill. Verified with targeted 7-file/41-test suite, full lint/typecheck/serial Vitest/build gate (62 files / 280 tests), HTTP/browser `/tracking` smoke, portal dry-run showing the messenger URL, and DB side-effect checks (`stockMovementSideEffects:0`, `invoiceSideEffects:0`). Full controlled scrape remains deferred until the Work Chrome profile is closed or manual CAPTCHA/2FA/security handoff is completed.
- Split delivered tracking out of the active `/tracking` list while retaining history: active table now shows only non-delivered rows with refresh actions; delivered records move to `Delivered tracking history` with linked order/source/latest event and total ship time computed from earliest carrier event to delivered time. Cleared stale `nextRefreshAt` for delivered `1Z675EW60490310023` with an audit log, retained events/status evidence, verified active rows 1 / delivered rows 1 in browser, total ship time `3d 17h`, full gates passed, and no stock/invoice side effects.
- Hardened `/tracking` Capture Alibaba for the current request: tracking-only capture now runs `--deep --recent-months=3`, scrolls lazy-loaded order/message surfaces before marking safe candidates, rejects send/reply/inquiry/chat controls, preserves shipment-relevant `conversationContext`, skips/prunes Alibaba tracking rows older than 3 months, removed old 2019 tracking row `LL270153423CN` from active tracking, and verified `/tracking` now shows 2 current rows with Ship24 configured, old row absent, no console errors, and no stock/invoice side effects. Controlled portal scraping still needs the Work Chrome profile closed because Chrome reported that profile already open.
- Installed the user-provided Ship24 API token into ignored `.env` (secret not echoed), restarted the local app, and verified live refresh: `npm run agent:tracking-refresh -- --json` returned configured true / scanned 3 / refreshed 3 / failed 0; browser `/tracking` shows `SHIP24 configured`, due refresh 0, delivered 1, needs config 0, failed 0; DB rows are refresh `SUCCESS`; no stock movement or supplier-invoice side effects were found.
- Wired the `/tracking` Capture Alibaba action to the automatic portal tracking collector: the server action now runs `scripts/alibaba-portal-agent.mjs --json --tracking-only`, then backfills saved Alibaba/email evidence. Tracking-only portal imports force `autoApply:false` and `autoCreateInvoices:false`, use actor `alibaba-tracking-capture-agent`, and keep default portal-agent full-import behavior unchanged. The Work Chrome fallback now handles both already-open profile and Chrome default-profile DevTools refusal by opening Alibaba orders in the configured Work profile and attempting saved `Continue as Musashi` / trusted Alibaba `Yes` assists before returning a manual controlled-scrape handoff. Verified with targeted tests, full gates, runtime CLI smoke, HTTP/browser `/tracking` smoke, and DB evidence of 3 saved tracking rows; no stock/invoice/payment/receiving side effects.
- Researched and implemented the default tracking provider setup: chose Ship24 for Lambenti's low-volume Alibaba/China-origin + UPS tracking needs, added a Ship24 provider adapter (`POST /public/v1/trackers/track`, bearer auth, `clientTrackerId`, `shipmentReference`, CA destination default, event/status normalization), preserved custom-HTTP fallback, updated `.env.example`/ignored `.env` non-secret defaults, `/tracking` setup copy, local runbook, and `docs/tracking-provider-research.md`. Verified with targeted Ship24/scheduler/page tests, `npm run quality` (60 files / 263 tests / build), scheduler skip smoke, HTTP `/tracking` smoke, browser render/console smoke, and no stock/payment/receiving side effects.
- Integrated tracking workbench: added evidence-only `TrackingNumber`/`TrackingEvent` persistence, automatic Alibaba portal/email tracking capture, source/order/import/PO linking, `/tracking` page, `/api/tracking/refresh`, local loopback auto-refresh support, quiet scheduler hook `npm run agent:tracking-refresh`, configurable tracking-service JSON refresh via `LAMBENTI_TRACKING_STATUS_URL_TEMPLATE`, sidebar/revalidation wiring, `var/**` lint/test ignores, `.env.example` placeholders/runbook notes, and data backfill for `888071620741`, `LL270153423CN`, and `1Z675EW60490310023`. Verified with RED/GREEN targeted tracking/scheduler tests, full `npm run quality` (60 serial files / 262 tests / build), HTTP `/tracking` + API smoke, browser render smoke, and no stock/payment/receiving side effects.
- Hardened Alibaba portal browser-agent navigation/tracking: added tested extraction/navigation/browser-launch/login-assist helper coverage, read-only candidate allow/deny filters, broader CAPTCHA/security/2FA detection, tracking-number preservation through the portal import API, distinct message-thread IDs, manual handoff propagation for challenges encountered during detail/invoice navigation, an about:blank/Work-profile-already-open login fallback that opens the Alibaba orders URL in the existing Chrome profile, saved Google login assist that waits for/clicks `Continue as Musashi`, trusted-account Alibaba `Yes` confirmation click for `team@lambenti.com`, and a verified read-only UIAutomation tracking scan path for Chrome's default-profile DevTools restriction. Live tracking check scanned 13 Alibaba orders and saved found tracking evidence (`888071620741`, `LL270153423CN`, plus UPS email `1Z675EW60490310023`) through portal import/audit/import-evidence persistence with no stock/payment/receiving side effects.
- Fixed LED strip supplier persistence: email order apply/reassessment now preserves manually selected item preferred suppliers, archive integration tests snapshot/restore protected LED seed items instead of deleting their temporary test suppliers out from under them, and the live LED strip rows were restored to audited supplier `jason zhou` plus real applied-order cost provenance with repair audit logs and no stock/accounting side effects.
- Verified LED supplier fix with RED regression, targeted preservation tests, full email archive suite (14 tests), lint, typecheck, serial Vitest (52 files / 229 tests), production build, DB confirmation, and browser DOM smoke of both LED edit dialogs selecting `jason zhou`.
- Improved `/accounting` usability/effectiveness: added an Attention queue, source-document triage metrics, per-document readiness badges, and a concise control trail so the operator sees exceptions/next actions before raw feature cards and tables while preserving no-stock/no-payment/no-approval automation boundaries.
- Patched dark-theme utility overrides for the new accounting status/triage color classes after browser smoke found unreadable blue/100/900 variants in dark mode.
- Verified the accounting UX pass with RED page test, targeted accounting tests (3 files / 13 tests), full `npm run quality` (lint/typecheck/52 serial test files/227 tests/build), HTTP `/accounting`/`/accounting/invoices`/`/accounting/payments` smoke, and browser dark-mode render/console checks.
- Implemented approved purchase request → draft purchase order conversion: service enforces purchasing permission, approved status, supplier, unit-price evidence, and no receiving side effects; `/purchasing/requests` now exposes `Create draft PO` and linked PO visibility; tests/browser smoke verified conversion and no stock movement.
- Made the local Postgres port fix durable: `docker-compose.yml` now defaults the DB host port to 55432 via `LAMBENTI_DB_HOST_PORT`, `.env.example` and ignored `.env` align on `localhost:55432`, `docs/local-runbook.md` documents the verified path, and `lambenti-postgres` was recreated as a compose-managed healthy `db` service on `55432:5432` using the same named volume.
- Configured the Lambenti profile's two-tier orchestration default: Hermes/GPT-5.5 remains controller and `delegate_task` children default to local LM Studio Qwen (`qwen2.5-coder-7b-instruct`) at 32,768 context / parallel=2.
- Added inventory-repo orchestration context (`.hermes.md`, AGENTS.md updates, `docs/hermes-two-tier-orchestration.md`) and a local-worker diagnostic script (`npm run agent:local-worker-smoke`).
- Updated `hermes-llm-orchestrator` routing/tuning and the centralized Lambenti Agent Control Plane to use LM Studio Qwen instead of the stale Ollama defaults.
- Verified Hermes config v29, LM Studio `/v1/models`, `lms ps` 32768/parallel=2, inventory local-worker smoke, inventory lint, orchestrator py_compile/pytest/local smoke, and Lambenti control-plane py_compile/local smoke.
- Note: the already-running Hermes GUI session caches delegation config from startup; restart/new session is needed before this chat's `delegate_task` tool itself uses local Qwen by default.
- Fixed current inventory/accounting operator requests: item edit preferred-supplier dropdown persistence, guarded `/accounting` source-document deletion, dashboard stale email catalog-matching notice removal, finished-good purchase/reorder exclusion, minimalist archive-supplier control, and Alibaba portal Work Chrome profile automation improvements with CAPTCHA-safe handoff.
- Verified the pass with targeted tests (10 files / 54 tests), Alibaba portal tests (2 files / 7 tests), full lint/typecheck/serial Vitest/build gates (52 files / 223 tests), HTTP route smoke, browser console/UI smoke, diff check, and redacted secret-pattern scan.
- Implemented posted balanced AP journal entries: migration `20260609012000_accounting_journal_entries`, source-linked/idempotent `JournalEntry` + `JournalEntryLine`, account snapshots, AP invoice approval posting, AP payment reconciliation posting, journal dashboard, CSV export API, and GL mapping setup guidance.
- Polished payment/accounting operator flows: `/accounting/journals` trial balance/ledger, `/accounting` posted-journal link, `/accounting/invoices` journal visibility and approve/pay copy, `/accounting/payments` approved-vs-received invoice split plus view-only mutation gating, and `/accounting/accounts` required default journal mappings.
- Verified journals/polish with RED→GREEN targeted tests, lint, typecheck, serial Vitest, build, HTTP route checks, browser render smoke, and journal CSV header smoke.
- Implemented full automatic-accounting expansion: supplier-scoped invoice identity, multiple invoices per PO, bank/payment reconciliation, AR/customer invoices, GST/HST CSV exports, GL account mapping metadata, and read-only landed-cost allocation reports.
- Added migrations `20260609001000_accounting_expansion` and `20260609004000_customer_invoices` and regenerated Prisma Client after clearing a stale Windows query-engine DLL lock from the local `5173` Node process.
- Added `/accounting/customer-invoices`, `/accounting/payments`, `/accounting/exports`, `/accounting/accounts`, `/accounting/landed-cost`, CSV export APIs, and invoice-ledger UI for staged invoice numbers/totals and payment allocation totals.
- Verified new accounting behavior with RED→GREEN integration tests, lint, typecheck, serial Vitest, build, HTTP route checks, browser render smoke, and CSV header smoke.
- Implemented automatic-accounting evidence bundles: documents can be attached to existing supplier invoice/PO evidence without posting AP, receiving stock, or marking paid; invoice matching now suggests existing bundles by hash/PO/supplier+invoice number; `/accounting/invoices` shows evidence bundle counts/downloads; upload refreshes the review queue.
- Hardened `/accounting` document readability after the two failed uploads: added JS OCR fallback, scanned-PDF page OCR, retry/manual text re-analysis controls, reviewable unreadable-document records, OCR-noise parsing fixes, and reprocessed the two source documents into usable `NEEDS_REVIEW` evidence rows.
- Built `/accounting` workbench for serious accounting records: drag/drop source uploads, private storage, SHA-256 dedupe, OCR/text/PDF/email extraction, structured analysis, review queue, download route, and apply-to-supplier-invoice controls with no inventory receiving side effects.
- Added `AccountingDocument` Prisma model/migration and service tests proving upload/analyze does not create operational records and apply-to-invoice leaves PO received quantity/stock movements unchanged.
- Improved invoice creation from reviewed source documents by accepting invoice number, dates, due date, source totals, currency, and document provenance; ran lint, typecheck, serial Vitest, build, and browser smoke with temporary upload cleanup.
- Fixed Order Email Agent line detection for similar Alibaba product-card rows: explicit card segments now stay distinct even when quantity/price and description tokens overlap.
- Added regression coverage for order `304716450001023166` shape and refreshed that existing import from 2 stored lines to 3 with no stock movement.
- Ran email parser/import tests, full `npm run quality`, and browser-smoked `/integrations/email-import` showing `Lines: 3` for the affected order.
- Researched recent Lambenti sessions and profile state with two read-only agents.
- Updated Lambenti profile skills/references for smarter context routing, current app briefing, bounded delegation, and verification ladder defaults.
- Updated memory/user profile with compact Lambenti operating facts and current app rules.
- Tuned Hermes lambenti profile tool-output and loop-guard settings.
- Pinned Lambenti skills in curator so core domain skills are not auto-archived.
- Updated weekly inventory app health cron prompt to use lint, typecheck, serial tests, and build.
- Updated app repo `AGENTS.md` and `package.json` so future agents use serial Vitest and the Lambenti operator protocol.
- Disabled macOS-only `computer_use` in the Windows Lambenti CLI profile to reduce irrelevant tool-schema/prompt overhead without removing core Lambenti engineering tools.
- Tightened Lambenti skill/audit wording so future agents use `npm run test:serial` / `npm run quality` and know which toolsets to keep.

## In progress
- None.

## Backlog candidates from research
- Customer/AR journal posting (accounts receivable, revenue, tax payable, customer payment/cash) remains future accounting scope; current posted journals cover AP invoice approval and AP payment reconciliation.
- Alibaba automatic ordering remains future scope: use the Work Chrome profile and existing portal reader/session handoff first, then add explicit human approval/payment controls before any supplier order placement.
- Production unit serials, QA logs, and packaging stages.
- Richer movement audit/running-balance view.
- Consider adding inline action-state messages to `/suppliers` item sourcing saves if operators need friendlier custom-supplier validation feedback.
