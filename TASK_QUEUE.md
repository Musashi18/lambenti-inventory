# TASK_QUEUE

## Done
- Added email-triggered shipment capture and lead-time learning: mailbox sync now detects shipment confirmations, extracts Alibaba order/detail/message URLs, launches targeted tracking-only Alibaba capture for those exact email links, backfills saved tracking evidence, links shipment tracking back to the initial-payment/applied PO by external Alibaba order ID, exposes a `/tracking` lead-time learning log with expandable per-item quantity/order/receipt/shipping samples, and feeds learned `leadTimeDays` into reorder automation findings/draft-PR rationale. Verified with lint, typecheck, targeted 6-file/56-test suite, full serial Vitest (62 files / 305 tests), build, HTTP `/tracking` smoke, browser `/tracking` smoke, and 0 browser console/JS errors. No live Alibaba capture, stock receiving, purchase approval, invoice/payment approval, or message send was triggered; temporary smoke server was stopped and port 5173 cleared.
- Removed the `/tracking` Automatic Alibaba capture card/action at the operator's request and repaired the Order Email Agent active queue. Root cause was tracking-only Alibaba portal Message Center/tracking snapshots being persisted as active `EmailOrderImport` rows and crowding out real supplier emails. Active `getEmailOrderImports()` / reassessment now exclude those tracking-only portal message/tracking evidence rows while preserving real matched order imports. Verified with targeted tracking/email tests (2 files / 16 tests), lint, typecheck, full serial Vitest (62 files / 299 tests), build, HTTP smokes for `/tracking` and `/integrations/email-import`, and browser smokes with 0 console/JS errors. Fresh `npm run start:local` is running on `127.0.0.1:5173` for review.
- Fixed Hermes readiness interruptions and resumed the interrupted `/tracking` work. Hermes patch: readiness polling no longer overlaps and no longer turns transient `setup.runtime_check` transport timeouts into a stale `Inference not ready` blocker; Windows log rollover now backs off on locked log files instead of emitting repeated logging tracebacks. Rebuilt Hermes desktop renderer for the release `dist`. Resumed Lambenti verification found and fixed current-tree drift: `src/modules/tracking/service.ts` lead-time typing/null narrowing now typechecks, and `/tracking` again exposes the `Capture Alibaba tracking` action/card wired to tracking-only portal capture, saved-evidence backfill, and old-row prune. Verified Hermes targeted readiness/logging checks, desktop typecheck/build, Lambenti targeted tracking tests, full `npm run quality` (62 files / 298 tests + build), HTTP `/tracking` 200, browser `/tracking` capture-card render, and 0 browser console/JS errors. No live Alibaba capture/import was triggered; the local app server remains running on `127.0.0.1:5173`.
- Fixed `/tracking` Capture Alibaba saved-row recognition/idempotency after the operator observed repeated Delivering → Completed & In Review cycles and an unnecessary Logistics Services detour: tracking-only capture now hydrates scan memory from persisted `TrackingNumber` rows before clicking Alibaba order candidates, reports saved rows/orders hydrated, treats saved order `304716450001023166` / tracking `888071620741` as `tracking-already-captured`, skips generic Alibaba Logistics Services marketplace links, and surfaces the new behavior in `/tracking` copy/action summaries. Proactively hydrated `var/alibaba-portal-agent-state.json`; verification showed `trackingRows:3`, `savedTrackingOrdersHydrated:1`, `rememberedOrders:8`, `order304TrackingNumbers:["888071620741"]`, and the Completed & In Review Track Package candidate skipped with reason `tracking-already-captured`. Verified with `node --check`, targeted tracking tests (3 files / 25 tests), full lint/typecheck/serial Vitest/build (62 files / 298 tests), HTTP/browser `/tracking` smoke, and 0 browser console/JS errors. No live Alibaba import/capture was triggered during this repair pass; no stock/invoice/payment/receiving side effects.
- Resumed and hardened `/tracking` Capture Alibaba stale-scan behavior after the user reported stale rechecks and skipped Completed & In Review entries. The portal agent now records no-tracking/null order attempts as checked, repairs order-memory id drift by trusting `order:<id>` keys, prioritizes/dedupes Track Package / Track Shipment(s) / logistics candidates, filters generic Alibaba logistics-services links, keeps following nested tracking controls, dedupes nested Message Center thread-card elements, and reports total remembered message threads correctly. Verified with `node --check`, targeted tracking tests (3 files / 23 tests), full lint/typecheck/serial Vitest/build (62 files / 296 tests), bounded controlled Alibaba dry-run (`configured:true`, `capturedSnapshots:3`, `ordersRemembered:8`, `messageThreadsRemembered:27`, dry-run/no import), and HTTP/browser `/tracking` smoke with 0 console errors.
- Fixed `/tracking` Capture Alibaba login-handoff regression after the user saw stale Work-profile wording and login-required output: clicked the current dedicated-profile trusted Alibaba `Yes` confirmation to complete login; Capture now passes the Lambenti email as trusted Alibaba account-confirmation fallback for the dedicated profile, fallback messages use the actual browser profile label instead of hard-coded Work-profile text, `/tracking` copy explains Continue as / Yes handoff, and the app capture timeout is now 420s because the full Delivering → Delivered → Message Center sweep currently takes ~170s dry-run. Verified dedicated dry-run and non-dry portal capture (`capturedSnapshots:12`, `imported:11`, `duplicates:1`), targeted 4-file/20-test suite, full lint/typecheck/serial Vitest/build (62 files / 288 tests), HTTP/browser smoke, and DB safety counts (`trackingRows:3`, `alibabaPortalImports:35`, `deliveredWithNextRefreshAt:0`, `stockMovementSideEffects:0`, `invoiceSideEffects:0`).
- Hardened `/tracking` Capture Alibaba for the required Delivering → Delivered → Messages sweep: tracking-only portal capture now checks Alibaba orders under the Delivering tab, checks Alibaba orders under the Delivered tab, then opens Message Center and individually clicks/scrolls each visible supplier conversation for shipping/tracking evidence. Tracking-only mode skips invoice download passes for speed, uses shorter tracking-specific waits, imports up to 200 evidence snapshots, preserves `conversationContext`, updates `/tracking` copy/action summary, keeps read-only/no-send/no-auto-apply/no-auto-invoice boundaries, and keeps delivered rows at `nextRefreshAt:null`. Verified with RED/GREEN targeted checks, full lint/typecheck/serial Vitest/build gate (62 files / 282 tests), HTTP/browser `/tracking` smoke, portal dry-run showing orders-first handoff, and DB side-effect checks (`stockMovementSideEffects:0`, `invoiceSideEffects:0`). Full controlled scrape remains deferred until the Work Chrome profile is closed or manual CAPTCHA/2FA/security handoff is completed.
- Hardened `/tracking` Capture Alibaba for the screenshot Message Center flow: tracking-only portal capture now opens `https://message.alibaba.com/message/messenger.htm`, scrolls/reads each visible supplier conversation thread individually, saves only shipment/tracking-relevant `conversationContext` from each message section, raises the portal import batch cap to 200 snapshots, updates `/tracking` operator copy, preserves read-only/no-send/no-auto-apply/no-auto-invoice boundaries, and keeps delivered rows at `nextRefreshAt:null` during capture backfill. Verified with targeted 7-file/41-test suite, full lint/typecheck/serial Vitest/build gate (62 files / 280 tests), HTTP/browser `/tracking` smoke, portal dry-run showing the messenger URL, and DB side-effect checks (`stockMovementSideEffects:0`, `invoiceSideEffects:0`). Full controlled scrape remains deferred until the Work Chrome profile is closed or manual CAPTCHA/2FA/security handoff is completed.
- Split delivered tracking out of the active `/tracking` list while retaining history: active table now shows only non-delivered rows with refresh actions; delivered records move to `Delivered tracking history` with linked order/source/latest event and total ship time computed from earliest carrier event to delivered time. Cleared stale `nextRefreshAt` for delivered `1Z675EW60490310023` with an audit log, retained events/status evidence, verified active rows 1 / delivered rows 1 in browser, total ship time `3d 17h`, full gates passed, and no stock/invoice side effects.
- Hardened `/tracking` Capture Alibaba for the current request: tracking-only capture now runs `--deep --recent-months=3`, scrolls lazy-loaded order/message surfaces before marking safe candidates, rejects send/reply/inquiry/chat controls, preserves shipment-relevant `conversationContext`, skips/prunes Alibaba tracking rows older than 3 months, removed old 2019 tracking row `LL270153423CN` from active tracking, and verified `/tracking` now shows 2 current rows with Ship24 configured, old row absent, no console errors, and no stock/invoice side effects. Controlled portal scraping still needs the Work Chrome profile closed because Chrome reported that profile already open.
- Installed the user-provided Ship24 API token into ignored `.env` (secret not echoed), restarted the local app, and verified live refresh: `npm run agent:tracking-refresh -- --json` returned configured true / scanned 3 / refreshed 3 / failed 0; browser `/tracking` shows `SHIP24 configured`, due refresh 0, delivered 1, needs config 0, failed 0; DB rows are refresh `SUCCESS`; no stock movement or supplier-invoice side effects were found.
- Wired the `/tracking` Capture Alibaba action to the automatic portal tracking collector: the server action now runs `scripts/alibaba-portal-agent.mjs --json --tracking-only`, then backfills saved Alibaba/email evidence. Tracking-only portal imports force `autoApply:false` and `autoCreateInvoices:false`, use actor `alibaba-tracking-capture-agent`, and keep default portal-agent full-import behavior unchanged. The Work Chrome fallback now handles both already-open profile and Chrome default-profile DevTools refusal by opening Alibaba orders in the configured Work profile and attempting saved `Continue as Musashi` / trusted Alibaba `Yes` assists before returning a manual controlled-scrape handoff. Verified with targeted tests, full gates, runtime CLI smoke, HTTP/browser `/tracking` smoke, and DB evidence of 3 saved tracking rows; no stock/invoice/payment/receiving side effects.
- Researched and implemented the default tracking provider setup: chose Ship24 for Lambenti's low-volume Alibaba/China-origin + UPS tracking needs, added a Ship24 provider adapter (`POST /public/v1/trackers/track`, bearer auth, `clientTrackerId`, `shipmentReference`, CA destination default, event/status normalization), preserved custom-HTTP fallback, updated `.env.example`/ignored `.env` non-secret defaults, `/tracking` setup copy, local runbook, and `docs/tracking-provider-research.md`. Verified with targeted Ship24/scheduler/page tests, `npm run quality` (60 files / 263 tests / build), scheduler skip smoke, HTTP `/tracking` smoke, browser render/console smoke, and no stock/payment/receiving side effects.
- Integrated tracking workbench: added evidence-only `TrackingNumber`/`TrackingEvent` persistence, automatic Alibaba portal/email tracking capture, source/order/import/PO linking, `/tracking` page, `/api/tracking/refresh`, local loopback auto-refresh support, quiet scheduler hook `npm run agent:tracking-refresh`, configurable tracking-service JSON refresh via `LAMBENTI_TRACKING_STATUS_URL_TEMPLATE`, sidebar/revalidation wiring, `var/**` lint/test ignores, `.env.example` placeholders/runbook notes, and data backfill for `888071620741`, `LL270153423CN`, and `1Z675EW60490310023`. Verified with RED/GREEN targeted tracking/scheduler tests, full `npm run quality` (60 serial files / 262 tests / build), HTTP `/tracking` + API smoke, browser render smoke, and no stock/payment/receiving side effects.
- Hardened Alibaba portal browser-agent navigation/tracking: added tested extraction/navigation/browser-launch/login-assist helper coverage, read-only candidate allow/deny filters, broader CAPTCHA/security/2FA detection, tracking-number preservation through the portal import API, distinct message-thread IDs, manual handoff propagation for challenges encountered during detail/invoice navigation, an about:blank/Work-profile-already-open login fallback that opens the Alibaba orders URL in the existing Chrome profile, saved Google login assist that waits for/clicks `Continue as Musashi`, trusted-account Alibaba `Yes` confirmation click for `team@lambenti.com`, and a verified read-only UIAutomation tracking scan path for Chrome's default-profile DevTools restriction. Live tracking check scanned 13 Alibaba orders and saved found tracking evidence (`888071620741`, `LL270153423CN`, plus UPS email `1Z675EW60490310023`) through portal import/audit/import-evidence persistence with no stock/payment/receiving side effects.
- Fixed LED strip supplier persistence: email order apply/reassessment now preserves manually selected item preferred suppliers, archive integration tests snapshot/restore protected LED seed items instead of deleting their temporary test suppliers out from under them, and the live LED strip rows were restored to audited supplier `jason zhou` plus real applied-order cost provenance with repair audit logs and no stock/accounting side effects.
- Verified LED supplier fix with RED regression, targeted preservation tests, full email archive suite (14 tests), lint, typecheck, serial Vitest (52 files / 229 tests), production build, DB confirmation, and browser DOM smoke of both LED edit dialogs selecting `jason zhou`.
- Improved `/accounting` usability/effectiveness: added an Attention queue, source-document triage metrics, per-document readiness badges, and a concise control trail so the operator sees exceptions/next actions before raw feature cards and tables while preserving no-stock/no-payment/no-approval automation boundaries.
- Patched dark-theme utility overrides for the new accounting status/triage color classes after browser smoke found unreadable blue/100/900 variants in dark mode.
- Verified the accounting UX pass with RED page test, targeted accounting tests (3 files / 13 tests), full `npm run quality` (lint/typecheck/52 serial test files/227 tests/build), HTTP `/accounting`/`/accounting/invoices`/`/accounting/payments` smoke, and browser dark-mode render/console checks.
- Implemented approved purchase request → draft purchase order conversion: service enforces purchasing permission, approved status, supplier, unit-price evidence, and no receiving side effects; `/purchasing/requests` now exposes `Create draft PO` and linked PO visibility; tests/browser smoke verified conversion and no stock movement.
- Made the local Postgres port fix durable: `docker-compose.yml` now defaults the DB host port to 55432 via `LAMBENTI_DB_HOST_PORT`, `.env.example` and ignored `.env` align on `localhost:55432`, `docs/local-runbook.md` documents the verified path, and `lambenti-postgres` was recreated as a compose-managed healthy `db` service on `55432:5432` using the same named volume.
- Configured the Lambenti profile's two-tier orchestration default: Hermes/GPT-5.5 remains controller and `delegate_task` children default to local LM Studio Qwen (`qwen2.5-coder-7b-instruct`) at 32,768 context / parallel=2.
- Added inventory-repo orchestration context (`.hermes.md`, AGENTS.md updates, `docs/hermes-two-tier-orchestration.md`) and a local-worker diagnostic script (`npm run agent:local-worker-smoke`).
- Updated `hermes-llm-orchestrator` routing/tuning and the centralized Lambenti Agent Control Plane to use LM Studio Qwen instead of the stale Ollama defaults.
- Verified Hermes config v29, LM Studio `/v1/models`, `lms ps` 32768/parallel=2, inventory local-worker smoke, inventory lint, orchestrator py_compile/pytest/local smoke, and Lambenti control-plane py_compile/local smoke.
- Note: the already-running Hermes GUI session caches delegation config from startup; restart/new session is needed before this chat's `delegate_task` tool itself uses local Qwen by default.
- Fixed current inventory/accounting operator requests: item edit preferred-supplier dropdown persistence, guarded `/accounting` source-document deletion, dashboard stale email catalog-matching notice removal, finished-good purchase/reorder exclusion, minimalist archive-supplier control, and Alibaba portal Work Chrome profile automation improvements with CAPTCHA-safe handoff.
- Verified the pass with targeted tests (10 files / 54 tests), Alibaba portal tests (2 files / 7 tests), full lint/typecheck/serial Vitest/build gates (52 files / 223 tests), HTTP route smoke, browser console/UI smoke, diff check, and redacted secret-pattern scan.
- Implemented posted balanced AP journal entries: migration `20260609012000_accounting_journal_entries`, source-linked/idempotent `JournalEntry` + `JournalEntryLine`, account snapshots, AP invoice approval posting, AP payment reconciliation posting, journal dashboard, CSV export API, and GL mapping setup guidance.
- Polished payment/accounting operator flows: `/accounting/journals` trial balance/ledger, `/accounting` posted-journal link, `/accounting/invoices` journal visibility and approve/pay copy, `/accounting/payments` approved-vs-received invoice split plus view-only mutation gating, and `/accounting/accounts` required default journal mappings.
- Verified journals/polish with RED→GREEN targeted tests, lint, typecheck, serial Vitest, build, HTTP route checks, browser render smoke, and journal CSV header smoke.
- Implemented full automatic-accounting expansion: supplier-scoped invoice identity, multiple invoices per PO, bank/payment reconciliation, AR/customer invoices, GST/HST CSV exports, GL account mapping metadata, and read-only landed-cost allocation reports.
- Added migrations `20260609001000_accounting_expansion` and `20260609004000_customer_invoices` and regenerated Prisma Client after clearing a stale Windows query-engine DLL lock from the local `5173` Node process.
- Added `/accounting/customer-invoices`, `/accounting/payments`, `/accounting/exports`, `/accounting/accounts`, `/accounting/landed-cost`, CSV export APIs, and invoice-ledger UI for staged invoice numbers/totals and payment allocation totals.
- Verified new accounting behavior with RED→GREEN integration tests, lint, typecheck, serial Vitest, build, HTTP route checks, browser render smoke, and CSV header smoke.
- Implemented automatic-accounting evidence bundles: documents can be attached to existing supplier invoice/PO evidence without posting AP, receiving stock, or marking paid; invoice matching now suggests existing bundles by hash/PO/supplier+invoice number; `/accounting/invoices` shows evidence bundle counts/downloads; upload refreshes the review queue.
- Hardened `/accounting` document readability after the two failed uploads: added JS OCR fallback, scanned-PDF page OCR, retry/manual text re-analysis controls, reviewable unreadable-document records, OCR-noise parsing fixes, and reprocessed the two source documents into usable `NEEDS_REVIEW` evidence rows.
- Built `/accounting` workbench for serious accounting records: drag/drop source uploads, private storage, SHA-256 dedupe, OCR/text/PDF/email extraction, structured analysis, review queue, download route, and apply-to-supplier-invoice controls with no inventory receiving side effects.
- Added `AccountingDocument` Prisma model/migration and service tests proving upload/analyze does not create operational records and apply-to-invoice leaves PO received quantity/stock movements unchanged.
- Improved invoice creation from reviewed source documents by accepting invoice number, dates, due date, source totals, currency, and document provenance; ran lint, typecheck, serial Vitest, build, and browser smoke with temporary upload cleanup.
- Fixed Order Email Agent line detection for similar Alibaba product-card rows: explicit card segments now stay distinct even when quantity/price and description tokens overlap.
- Added regression coverage for order `304716450001023166` shape and refreshed that existing import from 2 stored lines to 3 with no stock movement.
- Ran email parser/import tests, full `npm run quality`, and browser-smoked `/integrations/email-import` showing `Lines: 3` for the affected order.
- Researched recent Lambenti sessions and profile state with two read-only agents.
- Updated Lambenti profile skills/references for smarter context routing, current app briefing, bounded delegation, and verification ladder defaults.
- Updated memory/user profile with compact Lambenti operating facts and current app rules.
- Tuned Hermes lambenti profile tool-output and loop-guard settings.
- Pinned Lambenti skills in curator so core domain skills are not auto-archived.
- Updated weekly inventory app health cron prompt to use lint, typecheck, serial tests, and build.
- Updated app repo `AGENTS.md` and `package.json` so future agents use serial Vitest and the Lambenti operator protocol.
- Disabled macOS-only `computer_use` in the Windows Lambenti CLI profile to reduce irrelevant tool-schema/prompt overhead without removing core Lambenti engineering tools.
- Tightened Lambenti skill/audit wording so future agents use `npm run test:serial` / `npm run quality` and know which toolsets to keep.

## In progress
- None.

## Backlog candidates from research
- Customer/AR journal posting (accounts receivable, revenue, tax payable, customer payment/cash) remains future accounting scope; current posted journals cover AP invoice approval and AP payment reconciliation.
- Alibaba automatic ordering remains future scope: use the Work Chrome profile and existing portal reader/session handoff first, then add explicit human approval/payment controls before any supplier order placement.
- Phase I launch-readiness summary/page.
- Production unit serials, QA logs, and packaging stages.
- Richer movement audit/running-balance view.
- Consider adding inline action-state messages to `/suppliers` item sourcing saves if operators need friendlier custom-supplier validation feedback.
